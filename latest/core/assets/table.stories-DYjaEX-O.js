import{x as i,aa as d}from"./iframe-5Lm7tces.js";import{n as c,e as u}from"./ref-BFiSCbdZ.js";import{S as b}from"./table-DAvlEXFI.js";import"./card-D6dSe4S3.js";import"./badge-CtFOAM_C.js";import"./dropdown-D91bcjY_.js";import"./segmented-control-BY9GGwwp.js";import"./context-menu-iUnR_dmG.js";import"./link-CynKkQFJ.js";import"./input-ntYZtpAI.js";import"./img-CeRDAxT8.js";import"./formatted-account-Z2FQv7yv.js";import"./formatted-number-M4g4Pscf.js";import"./formatted-date-4CWsiHhw.js";import"./pagination-YkKi6m3U.js";import"./dot-grid-one-horizontal-DAdVwFsM.js";import"./arrow-rotate-counter-clockwise.component-BaIz-qe8.js";import"./chevron-right-CQWZu8Q4.js";import"./zoom-out.component-DWi1IxGS.js";import"./circle-check-BWAPXQ3H.js";import"./cross-small-CStRZ7oo.js";import{a as Je}from"./argTableProps-S8_F2fR7.js";import"./preload-helper-Dp1pzeXC.js";import"./localized-decorator-BtCNmkdo.js";import"./popover.component-DCru0COF.js";import"./floating-ui.dom-B1INwmQZ.js";import"./cross-small.component-C1-DW-dm.js";import"./icon.component-CxiioWC5.js";import"./dropdown.component-4HA-RhFd.js";import"./query-async-Bc2zrltU.js";import"./form-control-host.style-CyRug889.js";import"./form-control-header.component-BAo3pxK9.js";import"./badge.component-Bj7HwO9g.js";import"./flex.component-CnYXrlhu.js";import"./triangle-exclamation.component-BmhKpI1G.js";import"./card.component-_D-2OKVL.js";import"./unwrap-slots-BUEq3SeE.js";import"./checkmark.component-3jlRh9y4.js";import"./rbcb-toggle.template-CCu8OBQ9.js";import"./chevron-bottom.component-94SivROE.js";import"./menu-heading.component-DgAv2K9k.js";import"./context-menu.component-B4_VjvTW.js";import"./alert.component-C8zYWYyc.js";import"./circle-check.component-nW0sH0YD.js";import"./blur.component-q9oDKbUE.js";import"./breadcrumb.component-EiiHu3D3.js";import"./chevron-left.component-BBn6lPBM.js";import"./text-BYkRAgbf.js";import"./default-typography.styles-DNCV_-q-.js";import"./calendar.component-DsHW6AbQ.js";import"./card-linked.component-P3Fg5Ybf.js";import"./props-link-BtU942D1.js";import"./checkbox-group.component-80e5CdcV.js";import"./toggle-control-base.component-DgIC3AxH.js";import"./minus-small.component-Cs3ahbsh.js";import"./coachmark.component-CWAHm-m7.js";import"./datepicker.component-DKrQ1ioF.js";import"./attribute-converters-CdI0trPX.js";import"./calender-add.component-BYAvflhD.js";import"./chevron-right.component-Cq1kZ2-r.js";import"./details.component-MUdZ9wzZ.js";import"./divider.component-DahUNfAn.js";import"./signal.component-CZY0uGqQ.js";import"./filter-chips.component-DOQTCqCN.js";import"./resize-observer-B9k8v2TZ.js";import"./summary.component-BrF9dXtm.js";import"./arrow-up.component-CGJP3iDH.js";import"./grouped-list.component-CqYNRmCA.js";import"./arrow-down.component-cPkP1Rfk.js";import"./arrow-left.component-BCQwlXRa.js";import"./arrow-right.component-DAwifrbs.js";import"./sun.component-BWDT9rmD.js";import"./star.component-dtkGL-I_.js";import"./bubbles.component-DzpsdqNI.js";import"./chain-link-B8GqXosA.js";import"./chevron-right-small.component-BZMvL1Lg.js";import"./chevron-top.component-B5y-qct-.js";import"./circles-three.component--u26FKnR.js";import"./cloudy-sun.component-9NaSIDdO.js";import"./credit-card.component-CE4A14sc.js";import"./folder.component-DNNX6VJi.js";import"./magnifying-glass.component-CF5BnKQg.js";import"./people-profile.component-CHWZ0toX.js";import"./push.component-BOHbEOWG.js";import"./rocket.component-C4XIN0o8.js";import"./square-grid-circle.component-CemvRq_V.js";import"./img.component-D8C77vGV.js";import"./input.component-DxwS5kiU.js";import"./textarea.component-DXbugGqt.js";import"./mask.component-DzacVv-x.js";import"./menu-button.component-B3L0YppQ.js";import"./radio-group.component-DTQY1G8g.js";import"./rich-text.component-DV8CYwE5.js";import"./sensitive-number.component-BBZ40vYw.js";import"./sensitive-date.component-C8fcW6QW.js";import"./formatted-date.component-CPdBWhpo.js";import"./formatted-text-CWozVfPh.js";import"./sensitive-account.component-IwlCeejl.js";import"./select.component-CYTl7DkR.js";import"./spinner.component-CTPNSS9h.js";import"./video.component-BD-aGnRX.js";import"./card-pattern-01.component-D4WdN9sh.js";const Xe="https://api.seb.io/components/table/table.users.json",Ze="https://api.seb.io/components/table/table.feedback.json";let F=null,h=null,j=null,f=null;const et=e=>({...e,name:b(e.name,["avatar","value"]),email:b(e.email,["value","copy-button"]),status:b(e.status,["status"]),amount:b(e.amount,["amount","currency"]),account:b(e.account,["main"]),login:b(e.lastLogin,["main"]),download:b(e.download??"#",["main"])});let O=null,y=null;const tt=async()=>O||y||(y=fetch(Xe).then(e=>e.json()).then(e=>(O=e,e)),y),at=async()=>F||h||(h=tt().then(e=>e.map(et)).then(e=>(F=e,e)),h),Qe=e=>async t=>{let a=await e();if(t.searchQuery){const l=t.searchQuery.toLowerCase();a=a.filter(m=>Object.values(m).some(r=>r==null?void 0:r.toString().toLowerCase().includes(l)))}if(t.sortColumn&&a.length>0){const l=t.sortColumn;a=[...a].sort((m,r)=>{var N,V;const g=((N=m[l])==null?void 0:N.toString())??"",K=((V=r[l])==null?void 0:V.toString())??"";return t.sortDirection==="asc"?g.localeCompare(K):K.localeCompare(g)})}const o=(t.page-1)*t.rows,n=o+t.rows;return{rows:a.slice(o,n),total:a.length}},nt=Qe(at),s={Columns:[{key:"id",label:"ID",sortable:!1},{key:"name",label:"Name",sortable:!0},{key:"email",label:"Email",sortable:!0,justify:"space-between"},{key:"role",label:"Role",sortable:!0,visible:!1},{key:"status",label:"Status",sortable:!0},{key:"department",label:"Department",sortable:!0},{key:"amount",label:"Amount",sortable:!0,justify:"end"},{key:"account",label:"Account",sortable:!0},{key:"login",label:"Last Login",sortable:!0},{key:"download",label:"Download"}],Actions:{label:"Actions",justify:"end"},Tfoot:{label:"Totals",sticky:!0},Data:nt,SlotContent:e=>i`
      ${e.map(t=>i`
          <!-- name: avatar -->
          <gds-img
            src="${t.avatarUrl??"#"}"
            alt="${String(t.name)}"
            slot="name:${t.id}:avatar"
            width="xl"
            height="xl"
          ></gds-img>

          <!-- email: copy button -->
          <gds-button
            slot="email:${t.id}:copy-button"
            rank="tertiary"
            size="small"
          >
            <gds-icon-copy></gds-icon-copy>
          </gds-button>

          <!-- status: badge -->
          <gds-badge
            slot="status:${t.id}:status"
            variant="${String(t.status)==="Active"?"positive":"negative"}"
            size="small"
          >
            ${String(t.status)}
          </gds-badge>

          <!-- amount: formatted number -->
          <gds-formatted-number
            slot="amount:${t.id}:amount"
            .value=${t.amount}
          ></gds-formatted-number>

          <!-- amount: currency -->
          <gds-badge slot="amount:${t.id}:currency" size="small">
            SEK
          </gds-badge>

          <!-- account: formatted account -->
          <gds-formatted-account
            slot="account:${t.id}:main"
            account="${t.account}"
            format="seb-account"
          ></gds-formatted-account>

          <!-- login: formatted date -->
          <gds-formatted-date
            slot="login:${t.id}:main"
            .value="${String(t.login)}"
            locale="sv-SE"
            format="dateLong"
          ></gds-formatted-date>

          <!-- download: link with icon -->
          <gds-link
            slot="download:${t.id}:main"
            href="${t.download??"#"}"
            text-decoration="underline"
            download
          >
            Download file
            <gds-icon-cloud-download slot="trail"></gds-icon-cloud-download>
          </gds-link>

          <!-- actions: context menu -->
          <gds-context-menu slot="actions:${t.id}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit ${String(t.name)}</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,TfootSlotContent:e=>{const t=e.reduce((l,m)=>l+(Number(m.amount)||0),0),a=e.filter(l=>String(l.status)==="Active").length,o=Math.round(a/e.length*100),n=new Set(e.map(l=>l.department)).size;return i`
      <!-- tfoot: name row count -->
      <span slot="tfoot:name">${e.length} Users</span>

      <!-- tfoot: status active rate -->
      <gds-badge
        slot="tfoot:status"
        variant="${o>=70?"positive":o>=40?"warning":"negative"}"
        size="small"
      >
        ${o}% active
      </gds-badge>

      <!-- tfoot: department count -->
      <gds-badge slot="tfoot:department" variant="information" size="small">
        ${n} depts
      </gds-badge>

      <!-- tfoot: amount sum -->
      <gds-flex slot="tfoot:amount" gap="s" align-items="center">
        <gds-formatted-number .value=${t}></gds-formatted-number>
        <gds-badge size="small">SEK</gds-badge>
      </gds-flex>
    `}},st=async()=>j||f||(f=fetch(Ze).then(e=>e.json()).then(e=>(j=e,e)),f),ot=Qe(st),q={Columns:[{key:"name",label:"Name",sortable:!0,align:"start"},{key:"feedback",label:"Feedback",align:"start",width:"350px"},{key:"notes",label:"Notes",align:"start",width:"460px"},{key:"department",label:"Department",align:"start",width:"120px"},{key:"status",label:"Status",sortable:!0,align:"start",justify:"end",width:"100px"}],Data:ot},B=[{id:1,task:"Review contract",assignee:"Anna Svensson",priority:"High"},{id:2,task:"Update pricing",assignee:"Erik Lindgren",priority:"Medium"},{id:3,task:"Send invoice",assignee:"Maria Berg",priority:"Low"},{id:4,task:"Approve budget",assignee:"Johan Nilsson",priority:"High"}],rt=async e=>{const t=(e.page-1)*e.rows;return{rows:B.slice(t,t+e.rows),total:B.length}},p={Columns:[{key:"task",label:"Task",align:"center"},{key:"assignee",label:"Assignee",align:"center"},{key:"priority",label:"Priority",align:"center",width:"100px"}],MultipleActions:{label:"Actions",align:"center",justify:"start",sticky:!0},MultipleActionsSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-button
            slot="actions:${t.id??a+1}:main"
            rank="secondary"
            size="small"
            variant="positive"
          >
            <gds-icon-circle-check size="m"></gds-icon-circle-check>
          </gds-button>
          <gds-button
            slot="actions:${t.id??a+1}:main"
            rank="secondary"
            size="small"
            variant="negative"
          >
            <gds-icon-cross-small size="m"></gds-icon-cross-small>
          </gds-button>
        `)}
    `,ActionLink:{label:"Actions",align:"center",justify:"end",sticky:!0},ActionLinkSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-button
            slot="actions:${t.id??a+1}:main"
            size="small"
            rank="secondary"
          >
            View details
          </gds-button>
        `)}
    `,ActionContextMenu:{label:"Actions",align:"center",justify:"end",sticky:!0},ActionContextMenuSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-context-menu slot="actions:${t.id??a+1}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit</gds-menu-item>
            <gds-menu-item>Archive</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,Data:rt},G=new WeakSet,lt=e=>i`
  <div
    ${c(t=>{if(!t||G.has(t))return;G.add(t);const a=new IntersectionObserver(o=>{if(o[0].isIntersecting){a.disconnect();const n=e();d(n,t);const l=t.parentNode;if(l){for(;t.firstChild;)l.insertBefore(t.firstChild,t);l.removeChild(t)}}},{rootMargin:"200px"});a.observe(t)})}
    style="min-height:40vh"
  ></div>
`,an={title:"Components/Table",component:"gds-table",argTypes:{...Je("gds-table"),density:{control:{type:"select"},options:["comfortable","compact","spacious"],defaultValue:"comfortable"},variant:{control:{type:"select"},options:["primary","secondary","tertiary"],defaultValue:"secondary"},selectable:{control:{type:"boolean"},defaultValue:!0},searchLabel:{control:{type:"text"}},height:{control:{type:"string"},defaultValue:!1}},tags:["autodocs"],decorators:[(e,t)=>t.viewMode==="docs"?lt(()=>e()):e()],parameters:{docs:{description:{component:`
A data table component that handles asynchronous data loading, pagination, sorting, and filtering through a provider function. Supports row selection, searchable columns, responsive layouts, and customizable cell rendering including badges, buttons, links, and formatted values.
        `}}}},v={args:{columns:s.Columns,actions:s.Actions,density:"comfortable",variant:"secondary",data:s.Data,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?height="${e.height}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        rows="60"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},$={args:{columns:s.Columns,data:s.Data,selectable:!0,density:"comfortable"},parameters:{docs:{description:{story:`
The \`selectable\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \`gds-table-selection\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .density="${e.density}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},w={args:{columns:s.Columns,data:s.Data,searchable:!0,settings:!0},parameters:{docs:{description:{story:`
#### Search Functionality
The \`searchable\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \`settings\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},C={args:{columns:s.Columns,data:s.Data,searchable:!0,settings:!0,nocache:!0},parameters:{docs:{description:{story:"\nThe `nocache` property provides direct control over data retrieval. When set to `true`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.\n\n> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?nocache="${e.nocache}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},S={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:"\nSorting is enabled by setting `sortable: true` on column configuration. <br />\nClicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />\nThe sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a `gds-table-data-loaded` event with the sorted data.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},x={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},k={args:{columns:q.Columns,data:q.Data},parameters:{docs:{description:{story:`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        `}}},render:e=>i`
    <gds-table .columns="${e.columns}" .data="${e.data}"></gds-table>
  `},D={args:{columns:p.Columns,actions:p.MultipleActions,data:p.Data},parameters:{docs:{description:{story:`
The \`actions\` property defines row interactions shown as the last cell of each row.

Supports all cell types (button, link, context-menu, badge, etc.) and can contain multiple actions.

When passed as an object \`{ label, align, justify, sticky }\`, it configures the actions column header and cell layout.
The actual action content is provided through named slots using the \`actions:rowKey:main\` convention.

#### Sticky Actions
Set \`sticky: true\` on the actions configuration to pin the actions column to the right edge of the table during horizontal scrolling. This ensures action buttons remain accessible even when the table content overflows horizontally.
        `}}},render:e=>{const t=u(),a=u(),o=u();return i`
      <gds-table
        ${c(t)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        @gds-table-data-loaded=${n=>{const l=t.value;l&&d(p.MultipleActionsSlotContent(n.detail.rows),l)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${c(a)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${p.ActionContextMenu}"
        @gds-table-data-loaded=${n=>{const l=a.value;l&&d(p.ActionContextMenuSlotContent(n.detail.rows),l)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${c(o)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${p.ActionLink}"
        @gds-table-data-loaded=${n=>{const l=o.value;l&&d(p.ActionLinkSlotContent(n.detail.rows),l)}}
      >
      </gds-table>
    `}},R={args:{columns:s.Columns,actions:s.Actions,density:"comfortable",variant:"secondary",data:s.Data,tfoot:s.Tfoot,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},parameters:{docs:{description:{story:`
The \`tfoot\` property adds a \`<tfoot>\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \`label\` is provided, the first cell renders as a \`<th scope="row">\` for proper accessibility semantics.

#### Configuration

\`\`\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\`\`\`

#### Slot Convention

Table footer slots use the naming pattern \`tfoot:{columnKey}\`:

\`\`\`html
<gds-formatted-number slot="tfoot:amount" .value=\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\${count} active</gds-badge>
\`\`\`

Compute your aggregation in the \`gds-table-data-loaded\` handler and render slot content into the table.

When \`sticky: true\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(i`${s.SlotContent(o.detail.rows)}
          ${s.TfootSlotContent(o.detail.rows)}`,n)};return i`
      <gds-table
        ${c(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        height="${e.height}"
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        .tfoot="${e.tfoot}"
        rows="50"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},L={args:{columns:s.Columns,data:s.Data,headline:"User Management",headlineTag:"h2",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:"\nWhen the `headline` and `summary` properties are set, they will be displayed at the top of the table.\n\nYou can change the `headline-tag` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        headline="${e.headline}"
        headline-tag="${e.headlineTag}"
        summary="${e.summary}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},A={args:{columns:s.Columns,data:s.Data,plain:!0},parameters:{docs:{description:{story:"\nWhen `plain` boolean is set, the table header and footer are removed, rendering only the data table.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?plain="${e.plain}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},U={args:{columns:s.Columns,data:s.Data,striped:!0,density:"compact"},parameters:{docs:{description:{story:"\nWhen `striped` boolean is set, alternating row background colors are applied to even-numbered rows.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .density="${e.density}"
        ?striped="${e.striped}"
        .columns="${e.columns}"
        .data="${e.data}"
        rows="20"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},E={args:{columns:s.Columns,data:s.Data,density:"compact",headline:"User Management",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:'\nThe `density` property controls the table\'s visual spacing and affects the table\'s **built-in UI** (search input, column settings dropdown, pagination).\n\n- **`comfortable`** (Default) · Standard spacing for balanced readability\n- **`compact`** · Reduced spacing to maximize information density\n- **`spacious`** · Increased spacing for enhanced readability\n\n> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use `size="small"` on badges and buttons in `compact` or `comfortable` mode, and `size="medium"` in `spacious` mode to maintain visual consistency.\n        '}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        headline="${e.headline}"
        summary="${e.summary}"
        searchable
        settings
        .density="${e.density}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},T={args:{columns:s.Columns,data:s.Data,variant:"secondary",density:"comfortable"},argTypes:{variant:{control:"select",options:["primary","secondary","tertiary"]}},parameters:{docs:{description:{story:"\nThe `variant` property controls the visual style of the table container card.\nThese variants can be used: `primary`, `secondary` (Default), `tertiary` \n\n> Variant naming subject to change on the next major release\n        "}}},render:e=>{const t=e.variant==="tertiary"?"primary":"tertiary",a=u(),o=n=>{const l=a.value;l&&d(s.SlotContent(n.detail.rows),l)};return i`
      <gds-card variant="${t}" padding="l" border-radius="m">
        <gds-table
          ${c(a)}
          .density="${e.density}"
          .variant="${e.variant}"
          .columns="${e.columns}"
          .data="${e.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=${o}
        >
        </gds-table>
      </gds-card>
    `}},z={args:{columns:s.Columns,data:s.Data,responsive:!0},parameters:{docs:{description:{story:`
When the \`responsive\` boolean is set to \`true\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**: 
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?responsive="${e.responsive}"
        .columns="${e.columns}"
        .data="${e.data}"
        height="80vh"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},P={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content. 
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=${a}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    `}},M={args:{columns:s.Columns,data:s.Data,simulatedState:"normal"},argTypes:{simulatedState:{control:"select",options:["normal","error","empty","no-results"],description:"Simulate different table states"}},parameters:{docs:{description:{story:`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        `}}},render:e=>{const t=u(),a=async m=>{if(await new Promise(r=>setTimeout(r,500)),e.simulatedState==="error")throw new Error("Simulated error state");return e.simulatedState==="empty"?{rows:[],total:0}:e.simulatedState==="no-results"?{rows:[],total:0}:e.data(m)},o=m=>{const r=t.value;r&&d(s.SlotContent(m.detail.rows),r)},n=()=>{e.simulatedState="normal",t.value&&(t.value.dataLoadKey=`retry-${Date.now()}`)},l=()=>{var m;if(e.simulatedState="normal",t.value){t.value.dataLoadKey=`clear-${Date.now()}`;const r=(m=t.value.shadowRoot)==null?void 0:m.querySelector("gds-input");r&&(r.value="")}};return setTimeout(()=>{var m;if(e.simulatedState==="no-results"&&t.value){const r=(m=t.value.shadowRoot)==null?void 0:m.querySelector("gds-input");r&&(r.value="test search",r.dispatchEvent(new Event("input",{bubbles:!0})))}},100),i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${a}"
        .dataLoadKey="${e.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=${o}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-book-m">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${n}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-book-m">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-book-m">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${l}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    `}},W={args:{columns:s.Columns,data:s.Data,headline:"User Management",summary:"Overview of all users in the system",selectable:!0,searchable:!0,settings:!0,density:"comfortable"},parameters:{docs:{description:{story:`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \`key\` parameter in \`Slot()\`, or it falls back to \`row.id\`, then the row index.

#### 1. Compose data with \`Slot()\`

\`\`\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\`\`\`

The arguments are positional: \`Slot(value, slots, key)\` or \`Slot(value, key, slots)\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \`'value'\` to render the plain text alongside your components), and **key** is an optional \`string | number\` used as a stable row identifier.

#### 2. Render slot content on data load

\`\`\`ts
@gds-table-data-loaded=\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\`\`\`

#### 3. Slot naming convention

The slot name follows the pattern \`columnKey:rowKey:slotId\`.

\`\`\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\`\`\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \`Slot()\` API.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        .headline="${e.headline}"
        .summary="${e.summary}"
        ?selectable="${e.selectable}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        density="${e.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},I={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
The table component dispatches events for various user interactions. 

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \`{ page: number }\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \`{ rows: number }\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

This example demonstrates listening to pagination and sorting events and displaying the current state.
        `}}},render:e=>{const t=u(),a={currentPage:1,currentRows:10,sortColumn:"",sortDirection:"asc"},o=r=>{const g=t.value;g&&d(s.SlotContent(r.detail.rows),g)},n=r=>{console.log("🔔 Page change event:",r.detail),a.currentPage=r.detail.page;const g=document.querySelector("#page-status");g&&(g.textContent=`Page: ${a.currentPage}`)},l=r=>{console.log("🔔 Rows change event:",r.detail),a.currentRows=r.detail.rows;const g=document.querySelector("#rows-status");g&&(g.textContent=`Rows: ${a.currentRows}`)},m=r=>{console.log("🔔 Sort change event:",r.detail),a.sortColumn=r.detail.sortColumn,a.sortDirection=r.detail.sortDirection;const g=document.querySelector("#sort-status");g&&(g.textContent=`${a.sortColumn} (${a.sortDirection})`)};return i`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="secondary" padding="m" border-radius="m">
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-book-s" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    Page: ${a.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    Rows: ${a.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    ${a.sortColumn?`${a.sortColumn} (${a.sortDirection})`:"None"}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          ${c(t)}
          .columns="${e.columns}"
          .data="${e.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=${o}
          @gds-page-change=${r=>n(r)}
          @gds-rows-change=${r=>l(r)}
          @gds-sort-change=${r=>m(r)}
        >
        </gds-table>
      </gds-flex>
    `}};var H,_,Y;v.parameters={...v.parameters,docs:{...(H=v.parameters)==null?void 0:H.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    actions: Users.Actions,
    density: 'comfortable',
    variant: 'secondary',
    data: Users.Data,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?height="\${args.height}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        rows="60"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Y=(_=v.parameters)==null?void 0:_.docs)==null?void 0:Y.source}}};var Q,J,X;$.parameters={...$.parameters,docs:{...(Q=$.parameters)==null?void 0:Q.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    selectable: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`selectable\\\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \\\`gds-table-selection\\\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(X=(J=$.parameters)==null?void 0:J.docs)==null?void 0:X.source}}};var Z,ee,te;w.parameters={...w.parameters,docs:{...(Z=w.parameters)==null?void 0:Z.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
#### Search Functionality
The \\\`searchable\\\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \\\`settings\\\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(te=(ee=w.parameters)==null?void 0:ee.docs)==null?void 0:te.source}}};var ae,ne,se;C.parameters={...C.parameters,docs:{...(ae=C.parameters)==null?void 0:ae.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true,
    nocache: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`nocache\\\` property provides direct control over data retrieval. When set to \\\`true\\\`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.

> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?nocache="\${args.nocache}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(se=(ne=C.parameters)==null?void 0:ne.docs)==null?void 0:se.source}}};var oe,re,le;S.parameters={...S.parameters,docs:{...(oe=S.parameters)==null?void 0:oe.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Sorting is enabled by setting \\\`sortable: true\\\` on column configuration. <br />
Clicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />
The sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a \\\`gds-table-data-loaded\\\` event with the sorted data.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(le=(re=S.parameters)==null?void 0:re.docs)==null?void 0:le.source}}};var ie,de,ce;x.parameters={...x.parameters,docs:{...(ie=x.parameters)==null?void 0:ie.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ce=(de=x.parameters)==null?void 0:de.docs)==null?void 0:ce.source}}};var ue,me,ge;k.parameters={...k.parameters,docs:{...(ue=k.parameters)==null?void 0:ue.docs,source:{originalSource:`{
  args: {
    columns: Feedback.Columns,
    data: Feedback.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        \`
      }
    }
  },
  render: args => html\`
    <gds-table .columns="\${args.columns}" .data="\${args.data}"></gds-table>
  \`
}`,...(ge=(me=k.parameters)==null?void 0:me.docs)==null?void 0:ge.source}}};var pe,be,he;D.parameters={...D.parameters,docs:{...(pe=D.parameters)==null?void 0:pe.docs,source:{originalSource:`{
  args: {
    columns: ActionsData.Columns,
    actions: ActionsData.MultipleActions,
    data: ActionsData.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`actions\\\` property defines row interactions shown as the last cell of each row.

Supports all cell types (button, link, context-menu, badge, etc.) and can contain multiple actions.

When passed as an object \\\`{ label, align, justify, sticky }\\\`, it configures the actions column header and cell layout.
The actual action content is provided through named slots using the \\\`actions:rowKey:main\\\` convention.

#### Sticky Actions
Set \\\`sticky: true\\\` on the actions configuration to pin the actions column to the right edge of the table during horizontal scrolling. This ensures action buttons remain accessible even when the table content overflows horizontally.
        \`
      }
    }
  },
  render: args => {
    const multiRef = createRef<any>();
    const ctxRef = createRef<any>();
    const linkRef = createRef<any>();
    return html\`
      <gds-table
        \${ref(multiRef)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = multiRef.value;
      if (table) render(ActionsData.MultipleActionsSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(ctxRef)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${ActionsData.ActionContextMenu}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = ctxRef.value;
      if (table) render(ActionsData.ActionContextMenuSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(linkRef)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${ActionsData.ActionLink}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = linkRef.value;
      if (table) render(ActionsData.ActionLinkSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>
    \`;
  }
}`,...(he=(be=D.parameters)==null?void 0:be.docs)==null?void 0:he.source}}};var fe,ye,ve;R.parameters={...R.parameters,docs:{...(fe=R.parameters)==null?void 0:fe.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    actions: Users.Actions,
    density: 'comfortable',
    variant: 'secondary',
    data: Users.Data,
    tfoot: Users.Tfoot,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`tfoot\\\` property adds a \\\`<tfoot>\\\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \\\`label\\\` is provided, the first cell renders as a \\\`<th scope="row">\\\` for proper accessibility semantics.

#### Configuration

\\\`\\\`\\\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\\\`\\\`\\\`

#### Slot Convention

Table footer slots use the naming pattern \\\`tfoot:{columnKey}\\\`:

\\\`\\\`\\\`html
<gds-formatted-number slot="tfoot:amount" .value=\\\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\\\${count} active</gds-badge>
\\\`\\\`\\\`

Compute your aggregation in the \\\`gds-table-data-loaded\\\` handler and render slot content into the table.

When \\\`sticky: true\\\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(html\`\${Users.SlotContent(e.detail.rows)}
          \${Users.TfootSlotContent(e.detail.rows)}\`, table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        height="\${args.height}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        .tfoot="\${args.tfoot}"
        rows="50"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ve=(ye=R.parameters)==null?void 0:ye.docs)==null?void 0:ve.source}}};var $e,we,Ce;L.parameters={...L.parameters,docs:{...($e=L.parameters)==null?void 0:$e.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    headlineTag: 'h2',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`headline\\\` and \\\`summary\\\` properties are set, they will be displayed at the top of the table.

You can change the \\\`headline-tag\\\` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        headline-tag="\${args.headlineTag}"
        summary="\${args.summary}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ce=(we=L.parameters)==null?void 0:we.docs)==null?void 0:Ce.source}}};var Se,xe,ke;A.parameters={...A.parameters,docs:{...(Se=A.parameters)==null?void 0:Se.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    plain: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`plain\\\` boolean is set, the table header and footer are removed, rendering only the data table.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?plain="\${args.plain}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ke=(xe=A.parameters)==null?void 0:xe.docs)==null?void 0:ke.source}}};var De,Re,Le;U.parameters={...U.parameters,docs:{...(De=U.parameters)==null?void 0:De.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    striped: true,
    density: 'compact'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`striped\\\` boolean is set, alternating row background colors are applied to even-numbered rows.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?striped="\${args.striped}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="20"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Le=(Re=U.parameters)==null?void 0:Re.docs)==null?void 0:Le.source}}};var Ae,Ue,Ee;E.parameters={...E.parameters,docs:{...(Ae=E.parameters)==null?void 0:Ae.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    density: 'compact',
    headline: 'User Management',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`density\\\` property controls the table's visual spacing and affects the table's **built-in UI** (search input, column settings dropdown, pagination).

- **\\\`comfortable\\\`** (Default) · Standard spacing for balanced readability
- **\\\`compact\\\`** · Reduced spacing to maximize information density
- **\\\`spacious\\\`** · Increased spacing for enhanced readability

> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use \\\`size="small"\\\` on badges and buttons in \\\`compact\\\` or \\\`comfortable\\\` mode, and \\\`size="medium"\\\` in \\\`spacious\\\` mode to maintain visual consistency.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        summary="\${args.summary}"
        searchable
        settings
        .density="\${args.density}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ee=(Ue=E.parameters)==null?void 0:Ue.docs)==null?void 0:Ee.source}}};var Te,ze,Pe;T.parameters={...T.parameters,docs:{...(Te=T.parameters)==null?void 0:Te.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    variant: 'secondary',
    density: 'comfortable'
  },
  argTypes: {
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'tertiary']
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`variant\\\` property controls the visual style of the table container card.
These variants can be used: \\\`primary\\\`, \\\`secondary\\\` (Default), \\\`tertiary\\\` 

> Variant naming subject to change on the next major release
        \`
      }
    }
  },
  render: args => {
    const wrapperVariant = args.variant === 'tertiary' ? 'primary' : 'tertiary';
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-card variant="\${wrapperVariant}" padding="l" border-radius="m">
        <gds-table
          \${ref(tableRef)}
          .density="\${args.density}"
          .variant="\${args.variant}"
          .columns="\${args.columns}"
          .data="\${args.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=\${handleDataLoaded}
        >
        </gds-table>
      </gds-card>
    \`;
  }
}`,...(Pe=(ze=T.parameters)==null?void 0:ze.docs)==null?void 0:Pe.source}}};var Me,We,Ie;z.parameters={...z.parameters,docs:{...(Me=z.parameters)==null?void 0:Me.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    responsive: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`responsive\\\` boolean is set to \\\`true\\\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**: 
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?responsive="\${args.responsive}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        height="80vh"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ie=(We=z.parameters)==null?void 0:We.docs)==null?void 0:Ie.source}}};var Fe,je,Oe;P.parameters={...P.parameters,docs:{...(Fe=P.parameters)==null?void 0:Fe.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content. 
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    \`;
  }
}`,...(Oe=(je=P.parameters)==null?void 0:je.docs)==null?void 0:Oe.source}}};var Ke,Ne,Ve;M.parameters={...M.parameters,docs:{...(Ke=M.parameters)==null?void 0:Ke.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    simulatedState: 'normal'
  },
  argTypes: {
    simulatedState: {
      control: 'select',
      options: ['normal', 'error', 'empty', 'no-results'],
      description: 'Simulate different table states'
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const dataProvider = async (request: any) => {
      await new Promise(resolve => setTimeout(resolve, 500));
      if (args.simulatedState === 'error') {
        throw new Error('Simulated error state');
      }
      if (args.simulatedState === 'empty') {
        return {
          rows: [],
          total: 0
        };
      }
      if (args.simulatedState === 'no-results') {
        return {
          rows: [],
          total: 0
        };
      }
      return args.data(request);
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handleRetry = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`retry-\${Date.now()}\`;
      }
    };
    const handleClearSearch = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`clear-\${Date.now()}\`;
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = '';
        }
      }
    };

    // Simulate search query for no-results state
    setTimeout(() => {
      if (args.simulatedState === 'no-results' && tableRef.value) {
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = 'test search';
          searchInput.dispatchEvent(new Event('input', {
            bubbles: true
          }));
        }
      }
    }, 100);
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${dataProvider}"
        .dataLoadKey="\${args.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-book-m">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleRetry}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-book-m">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-book-m">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleClearSearch}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    \`;
  }
}`,...(Ve=(Ne=M.parameters)==null?void 0:Ne.docs)==null?void 0:Ve.source}}};var qe,Be,Ge;W.parameters={...W.parameters,docs:{...(qe=W.parameters)==null?void 0:qe.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    summary: 'Overview of all users in the system',
    selectable: true,
    searchable: true,
    settings: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \\\`key\\\` parameter in \\\`Slot()\\\`, or it falls back to \\\`row.id\\\`, then the row index.

#### 1. Compose data with \\\`Slot()\\\`

\\\`\\\`\\\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\\\`\\\`\\\`

The arguments are positional: \\\`Slot(value, slots, key)\\\` or \\\`Slot(value, key, slots)\\\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \\\`'value'\\\` to render the plain text alongside your components), and **key** is an optional \\\`string | number\\\` used as a stable row identifier.

#### 2. Render slot content on data load

\\\`\\\`\\\`ts
@gds-table-data-loaded=\\\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\\\`\\\`\\\`

#### 3. Slot naming convention

The slot name follows the pattern \\\`columnKey:rowKey:slotId\\\`.

\\\`\\\`\\\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\\\`\\\`\\\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \\\`Slot()\\\` API.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .headline="\${args.headline}"
        .summary="\${args.summary}"
        ?selectable="\${args.selectable}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        density="\${args.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ge=(Be=W.parameters)==null?void 0:Be.docs)==null?void 0:Ge.source}}};var He,_e,Ye;I.parameters={...I.parameters,docs:{...(He=I.parameters)==null?void 0:He.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table component dispatches events for various user interactions. 

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \\\`{ page: number }\\\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \\\`{ rows: number }\\\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \\\`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\\\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

This example demonstrates listening to pagination and sorting events and displaying the current state.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const state = {
      currentPage: 1,
      currentRows: 10,
      sortColumn: '',
      sortDirection: 'asc'
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handlePageChange = (e: CustomEvent) => {
      console.log('🔔 Page change event:', e.detail);
      state.currentPage = e.detail.page;
      const pageEl = document.querySelector('#page-status');
      if (pageEl) {
        pageEl.textContent = \`Page: \${state.currentPage}\`;
      }
    };
    const handleRowsChange = (e: CustomEvent) => {
      console.log('🔔 Rows change event:', e.detail);
      state.currentRows = e.detail.rows;
      const rowsEl = document.querySelector('#rows-status');
      if (rowsEl) {
        rowsEl.textContent = \`Rows: \${state.currentRows}\`;
      }
    };
    const handleSortChange = (e: CustomEvent) => {
      console.log('🔔 Sort change event:', e.detail);
      state.sortColumn = e.detail.sortColumn;
      state.sortDirection = e.detail.sortDirection;
      const sortEl = document.querySelector('#sort-status');
      if (sortEl) {
        sortEl.textContent = \`\${state.sortColumn} (\${state.sortDirection})\`;
      }
    };
    return html\`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="secondary" padding="m" border-radius="m">
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-book-s" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    Page: \${state.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    Rows: \${state.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-book-s" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-regular-m"
                    color="primary"
                  >
                    \${state.sortColumn ? \`\${state.sortColumn} (\${state.sortDirection})\` : 'None'}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          \${ref(tableRef)}
          .columns="\${args.columns}"
          .data="\${args.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=\${handleDataLoaded}
          @gds-page-change=\${(e: CustomEvent) => handlePageChange(e)}
          @gds-rows-change=\${(e: CustomEvent) => handleRowsChange(e)}
          @gds-sort-change=\${(e: CustomEvent) => handleSortChange(e)}
        >
        </gds-table>
      </gds-flex>
    \`;
  }
}`,...(Ye=(_e=I.parameters)==null?void 0:_e.docs)==null?void 0:Ye.source}}};const nn=["Overview","Selection","Filtering","Caching","Sorting","Cell","Formatting","Actions","Tfoot","Headline","Plain","Striped","Density","Variant","Responsive","Slots","StateSlots","SlotComposition","Events"];export{D as Actions,C as Caching,x as Cell,E as Density,I as Events,w as Filtering,k as Formatting,L as Headline,v as Overview,A as Plain,z as Responsive,$ as Selection,W as SlotComposition,P as Slots,S as Sorting,M as StateSlots,U as Striped,R as Tfoot,T as Variant,nn as __namedExportsOrder,an as default};
