import { Meta, Canvas } from '@storybook/addon-docs/blocks'

<Meta title="Utilities/Flexbox/Align content" />

# Flexbox align content

This utility class can be applied to elements with `display: flex` or `display: inline-flex`. Some common cases include `.row`, `.d-flex` etc.

**Class:** `align-content-{breakpoint}-{value}` <br />
**Values:** `start`,`between`,`center`,`stretch`,`around`,`end` <br />
**Responsive:** true

## Examples

### Responsive

Coming soon!
