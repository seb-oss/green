import { NggButtonComponent } from './button.component'
import { Canvas, Story, Meta } from '@storybook/addon-docs'
import * as Stories from './button.stories.ts'

<Meta of={Stories} />

<style>{`
    div[id^=story--] > div{
        height: auto !important;
    }
`}</style>

# Buttons

Green buttons are native `<button>` elements enchanced with SEB Green styling. Apply the `ngg-button` attribute to greenify a button.

{' '}

<Story of={Stories.Default} />

## Types

<Canvas of={Stories.Types} />

## Variants

<Canvas of={Stories.Variants} />

## Sizes

<Canvas of={Stories.Sizes} />
