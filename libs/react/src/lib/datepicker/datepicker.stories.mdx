import { Datepicker } from './datepicker'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

export const Template = ({ align, ...props }) => <Datepicker {...props} />

<Meta title="Components/Datepicker" component={Datepicker} />

# Datepicker

Datepicker Component

<Story
  name="Datepicker"
  args={{
    label: 'Pick date',
    onChange: (newDate) => console.log('New date selected: ', newDate),
    selectedDate: new Date(),
  }}
  parameters={{
    componentIds: ['component-datepicker'],
  }}
>
  {Template.bind({})}
</Story>

### Available props

<ArgsTable />

Props are forwarded to the underlying web component, so anything supported by `gds-datepicker` can be used. See https://storybook.seb.io/latest/core/?path=/docs/components-datepicker--page for more details.

### Localization

Localization is handled by Lit Localize in the underlying web component. You can set the locale like this:

```ts
// Import the `gdsInitLocalization` function from Green Core
import { gdsInitLocalization } from '@sebgroup/green-core/localization'

// Then initialize Lit Localize by calling our wrapper function. This returns a `setLocale` function which you can use to set the locale.
const { setLocale } = gdsInitLocalization()

// Later in your code you can set the locale like this:
setLocale('sv')
```

Currently, only translations for Swedish and English is built in.
