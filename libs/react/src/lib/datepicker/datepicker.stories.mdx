import { Datepicker } from './datepicker'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

export const Template = ({ align, ...props }) => <Datepicker {...props} />

<Meta title="Components/Datepicker" component={Datepicker} />

# Datepicker

Datepicker Component

<Story 
  name="Datepicker"
  args={{
    onChange: (newDate) => console.log('New date selected: ', newDate),
    selectedDate: new Date()
  }} 
  parameters={{
    componentIds: ['component-datepicker']
  }}>
  {Template.bind({})}
</Story>

### Available props

| Input (attribute) | Description                                                          | Default |
| :---------------- | :------------------------------------------------------------------- | :------ |
| onChange          | A function of the signature `(Date) => void`.                        |         |
| selectedDate      | A value of type `Date` or `string`. Sets the currently selected date.|         |
