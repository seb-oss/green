import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import Navbar from './navbar'

export const Template = ({ children, ...props }) => (
  <Navbar {...props}>{children}</Navbar>
)

# Navbar

Simple navbar component.

<Meta
  title="Components/Navbar"
  component={Navbar}
  argTypes={{
    variant: {
      options: [undefined, 'bg-light', 'bg-dark'],
    },
  }}
/>

### Transparent navbar with heading and brand and header as link

<Canvas>
  <Story name="Default" args={{ title: 'Page heading', titleLink: '#' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Light navbar with grid and additional content

<Canvas>
  <Navbar title="Page heading" variant="bg-light">
    <input type="text" placeholder="Input field" />
    <button type="button" className="ghost">
      Search
    </button>
  </Navbar>
</Canvas>

#### Available props

<ArgsTable of={Navbar} />
