import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Navbar from './navbar'

export const Template = ({ children, ...props }) => (
  <Navbar {...props}>{children}</Navbar>
)

# Navbar

Simple navbar component.

<Meta title="Components/Navbar" component={Navbar} />

### Transparent navbar with heading and brand as link

<Canvas>
  <Story name="Default" args={{ title: 'Page heading' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Dark navbar with heading and brand as link

<Canvas>
  <Story name="Dark" args={{ title: 'Page heading', variant: 'bg-dark' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Light navbar with heading and brand as link

<Canvas>
  <Story name="Light" args={{ title: 'Page heading', variant: 'bg-light' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Search"
    args={{
      title: 'Page heading',
      children: [
        <input type="text" placeholder="Input field" />,
        <button type="button" class="ghost">
          Search
        </button>,
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
