import { Meta, Story, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import Card from './card'
import Button from '../form/button/button'
import Link from '../link/link'
import * as CardStories from './card.stories';
import * as Core from '../../../../../dist/libs/core/src/generated/react/index.js'

export const Template = ({ children, ...props }) => (
  <Card {...props}>{children}</Card>
)

export const Header = () => (
  <>
    <h3>Card Headline</h3>
    <Button variant="close">
      <span className="sr-only">Close</span>
      <i></i>
    </Button>
  </>
)

export const Footer = () => (
  <>
    <Link href="https://seb.se" className="button link">
      Link
    </Link>
    <Button>Default button</Button>
    <Button variant="primary">Primary</Button>
  </>
)

<Meta of={CardStories} />

<Core.GdsTheme>
  <Core.GdsDiv margin="xl 0">
    <Core.GdsAlert variant="notice">**Note:** This version of Card is deprecated! Please use the [web component from Green Core](https://storybook.seb.io/latest/core/?path=/docs/components-card--docs) instead.</Core.GdsAlert>
  </Core.GdsDiv>
</Core.GdsTheme>

# Cards

A card is container for short content, related pieces of information, and action on a specific content.

<Canvas of={CardStories.Default} />

## Card without header

<Card>
  <p>
    <strong>Not passing header prop, but creating it in body</strong>
  </p>
  <p class="mb-5">
    Then you don't get the close button because it's styling is tied to the
    header
  </p>
  <div>
    <button class="primary">Primary button</button>
  </div>
</Card>

## Headline and text content
<Card header={<h3>Card one headline</h3>}>Card one content text</Card>
<br /><br />
<Card header={<h3>Card two headline</h3>} />

## Card with button

<Card header={<Header />} footer={<Footer />}>
  Here is some content with link and two buttons footer
</Card>

## Props

<ArgTypes />
