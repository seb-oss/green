import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import Card from './card'
import Button from '../form/button/button'
import Link from '../link/link'

export const Template = ({ children, ...props }) => (
  <Card {...props}>{children}</Card>
)

export const Header = () => (
  <>
    <h3>Card Headline</h3>
    <Button variant="close">
      <span className="sr-only">Close</span>
      <i></i>
    </Button>
  </>
)

export const Footer = () => (
  <>
    <Link href="https://seb.se" className="button link">
      Link
    </Link>
    <Button>Default button</Button>
    <Button variant="primary">Primary</Button>
  </>
)

<Meta title="Components/Card" component={Card} />

# Cards

Simple Card Component

<Story
  name="Card"
  parameters={{
    componentIds: ['component-card']
  }}
  args={{ header: <Header />, footer: <Footer />, children: 'Card content' }}
>
  {Template.bind({})}
</Story>

#

## Headline and Text Content

<Canvas>
  <Card header={<h3>Card with Headline</h3>}>Here is some content</Card>
  <Card header={<h3>Card with Headline</h3>}></Card>
</Canvas>

## Card with Button

<Canvas>
  <Card header={<Header />} footer={<Footer />}>
    Here is some content with link and button footer
  </Card>
</Canvas>

<ArgsTable of={Card} />
