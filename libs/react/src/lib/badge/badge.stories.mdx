import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Badge from './badge'

export const Template = ({ ...props }) => <Badge {...props} />

<Meta title="Components/Badge" component={Badge} />

# Badge

Simple Badge Component

<Story
  name="Badge"
  parameters={{
    componentIds: ['component-badge'],
  }}
  args={{
    children: 'Primary',
    badgeType: 'info',
    isCloseable: false,
  }}
>
  {Template.bind({})}
</Story>

#

### Types

Badges are available in a number of different variants. Please note that the colors used in the examples below are not final yet.

<Canvas>
  <div className="badge-container">
    <Badge>Badge</Badge>
    <Badge badgeType="info">Information</Badge>
    <Badge badgeType="success">Success</Badge>
    <Badge badgeType="warning">Warning</Badge>
    <Badge badgeType="danger">Danger</Badge>
    <Badge badgeType="light">Light</Badge>
    <Badge badgeType="dark">Dark</Badge>
    <Badge customColor="#062BCC" customBackgroundColor="#E6F5FF">
      Custom
    </Badge>
  </div>
</Canvas>

### Dismissible Badges

Badges that can be dismissed/removed. Please note that this is currently not in the designs in Figma/Design Library and might be removed from code.

<Canvas>
  <div className="badge-container">
    <Badge isCloseable>Badge</Badge>
    <Badge badgeType="info" isCloseable>
      Information
    </Badge>
    <Badge badgeType="success" isCloseable>
      Success
    </Badge>
    <Badge badgeType="warning" isCloseable>
      Warning
    </Badge>
    <Badge badgeType="danger" isCloseable>
      Danger
    </Badge>
    <Badge badgeType="light" isCloseable>
      Light
    </Badge>
    <Badge badgeType="dark" isCloseable>
      Light
    </Badge>
    <Badge customColor="#062BCC" customBackgroundColor="#E6F5FF" isCloseable>
      Custom
    </Badge>
  </div>
</Canvas>
