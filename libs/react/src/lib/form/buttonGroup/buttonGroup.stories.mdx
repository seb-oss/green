import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { ButtonGroup } from './buttonGroup'
import { Button } from '../button/button'

export const Template = ({ children, ...props }) => (
  <ButtonGroup {...props}>
    <Button>1 month</Button>
    <Button>3 months</Button>
    <Button>This year</Button>
    <Button>1 year</Button>
    <Button>3 years</Button>
    <Button>5 years</Button>
    <Button>10 years</Button>
  </ButtonGroup>
)

<Meta title="Components/Form" component={ButtonGroup} />

# Buttons aka button group

Grouping Button Components

<Canvas>
  <Story
    name="Button Group"
    parameters={{
      componentIds: ['component-segmented-control'],
    }}
    args={{
      variant: 'ghost',
      selectedIndex: 4,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Sizes

<Canvas>
  <ButtonGroup variant="secondary" selectedIndex={2} size="small">
    <Button>Button 1</Button>
    <Button>Button 2</Button>
  </ButtonGroup>
</Canvas>
