import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Checkbox } from '../input/input'

export const CheckboxTemplate = ({ ...props }) => <Checkbox {...props} />

<Meta title="Components/Checkbox" component={Checkbox} />

# Checkbox

Simple Checkbox component

<Canvas>
  <Story
    name="Default"
    parameters={{
      componentIds: ['component-checkbox']
    }}
    args={{ label: 'Checkbox input', checked: true }}>
    {CheckboxTemplate.bind({})}
  </Story>
</Canvas>

## States and variants checkbox

<Canvas>
  <form>
    <Checkbox label="Normal unchecked" />
    <Checkbox label="Normal checked" checked />
  </form>
  <form>
    <Checkbox label="Valid unchecked" className="is-valid" />
    <Checkbox label="Valid checked" className="is-valid" checked />
  </form>
  <form>
    <Checkbox label="Invalid unchecked" className="is-invalid" />
    <Checkbox label="Invalid checked" className="is-invalid" checked />
  </form>
  <form>
    <Checkbox label="Disabled unchecked" disabled />
    <Checkbox label="Disabled checked" disabled checked />
  </form>
</Canvas>

## Validation

<Canvas>
  <Story name="invalid"
         args={{
           label: 'Invalid',
           validator: {
             indicator: 'error',
             message: 'Error'
           }}}
  >
    {CheckboxTemplate.bind({})}
  </Story>
</Canvas>

