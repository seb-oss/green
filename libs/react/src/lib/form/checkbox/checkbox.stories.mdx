import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Checkbox } from '../input/input'

export const CheckboxTemplate = ({ ...props }) => <Checkbox {...props} />

<Meta title="Components/Form/Checkbox" component={Checkbox} />

# Checkbox

Simple Checkbox component

<Canvas>
  <Story
    name="Default"
    parameters={{
      componentIds: ['component-checkbox']
    }}
    args={{ label: 'Checkbox input', checked: true }}>
    {CheckboxTemplate.bind({})}
  </Story>
</Canvas>

## States and variants checkbox

<Canvas>
  <form>
    <Checkbox label="Checkbox" />
    <Checkbox label="Checkbox" checked />
  </form>
  <form>
    <Checkbox label="Checkbox" className="is-valid" />
    <Checkbox label="Checkbox" className="is-valid" checked />
  </form>
  <form>
    <Checkbox label="Checkbox" className="is-invalid" />
    <Checkbox label="Checkbox" className="is-invalid" checked />
  </form>
  <form>
    <Checkbox label="Checkbox" disabled />
    <Checkbox label="Checkbox" disabled checked />
  </form>
</Canvas>

## Validation

<Canvas>
  <Story name="invalid"
    args={{ label: 'Invalid', validator: {
                                    indicator: 'error',
                                    message: 'Error'
                                   }}}
     >
    {CheckboxTemplate.bind({})}
  </Story>
</Canvas>

