import { Meta, Story, Canvas } from '@storybook/addon-docs'
import {
  Form,
  TextInput,
  EmailInput,
  NumberInput,
  Button,
  ButtonGroup,
  Checkbox,
  Text,
} from './'

export const Template = ({ children, ...props }) => (
  <Form {...props}>
    <h2>Inputs</h2>
    <TextInput id="namedField" label="Some field" />
    <EmailInput label="Email field" />
    <NumberInput label="Numeric field" value={3} min={-1} max={10} step={2} />
    <h2>Checkbox</h2>
    <Checkbox label="Some checkbox" checked={true} />
    <h2>Text</h2>
    <Text>Static text</Text>
    <h2>Button Group</h2>
    <ButtonGroup>
      <Button>Button 1</Button>
      <Button>Button 2</Button>
    </ButtonGroup>
    <ButtonGroup variant="ghost">
      <Button>Button 1</Button>
      <Button>Button 2</Button>
    </ButtonGroup>
    <h2>Buttons</h2>
    <Button type="reset">Reset</Button>
    <Button type="submit">Submit</Button>
  </Form>
)

<Meta title="Components/Form" component={Form} />

# Forms

<Canvas>
  <Story
    name="Form"
    args={{
      onSubmit: (e) => {
        e.preventDefault()
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
