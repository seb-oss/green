import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { TextInput, EmailInput, NumberInput, RenderInput } from './input'

export const TextInputTemplate = ({ ...props }) => <TextInput {...props} />
export const EmailInputTemplate = ({ ...props }) => <EmailInput {...props} />
export const NumberInputTemplate = ({ ...props }) => <NumberInput {...props} />

<Meta title="Components/Form/Input" component={RenderInput} />

# Inputs

### Text Input

<Canvas>
  <Story
    name="TextInput"
    parameters={{
      componentIds: ['component-input']
    }}
    args={{ label: 'Text input', info: 'Input some text here' }}
  >
    {TextInputTemplate.bind({})}
  </Story>
</Canvas>

### Email Input

<Canvas>
  <Story
    name="EmailInput"
    args={{ label: 'Email input', info: 'Only email input' }}
  >
    {EmailInputTemplate.bind({})}
  </Story>
</Canvas>

### Number Input

<Canvas>
  <Story
    name="NumberInput"
    args={{ label: 'Number input', info: 'Only number input' }}
  >
    {NumberInputTemplate.bind({})}
  </Story>
</Canvas>

### Validation

When placed after an input the form info also shows the state of the input if either `.is-valid` or `.is-invalid` has been added to the input or if the input is scoped to `.was-validated` (usually added to the form).

<Canvas>
  <TextInput
    label="Text Input"
    info="Info"
    validator={{ message: 'success', indicator: 'success' }}
  />
  <EmailInput
    label="Email Input"
    info="Info"
    validator={{ message: 'error', indicator: 'error' }}
  />
  <NumberInput
    label="Number Input"
    info="Info"
    validator={{ message: 'nuetral', indicator: 'info' }}
  />
</Canvas>

### Expandable information

<Canvas>
  <TextInput
    label="Text Input"
    info="Info"
    expandableInfo="This is some expandable information that is very long as was created that way in order to test how it preforms in such a situation"
  />
</Canvas>

<ArgsTable story="TextInput" />
