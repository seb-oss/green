import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { FilterChips, FilterChip } from './filter-chips'

<Meta title="Components/Filter Chips" component={FilterChips} />

# Filter Chips

[Source code](https://github.com/sebgroup/green/tree/main/libs/react/src/lib/filter-chips)
&nbsp;|&nbsp;
[Usage guidelines](https://designlibrary.sebgroup.com/components/filter-chip)

## Importing the component

To use the component, import `FilterChips` and `FilterChip` like this:

```jsx
import { FilterChips, FilterChips } from '@sebgroup/green-react'

// Use as JSX element in your app
return (
  <FilterChips value="division">
    <FilterChip value="all">All</FilterChip>
    <FilterChip value="top-news">Top news</FilterChip>
    <FilterChip value="division">Division</FilterChip>
    <FilterChip value="global">Global</FilterChip>
    <FilterChip value="country">Country</FilterChip>
    <FilterChip value="lcfi">
      Large Corporate & Financial Institutions
    </FilterChip>
  </FilterChips>
)
```

<Canvas>
  <FilterChips value="division">
    <FilterChip value="all">All</FilterChip>
    <FilterChip value="top-news">Top news</FilterChip>
    <FilterChip value="division">Division</FilterChip>
    <FilterChip value="global">Global</FilterChip>
    <FilterChip value="country">Country</FilterChip>
    <FilterChip value="lcfi">
      Large Corporate & Financial Institutions
    </FilterChip>
  </FilterChips>
</Canvas>

## More examples

For various examples of how to use the component, head over to the [Green Core storybook](https://sebgroup.github.io/green/latest/core/?path=/story/components-filter-chips--page)
