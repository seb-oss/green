import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import { Select, Option, OptionGroup } from './select'
import * as SelectStories from './select.stories';

export const Template = ({ children, ...props }) => (
  <Select {...props}>{children}</Select>
)

# Select

Select component.

<Meta of={SelectStories} />

<Canvas of={SelectStories.WithOptions} />

<Canvas of={SelectStories.WithPlaceholder} />

<Canvas of={SelectStories.WithOptionGroups} />

<Canvas of={SelectStories.WithDisabledGroup} />

<Canvas of={SelectStories.WithValidation} />

### Expandlable information

<Canvas of={SelectStories.ExpandableInformation} />

#### Available props

<Controls />
