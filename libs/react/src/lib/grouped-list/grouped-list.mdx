import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { GroupedList, ListItem } from './grouped-list'
import * as GroupedListStories from './grouped-list.stories'

<Meta of={GroupedListStories} />

# Grouped List

[Source code](https://github.com/seb-oss/green/tree/main/libs/react/src/lib/grouped-list)
&nbsp;|&nbsp;
[Usage guidelines](https://designlibrary.sebgroup.com/components/lists)

## Overview

List are used to list many datapoints with labels in a structured way. They may be grouped under heading and they may have a link or button added to act on the data being displayed.

## Importing the component

To use the component, import `GroupedList` and `ListItem` like this:

```jsx
import { GroupedList, ListItem } from '@sebgroup/green-react/src/lib/list'

// Use as JSX element in your app
return (
  <GroupedList label="List heading">
    <ListItem>Item 1</ListItem>
    <ListItem>Item 2</ListItem>
    <ListItem>Item 3</ListItem>
  </GroupedList>
)
```

<Canvas>
  <GroupedList label="List heading">
    <ListItem>Item 1</ListItem>
    <ListItem>Item 2</ListItem>
    <ListItem>Item 3</ListItem>
  </GroupedList>
</Canvas>

## Register transitional styles

This component does not need you to import styles from `Chlorophyll` it handles it with "transitional styles" that you need to register.

```jsx
import { registerTransitionalStyles } from '@sebgroup/green-core/scoping'
// Register the styles once in your apps entrypoint (main.tsx, app.tsx) file
registerTransitionalStyles()
```

## Examples

For various examples of how to use the component, head over to the [Green Core storybook](https://storybook.seb.io/latest/core/?path=/story/components-grouped-list--page)
