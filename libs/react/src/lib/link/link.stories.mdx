import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Link from './link'

export const Template = ({ children, ...props }) => <Link {...props} />

<Meta title="Components/Link" component={Link} />

# Link

Simple link component

<Canvas>
  <Story name="Link" args={{ text: 'Go to seb.se', link: 'https://seb.se' }}>
    {Template.bind({})}
  </Story>
</Canvas>
