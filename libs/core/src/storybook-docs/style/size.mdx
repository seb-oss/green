import { Meta, Markdown } from '@storybook/addon-docs'
import '../../components/container/container'
import '../../components/card/card'
import '../../components/flex/flex'
import '../../components/text/text'
import '../../components/grid/grid'
import './size.css'

import sizeTokens from '../../../../tokens/src/tokens/size.json';

<Meta title="Style/Size" />

# Size

### The spacing scale is based on a 4px grid.

The spacing scale is used for margin, padding, gap, radius and all the spacing values.

### Spacing Scale
<gds-grid columns={Object.keys(sizeTokens.ref.size).length} gap="xs" width="100%" auto-columns="80">
{Object.entries(sizeTokens.ref.size).map(([size, { value }]) => (
<gds-card padding="s" border-radius="l" key={size}>
    <gds-flex gap="l" align-items="center" flex-direction="column">
        <gds-card variant="secondary" padding="s" width="100%" box-sizing="border-box">
            <gds-flex align-items="center" justify-content="center" flex-direction="column">
                <gds-text>{size.toLowerCase()}</gds-text>
                <gds-text tag="small" color="secondary/0.4">{value}px</gds-text>
            </gds-flex>
        </gds-card>
        <gds-card 
            height={value + 'px'}
            max-height="400px"
            padding="0"
            width="100%"
            border="0"
            variant="tertiary">
        </gds-card>
    </gds-flex>
</gds-card>
))}
</gds-grid>
 
 <br />
 <br />
 <br />

 ### *1 max
 The `max` size is the maximum size of the spacing scale used when you want to create a circular shape.

 Example
```
    <gds-card border-radius="max">max</gds-card>
```

 <br />
 <br />
 <br />

<gds-card variant="tertiary" border-radius="max" width="200px">
    <gds-flex align-items="center" justify-content="center"  height="200px">
        <gds-text tag="h5">max (999px)</gds-text>
    </gds-flex>
</gds-card>
 