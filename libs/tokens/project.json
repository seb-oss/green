{
  "name": "tokens",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/tokens/src",
  "targets": {
    "build": {
      "executor": "@nxkit/style-dictionary:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/tokens",
        "styleDictionaryConfig": "libs/tokens/style-dictionary.config.ts",
        "tsConfig": "libs/tokens/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "libs/tokens/**/*.{js,ts}",
          "libs/tokens/package.json"
        ]
      }
    },
    "update-deps-and-release": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "nx run tokens:update-dependencies && nx run tokens:version-and-release"
          }
        ]
      }
    },
    "update-dependencies": {
      "executor": "@sebgroup/repo-tools:update-lib-deps",
      "options": {}
    },
    "version-and-release": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "commitMessageFormat": "chore({projectName}): release version ${version}",
        "tagPrefix": "@sebgroup/green-{projectName}@",
        "push": true,
        "preid": "beta",
        "releaseAs": "prerelease",
        "postTargets": ["tokens:publish", "tokens:github"]
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "${tag}",
        "notes": "${notes}"
      }
    },
    "publish": {
      "executor": "ngx-deploy-npm:deploy",
      "dependsOn": ["copy-package-json"],
      "options": {
        "tag": "beta",
        "access": "public",
        "dryRun": true
      }
    },
    "copy-package-json": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mkdir -p dist/libs/tokens && cp libs/tokens/package.json dist/libs/tokens"
      }
    }

},
  "tags": []
}
