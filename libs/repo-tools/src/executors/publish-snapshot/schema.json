{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Publish snapshot",
  "description": "Publish to npm, but only if the version is a snapshot version",
  "type": "object",
  "properties": {
    "label": {
      "type": "string",
      "description": "Snapshot label - asserts that the version string contains this label"
    }
  },
  "required": []
}
