import * as Core from '@sebgroup/green-core/react'
import { ColorPreviewProps } from '../part.preview.types'

export default function Typography({ token }: ColorPreviewProps) {
  return (
    <Core.GdsFlex flex-direction="column" gap="s">
      Typography
    </Core.GdsFlex>
  )
}
