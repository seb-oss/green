import{b as i,aa as d}from"./iframe-0Xj-FSRu.js";import{n as c,e as u}from"./ref-BDCM2VJ7.js";import{S as b}from"./table-BtQLnPmo.js";import"./card-BdLrr9WH.js";import"./badge-BRb5zHsA.js";import"./dropdown-DYSVC1UR.js";import"./segmented-control-DENLf6p6.js";import"./context-menu-Bw-BVXq4.js";import"./link-C4-pce5Q.js";import"./input-BOdMuvdz.js";import"./img-s9Jg3Srs.js";import"./formatted-account-DZybDKDE.js";import"./formatted-number-W13pRt6X.js";import"./formatted-date-BV8EISIH.js";import"./pagination-Rg9F3xJ3.js";import"./dot-grid-one-horizontal-Ba8fDSHf.js";import"./arrow-rotate-counter-clockwise.component-tGt2ObjO.js";import"./chevron-right-RSIWi8pn.js";import"./zoom-out.component-Cj-l-8l_.js";import"./circle-check-CqddSIco.js";import"./cross-small-DhixJNzo.js";import{a as tt}from"./argTableProps-C5IZiN1D.js";import"./preload-helper-Dp1pzeXC.js";import"./localized-decorator-tooSvWZL.js";import"./popover.component-CtFn1tSk.js";import"./floating-ui.dom-Ceeo7fpE.js";import"./cross-small.component-CE3aOT7Y.js";import"./icon.component-CV1KbxAg.js";import"./dropdown.component-Pf87JpDF.js";import"./query-async-5M94EEKt.js";import"./form-control-host.style-ekQtGWJ1.js";import"./form-control-header.component-BMTzS4s3.js";import"./badge.component-Cw5pSRiQ.js";import"./flex.component-C24BRjUC.js";import"./triangle-exclamation.component-BhDkkkFL.js";import"./card.component-CmhMSv3r.js";import"./circle-info.component-CAZ_S5Rv.js";import"./unwrap-slots-DEDuk6t4.js";import"./checkmark.component-D_pr9FQ-.js";import"./rbcb-toggle.template-DWZWpCYX.js";import"./chevron-bottom.component-BNHcVdep.js";import"./menu-heading.component-AJ2vkena.js";import"./context-menu.component-DH0atTjS.js";import"./alert.component-B-wTPW0b.js";import"./circle-check.component-DXD3Dx2y.js";import"./blur.component-0J6JNkWU.js";import"./breadcrumb.component-CxAtp1eC.js";import"./chevron-left.component-26P-BsNM.js";import"./text.component-Bt59VfFw.js";import"./default-typography.styles-La8Oq4bY.js";import"./calendar.component-_6se86-a.js";import"./card-linked.component-ChXX3sGG.js";import"./props-link-B91mW3GM.js";import"./checkbox-group.component-heCj9LS5.js";import"./toggle-control-base.component-CVZfpTrz.js";import"./minus-small.component-1iWuY-l_.js";import"./coachmark.component-BTGR7gKy.js";import"./datepicker.component-m54oXo0M.js";import"./attribute-converters-CdI0trPX.js";import"./calender-add.component-DuX4bALj.js";import"./chevron-right.component-4p9ivSkx.js";import"./details.component-BfEDH8iG.js";import"./divider.component-D1MU3xeZ.js";import"./signal.component-bdHobts4.js";import"./filter-chips.component-B6u6yVRp.js";import"./resize-observer-B9k8v2TZ.js";import"./summary.component-BFPLfgGA.js";import"./arrow-up.component-DfDKi3NT.js";import"./grouped-list.component-C6rnzdXE.js";import"./arrow-down.component-BUGgh6TJ.js";import"./arrow-left.component-BDdqKcev.js";import"./arrow-right.component-CFT0HKUg.js";import"./sun.component-CymnmxAP.js";import"./star.component-D9Lg4pME.js";import"./bubbles.component-NDGsKATA.js";import"./chain-link-IAyB0-In.js";import"./chevron-right-small.component-DwwlJC-z.js";import"./chevron-top.component-B6Gt9Lg6.js";import"./circles-three.component-BO-WVgAE.js";import"./cloudy-sun.component-DfVQdREU.js";import"./credit-card.component-Bp0ZWeNx.js";import"./folder.component-Cbs0f11p.js";import"./magnifying-glass.component-BFoCzHC-.js";import"./people-profile.component--2ruQGMb.js";import"./push.component-CM---DEf.js";import"./rocket.component-D7mqW19w.js";import"./square-grid-circle.component-4TUGBNaP.js";import"./img.component-CxRmcFRb.js";import"./input.component-BaOQtrTl.js";import"./textarea.component-D0JCBqMK.js";import"./mask.component-B_XgIx3h.js";import"./menu-button.component-B80xal6h.js";import"./radio-group.component-BY1BcmpC.js";import"./rich-text.component-BRPBVLQt.js";import"./sensitive-number.component-DFWEXJlw.js";import"./sensitive-date.component-CfUQmcxM.js";import"./formatted-date.component-B_2BxF0b.js";import"./formatted-text-4Xzs0Tpr.js";import"./text-Qp32TztE.js";import"./sensitive-account.component-DEUgEPoU.js";import"./select.component-D4F3oY-T.js";import"./spinner.component-71A7uECe.js";import"./video.component-jQhtYcss.js";import"./card-pattern-01.component-_ICzPo7u.js";const at="https://api.seb.io/components/table/table.users.json",nt="https://api.seb.io/components/table/table.feedback.json";let j=null,h=null,O=null,f=null;const st=e=>({...e,name:b(e.name,["avatar","value"]),email:b(e.email,["value","copy-button"]),status:b(e.status,["status"]),amount:b(e.amount,["amount","currency"]),account:b(e.account,["main"]),login:b(e.lastLogin,["main"]),download:b(e.download??"#",["main"])});let N=null,y=null;const ot=async()=>N||y||(y=fetch(at).then(e=>e.json()).then(e=>(N=e,e)),y),rt=async()=>j||h||(h=ot().then(e=>e.map(st)).then(e=>(j=e,e)),h),et=e=>async t=>{let a=await e();if(t.searchQuery){const r=t.searchQuery.toLowerCase();a=a.filter(m=>Object.values(m).some(l=>l==null?void 0:l.toString().toLowerCase().includes(r)))}if(t.sortColumn&&a.length>0){const r=t.sortColumn;a=[...a].sort((m,l)=>{var V,q;const g=((V=m[r])==null?void 0:V.toString())??"",K=((q=l[r])==null?void 0:q.toString())??"";return t.sortDirection==="asc"?g.localeCompare(K):K.localeCompare(g)})}const o=(t.page-1)*t.rows,n=o+t.rows;return{rows:a.slice(o,n),total:a.length}},lt=et(rt),s={Columns:[{key:"id",label:"ID",sortable:!1},{key:"name",label:"Name",sortable:!0},{key:"email",label:"Email",sortable:!0,justify:"space-between"},{key:"role",label:"Role",sortable:!0,visible:!1},{key:"status",label:"Status",sortable:!0},{key:"department",label:"Department",sortable:!0},{key:"amount",label:"Amount",sortable:!0,justify:"end"},{key:"account",label:"Account",sortable:!0},{key:"login",label:"Last Login",sortable:!0},{key:"download",label:"Download"}],Actions:{label:"Actions",justify:"end"},Tfoot:{label:"Totals",sticky:!0},Data:lt,SlotContent:e=>i`
      ${e.map(t=>i`
          <!-- name: avatar -->
          <gds-img
            src="${t.avatarUrl??"#"}"
            alt="${String(t.name)}"
            slot="name:${t.id}:avatar"
            width="xl"
            height="xl"
          ></gds-img>

          <!-- email: copy button -->
          <gds-button
            slot="email:${t.id}:copy-button"
            rank="tertiary"
            size="small"
          >
            <gds-icon-copy></gds-icon-copy>
          </gds-button>

          <!-- status: badge -->
          <gds-badge
            slot="status:${t.id}:status"
            variant="${String(t.status)==="Active"?"positive":"negative"}"
            size="small"
          >
            ${String(t.status)}
          </gds-badge>

          <!-- amount: formatted number -->
          <gds-formatted-number
            slot="amount:${t.id}:amount"
            .value=${t.amount}
          ></gds-formatted-number>

          <!-- amount: currency -->
          <gds-badge slot="amount:${t.id}:currency" size="small">
            SEK
          </gds-badge>

          <!-- account: formatted account -->
          <gds-formatted-account
            slot="account:${t.id}:main"
            account="${t.account}"
            format="seb-account"
          ></gds-formatted-account>

          <!-- login: formatted date -->
          <gds-formatted-date
            slot="login:${t.id}:main"
            .value="${String(t.login)}"
            locale="sv-SE"
            format="dateLong"
          ></gds-formatted-date>

          <!-- download: link with icon -->
          <gds-link
            slot="download:${t.id}:main"
            href="${t.download??"#"}"
            text-decoration="underline"
            download
          >
            Download file
            <gds-icon-cloud-download slot="trail"></gds-icon-cloud-download>
          </gds-link>

          <!-- actions: context menu -->
          <gds-context-menu slot="actions:${t.id}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit ${String(t.name)}</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,TfootSlotContent:e=>{const t=e.reduce((r,m)=>r+(Number(m.amount)||0),0),a=e.filter(r=>String(r.status)==="Active").length,o=Math.round(a/e.length*100),n=new Set(e.map(r=>r.department)).size;return i`
      <!-- tfoot: name row count -->
      <span slot="tfoot:name">${e.length} Users</span>

      <!-- tfoot: status active rate -->
      <gds-badge
        slot="tfoot:status"
        variant="${o>=70?"positive":o>=40?"warning":"negative"}"
        size="small"
      >
        ${o}% active
      </gds-badge>

      <!-- tfoot: department count -->
      <gds-badge slot="tfoot:department" variant="information" size="small">
        ${n} depts
      </gds-badge>

      <!-- tfoot: amount sum -->
      <gds-flex slot="tfoot:amount" gap="s" align-items="center">
        <gds-formatted-number .value=${t}></gds-formatted-number>
        <gds-badge size="small">SEK</gds-badge>
      </gds-flex>
    `}},it=async()=>O||f||(f=fetch(nt).then(e=>e.json()).then(e=>(O=e,e)),f),dt=et(it),B={Columns:[{key:"name",label:"Name",sortable:!0,align:"start"},{key:"feedback",label:"Feedback",align:"start",width:"350px"},{key:"notes",label:"Notes",align:"start",width:"460px"},{key:"department",label:"Department",align:"start",width:"120px"},{key:"status",label:"Status",sortable:!0,align:"start",justify:"end",width:"100px"}],Data:dt},_=[{id:1,task:"Review contract",assignee:"Anna Svensson",priority:"High"},{id:2,task:"Update pricing",assignee:"Erik Lindgren",priority:"Medium"},{id:3,task:"Send invoice",assignee:"Maria Berg",priority:"Low"},{id:4,task:"Approve budget",assignee:"Johan Nilsson",priority:"High"}],ct=async e=>{const t=(e.page-1)*e.rows;return{rows:_.slice(t,t+e.rows),total:_.length}},p={Columns:[{key:"task",label:"Task",align:"center"},{key:"assignee",label:"Assignee",align:"center"},{key:"priority",label:"Priority",align:"center",width:"100px"}],MultipleActions:{label:"Actions",align:"center",justify:"start",sticky:!0},MultipleActionsSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-button
            slot="actions:${t.id??a+1}:main"
            rank="secondary"
            size="small"
            variant="positive"
          >
            <gds-icon-circle-check size="m"></gds-icon-circle-check>
          </gds-button>
          <gds-button
            slot="actions:${t.id??a+1}:main"
            rank="secondary"
            size="small"
            variant="negative"
          >
            <gds-icon-cross-small size="m"></gds-icon-cross-small>
          </gds-button>
        `)}
    `,ActionLink:{label:"Actions",align:"center",justify:"end",sticky:!0},ActionLinkSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-button
            slot="actions:${t.id??a+1}:main"
            size="small"
            rank="secondary"
          >
            View details
          </gds-button>
        `)}
    `,ActionContextMenu:{label:"Actions",align:"center",justify:"end",sticky:!0},ActionContextMenuSlotContent:e=>i`
      ${e.map((t,a)=>i`
          <gds-context-menu slot="actions:${t.id??a+1}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit</gds-menu-item>
            <gds-menu-item>Archive</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,Data:ct},G=new WeakSet,ut=e=>i`
  <div
    ${c(t=>{if(!t||G.has(t))return;G.add(t);const a=new IntersectionObserver(o=>{if(o[0].isIntersecting){a.disconnect();const n=e();d(n,t);const r=t.parentNode;if(r){for(;t.firstChild;)r.insertBefore(t.firstChild,t);r.removeChild(t)}}},{rootMargin:"200px"});a.observe(t)})}
    style="min-height:40vh"
  ></div>
`,dn={title:"Components/Table",component:"gds-table",argTypes:{...tt("gds-table"),density:{control:{type:"select"},options:["comfortable","compact","spacious"],defaultValue:"comfortable"},variant:{control:{type:"select"},options:["neutral-01","neutral-01-2","neutral-02","neutral-02-2","neutral-02-outlined"],defaultValue:"neutral-02-outlined"},selectable:{control:{type:"boolean"},defaultValue:!0},searchLabel:{control:{type:"text"}},height:{control:{type:"string"},defaultValue:!1}},tags:["autodocs"],decorators:[(e,t)=>t.viewMode==="docs"?ut(()=>e()):e()],parameters:{docs:{description:{component:`
A data table component that handles asynchronous data loading, pagination, sorting, and filtering through a provider function. Supports row selection, searchable columns, responsive layouts, and customizable cell rendering including badges, buttons, links, and formatted values.
        `}}}},v={args:{columns:s.Columns,actions:s.Actions,density:"comfortable",variant:"neutral-02-outlined",data:s.Data,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?height="${e.height}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        rows="60"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},w={args:{columns:s.Columns,data:s.Data,selectable:!0,density:"comfortable"},parameters:{docs:{description:{story:`
The \`selectable\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \`gds-table-selection\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .density="${e.density}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},$={args:{columns:s.Columns,data:s.Data,searchable:!0,settings:!0},parameters:{docs:{description:{story:`
#### Search Functionality
The \`searchable\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \`settings\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},C={args:{columns:s.Columns,data:s.Data,searchable:!0,settings:!0,nocache:!0},parameters:{docs:{description:{story:"\nThe `nocache` property provides direct control over data retrieval. When set to `true`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.\n\n> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?nocache="${e.nocache}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},S={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:"\nSorting is enabled by setting `sortable: true` on column configuration. <br />\nClicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />\nThe sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a `gds-table-data-loaded` event with the sorted data.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},x={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},k={args:{columns:B.Columns,data:B.Data},parameters:{docs:{description:{story:`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        `}}},render:e=>i`
    <gds-table .columns="${e.columns}" .data="${e.data}"></gds-table>
  `},D={args:{columns:p.Columns,actions:p.MultipleActions,data:p.Data},parameters:{docs:{description:{story:`
The \`actions\` property defines row interactions shown as the last cell of each row.

Supports all cell types (button, link, context-menu, badge, etc.) and can contain multiple actions.

When passed as an object \`{ label, align, justify, sticky }\`, it configures the actions column header and cell layout.
The actual action content is provided through named slots using the \`actions:rowKey:main\` convention.

#### Sticky Actions
Set \`sticky: true\` on the actions configuration to pin the actions column to the right edge of the table during horizontal scrolling. This ensures action buttons remain accessible even when the table content overflows horizontally.
        `}}},render:e=>{const t=u(),a=u(),o=u();return i`
      <gds-table
        ${c(t)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        @gds-table-data-loaded=${n=>{const r=t.value;r&&d(p.MultipleActionsSlotContent(n.detail.rows),r)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${c(a)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${p.ActionContextMenu}"
        @gds-table-data-loaded=${n=>{const r=a.value;r&&d(p.ActionContextMenuSlotContent(n.detail.rows),r)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${c(o)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${p.ActionLink}"
        @gds-table-data-loaded=${n=>{const r=o.value;r&&d(p.ActionLinkSlotContent(n.detail.rows),r)}}
      >
      </gds-table>
    `}},R={args:{columns:s.Columns,actions:s.Actions,density:"comfortable",variant:"neutral-02-outlined",data:s.Data,tfoot:s.Tfoot,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},parameters:{docs:{description:{story:`
The \`tfoot\` property adds a \`<tfoot>\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \`label\` is provided, the first cell renders as a \`<th scope="row">\` for proper accessibility semantics.

#### Configuration

\`\`\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\`\`\`

#### Slot Convention

Table footer slots use the naming pattern \`tfoot:{columnKey}\`:

\`\`\`html
<gds-formatted-number slot="tfoot:amount" .value=\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\${count} active</gds-badge>
\`\`\`

Compute your aggregation in the \`gds-table-data-loaded\` handler and render slot content into the table.

When \`sticky: true\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(i`${s.SlotContent(o.detail.rows)}
          ${s.TfootSlotContent(o.detail.rows)}`,n)};return i`
      <gds-table
        ${c(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        height="${e.height}"
        .columns="${e.columns}"
        .data="${e.data}"
        .actions="${e.actions}"
        .tfoot="${e.tfoot}"
        rows="50"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},L={args:{columns:s.Columns,data:s.Data,headline:"User Management",headlineTag:"h2",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:"\nWhen the `headline` and `summary` properties are set, they will be displayed at the top of the table.\n\nYou can change the `headline-tag` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        headline="${e.headline}"
        headline-tag="${e.headlineTag}"
        summary="${e.summary}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},A={args:{columns:s.Columns,data:s.Data,plain:!0},parameters:{docs:{description:{story:"\nWhen `plain` boolean is set, the table header and footer are removed, rendering only the data table.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?plain="${e.plain}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},U={args:{columns:s.Columns,data:s.Data,striped:!0,density:"compact"},parameters:{docs:{description:{story:"\nWhen `striped` boolean is set, alternating row background colors are applied to even-numbered rows.\n        "}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .density="${e.density}"
        ?striped="${e.striped}"
        .columns="${e.columns}"
        .data="${e.data}"
        rows="20"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},E={args:{columns:s.Columns,data:s.Data,density:"compact",headline:"User Management",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:'\nThe `density` property controls the table\'s visual spacing and affects the table\'s **built-in UI** (search input, column settings dropdown, pagination).\n\n- **`comfortable`** (Default) · Standard spacing for balanced readability\n- **`compact`** · Reduced spacing to maximize information density\n- **`spacious`** · Increased spacing for enhanced readability\n\n> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use `size="small"` on badges and buttons in `compact` or `comfortable` mode, and `size="medium"` in `spacious` mode to maintain visual consistency.\n        '}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        headline="${e.headline}"
        summary="${e.summary}"
        searchable
        settings
        .density="${e.density}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},T={args:{columns:s.Columns,data:s.Data,variant:"neutral-02-outlined",density:"comfortable"},argTypes:{variant:{control:"select",options:["neutral-01","neutral-01-2","neutral-02","neutral-02-2","neutral-02-outlined","brand-01","brand-02"]}},parameters:{docs:{description:{story:"\nThe `variant` property controls the visual style of the table container card.\nAvailable variants: `neutral-01`, `neutral-01-2`, `neutral-02`, `neutral-02-2`, `neutral-02-outlined` (Default), `brand-01`, `brand-02`\n        "}}},render:e=>{const t=e.variant==="tertiary"?"primary":"tertiary",a=u(),o=n=>{const r=a.value;r&&d(s.SlotContent(n.detail.rows),r)};return i`
      <gds-card variant="${t}" padding="l" border-radius="m">
        <gds-table
          ${c(a)}
          .density="${e.density}"
          .variant="${e.variant}"
          .columns="${e.columns}"
          .data="${e.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=${o}
        >
        </gds-table>
      </gds-card>
    `}},z={args:{columns:s.Columns,data:s.Data,responsive:!0},parameters:{docs:{description:{story:`
When the \`responsive\` boolean is set to \`true\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**:
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        ?responsive="${e.responsive}"
        .columns="${e.columns}"
        .data="${e.data}"
        height="80vh"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},P={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=${a}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    `}},I={args:{columns:s.Columns,data:s.Data,simulatedState:"normal"},argTypes:{simulatedState:{control:"select",options:["normal","error","empty","no-results"],description:"Simulate different table states"}},parameters:{docs:{description:{story:`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        `}}},render:e=>{const t=u(),a=async m=>{if(await new Promise(l=>setTimeout(l,500)),e.simulatedState==="error")throw new Error("Simulated error state");return e.simulatedState==="empty"?{rows:[],total:0}:e.simulatedState==="no-results"?{rows:[],total:0}:e.data(m)},o=m=>{const l=t.value;l&&d(s.SlotContent(m.detail.rows),l)},n=()=>{e.simulatedState="normal",t.value&&(t.value.dataLoadKey=`retry-${Date.now()}`)},r=()=>{var m;if(e.simulatedState="normal",t.value){t.value.dataLoadKey=`clear-${Date.now()}`;const l=(m=t.value.shadowRoot)==null?void 0:m.querySelector("gds-input");l&&(l.value="")}};return setTimeout(()=>{var m;if(e.simulatedState==="no-results"&&t.value){const l=(m=t.value.shadowRoot)==null?void 0:m.querySelector("gds-input");l&&(l.value="test search",l.dispatchEvent(new Event("input",{bubbles:!0})))}},100),i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${a}"
        .dataLoadKey="${e.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=${o}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-m-book">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${n}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-m-book">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-m-book">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${r}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    `}},M={args:{columns:s.Columns,data:s.Data,headline:"User Management",summary:"Overview of all users in the system",selectable:!0,searchable:!0,settings:!0,density:"comfortable"},parameters:{docs:{description:{story:`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \`key\` parameter in \`Slot()\`, or it falls back to \`row.id\`, then the row index.

#### 1. Compose data with \`Slot()\`

\`\`\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\`\`\`

The arguments are positional: \`Slot(value, slots, key)\` or \`Slot(value, key, slots)\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \`'value'\` to render the plain text alongside your components), and **key** is an optional \`string | number\` used as a stable row identifier.

#### 2. Render slot content on data load

\`\`\`ts
@gds-table-data-loaded=\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\`\`\`

#### 3. Slot naming convention

The slot name follows the pattern \`columnKey:rowKey:slotId\`.

\`\`\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\`\`\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \`Slot()\` API.
        `}}},render:e=>{const t=u(),a=o=>{const n=t.value;n&&d(s.SlotContent(o.detail.rows),n)};return i`
      <gds-table
        ${c(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        .headline="${e.headline}"
        .summary="${e.summary}"
        ?selectable="${e.selectable}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        density="${e.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=${a}
      >
      </gds-table>
    `}},W={args:{columns:s.Columns,data:s.Data},parameters:{docs:{description:{story:`
The table component dispatches events for various user interactions.

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \`{ page: number }\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \`{ rows: number }\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

This example demonstrates listening to pagination and sorting events and displaying the current state.
        `}}},render:e=>{const t=u(),a={currentPage:1,currentRows:10,sortColumn:"",sortDirection:"asc"},o=l=>{const g=t.value;g&&d(s.SlotContent(l.detail.rows),g)},n=l=>{console.log("🔔 Page change event:",l.detail),a.currentPage=l.detail.page;const g=document.querySelector("#page-status");g&&(g.textContent=`Page: ${a.currentPage}`)},r=l=>{console.log("🔔 Rows change event:",l.detail),a.currentRows=l.detail.rows;const g=document.querySelector("#rows-status");g&&(g.textContent=`Rows: ${a.currentRows}`)},m=l=>{console.log("🔔 Sort change event:",l.detail),a.sortColumn=l.detail.sortColumn,a.sortDirection=l.detail.sortDirection;const g=document.querySelector("#sort-status");g&&(g.textContent=`${a.sortColumn} (${a.sortDirection})`)};return i`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="neutral-02" padding="m" border-radius="m" outline>
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-s-book" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Page: ${a.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Rows: ${a.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    ${a.sortColumn?`${a.sortColumn} (${a.sortDirection})`:"None"}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          ${c(t)}
          .columns="${e.columns}"
          .data="${e.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=${o}
          @gds-page-change=${l=>n(l)}
          @gds-rows-change=${l=>r(l)}
          @gds-sort-change=${l=>m(l)}
        >
        </gds-table>
      </gds-flex>
    `}},F={args:{rows:8,options:[8,20]},parameters:{docs:{description:{story:`
      Demonstrates a pagination edge case where the selected page size exceeds the total number of rows.

1. Dataset has 13 rows total
2. Initial page size is 8
3. Change rows per page to 20

In this edge case, page navigation is hidden when only one page remains, while the rows-per-page selector stays visible so users can switch back to a smaller page size.
        `}}},render:e=>{const t=[{key:"id",label:"ID",sortable:!0},{key:"name",label:"Name",sortable:!0},{key:"status",label:"Status",sortable:!0}],a=Array.from({length:13},(n,r)=>({id:r+1,name:`User ${r+1}`,status:r%2===0?"Active":"Inactive"}));return i`
      <gds-table
        headline="Pagination edge case"
        summary="13 rows total with page-size switching between 8 and 20."
        .columns=${t}
        .data=${async n=>{const r=(n.page-1)*n.rows,m=r+n.rows;return{rows:a.slice(r,m),total:a.length}}}
        .rows=${e.rows}
        .options=${e.options}
      >
      </gds-table>
    `}};var H,Y,Q;v.parameters={...v.parameters,docs:{...(H=v.parameters)==null?void 0:H.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    actions: Users.Actions,
    density: 'comfortable',
    variant: 'neutral-02-outlined' as Variant,
    data: Users.Data,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?height="\${args.height}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        rows="60"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Q=(Y=v.parameters)==null?void 0:Y.docs)==null?void 0:Q.source}}};var J,X,Z;w.parameters={...w.parameters,docs:{...(J=w.parameters)==null?void 0:J.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    selectable: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`selectable\\\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \\\`gds-table-selection\\\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Z=(X=w.parameters)==null?void 0:X.docs)==null?void 0:Z.source}}};var ee,te,ae;$.parameters={...$.parameters,docs:{...(ee=$.parameters)==null?void 0:ee.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
#### Search Functionality
The \\\`searchable\\\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \\\`settings\\\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ae=(te=$.parameters)==null?void 0:te.docs)==null?void 0:ae.source}}};var ne,se,oe;C.parameters={...C.parameters,docs:{...(ne=C.parameters)==null?void 0:ne.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true,
    nocache: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`nocache\\\` property provides direct control over data retrieval. When set to \\\`true\\\`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.

> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?nocache="\${args.nocache}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(oe=(se=C.parameters)==null?void 0:se.docs)==null?void 0:oe.source}}};var re,le,ie;S.parameters={...S.parameters,docs:{...(re=S.parameters)==null?void 0:re.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Sorting is enabled by setting \\\`sortable: true\\\` on column configuration. <br />
Clicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />
The sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a \\\`gds-table-data-loaded\\\` event with the sorted data.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ie=(le=S.parameters)==null?void 0:le.docs)==null?void 0:ie.source}}};var de,ce,ue;x.parameters={...x.parameters,docs:{...(de=x.parameters)==null?void 0:de.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(ue=(ce=x.parameters)==null?void 0:ce.docs)==null?void 0:ue.source}}};var me,ge,pe;k.parameters={...k.parameters,docs:{...(me=k.parameters)==null?void 0:me.docs,source:{originalSource:`{
  args: {
    columns: Feedback.Columns,
    data: Feedback.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        \`
      }
    }
  },
  render: args => html\`
    <gds-table .columns="\${args.columns}" .data="\${args.data}"></gds-table>
  \`
}`,...(pe=(ge=k.parameters)==null?void 0:ge.docs)==null?void 0:pe.source}}};var be,he,fe;D.parameters={...D.parameters,docs:{...(be=D.parameters)==null?void 0:be.docs,source:{originalSource:`{
  args: {
    columns: ActionsData.Columns,
    actions: ActionsData.MultipleActions,
    data: ActionsData.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`actions\\\` property defines row interactions shown as the last cell of each row.

Supports all cell types (button, link, context-menu, badge, etc.) and can contain multiple actions.

When passed as an object \\\`{ label, align, justify, sticky }\\\`, it configures the actions column header and cell layout.
The actual action content is provided through named slots using the \\\`actions:rowKey:main\\\` convention.

#### Sticky Actions
Set \\\`sticky: true\\\` on the actions configuration to pin the actions column to the right edge of the table during horizontal scrolling. This ensures action buttons remain accessible even when the table content overflows horizontally.
        \`
      }
    }
  },
  render: args => {
    const multiRef = createRef<any>();
    const ctxRef = createRef<any>();
    const linkRef = createRef<any>();
    return html\`
      <gds-table
        \${ref(multiRef)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = multiRef.value;
      if (table) render(ActionsData.MultipleActionsSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(ctxRef)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${ActionsData.ActionContextMenu}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = ctxRef.value;
      if (table) render(ActionsData.ActionContextMenuSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(linkRef)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${ActionsData.ActionLink}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = linkRef.value;
      if (table) render(ActionsData.ActionLinkSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>
    \`;
  }
}`,...(fe=(he=D.parameters)==null?void 0:he.docs)==null?void 0:fe.source}}};var ye,ve,we;R.parameters={...R.parameters,docs:{...(ye=R.parameters)==null?void 0:ye.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    actions: Users.Actions,
    density: 'comfortable',
    variant: 'neutral-02-outlined' as Variant,
    data: Users.Data,
    tfoot: Users.Tfoot,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`tfoot\\\` property adds a \\\`<tfoot>\\\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \\\`label\\\` is provided, the first cell renders as a \\\`<th scope="row">\\\` for proper accessibility semantics.

#### Configuration

\\\`\\\`\\\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\\\`\\\`\\\`

#### Slot Convention

Table footer slots use the naming pattern \\\`tfoot:{columnKey}\\\`:

\\\`\\\`\\\`html
<gds-formatted-number slot="tfoot:amount" .value=\\\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\\\${count} active</gds-badge>
\\\`\\\`\\\`

Compute your aggregation in the \\\`gds-table-data-loaded\\\` handler and render slot content into the table.

When \\\`sticky: true\\\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(html\`\${Users.SlotContent(e.detail.rows)}
          \${Users.TfootSlotContent(e.detail.rows)}\`, table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        height="\${args.height}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        .actions="\${args.actions}"
        .tfoot="\${args.tfoot}"
        rows="50"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(we=(ve=R.parameters)==null?void 0:ve.docs)==null?void 0:we.source}}};var $e,Ce,Se;L.parameters={...L.parameters,docs:{...($e=L.parameters)==null?void 0:$e.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    headlineTag: 'h2',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`headline\\\` and \\\`summary\\\` properties are set, they will be displayed at the top of the table.

You can change the \\\`headline-tag\\\` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        headline-tag="\${args.headlineTag}"
        summary="\${args.summary}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Se=(Ce=L.parameters)==null?void 0:Ce.docs)==null?void 0:Se.source}}};var xe,ke,De;A.parameters={...A.parameters,docs:{...(xe=A.parameters)==null?void 0:xe.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    plain: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`plain\\\` boolean is set, the table header and footer are removed, rendering only the data table.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?plain="\${args.plain}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(De=(ke=A.parameters)==null?void 0:ke.docs)==null?void 0:De.source}}};var Re,Le,Ae;U.parameters={...U.parameters,docs:{...(Re=U.parameters)==null?void 0:Re.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    striped: true,
    density: 'compact'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`striped\\\` boolean is set, alternating row background colors are applied to even-numbered rows.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?striped="\${args.striped}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="20"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ae=(Le=U.parameters)==null?void 0:Le.docs)==null?void 0:Ae.source}}};var Ue,Ee,Te;E.parameters={...E.parameters,docs:{...(Ue=E.parameters)==null?void 0:Ue.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    density: 'compact',
    headline: 'User Management',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`density\\\` property controls the table's visual spacing and affects the table's **built-in UI** (search input, column settings dropdown, pagination).

- **\\\`comfortable\\\`** (Default) · Standard spacing for balanced readability
- **\\\`compact\\\`** · Reduced spacing to maximize information density
- **\\\`spacious\\\`** · Increased spacing for enhanced readability

> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use \\\`size="small"\\\` on badges and buttons in \\\`compact\\\` or \\\`comfortable\\\` mode, and \\\`size="medium"\\\` in \\\`spacious\\\` mode to maintain visual consistency.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        summary="\${args.summary}"
        searchable
        settings
        .density="\${args.density}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Te=(Ee=E.parameters)==null?void 0:Ee.docs)==null?void 0:Te.source}}};var ze,Pe,Ie;T.parameters={...T.parameters,docs:{...(ze=T.parameters)==null?void 0:ze.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    variant: 'neutral-02-outlined' as Variant,
    density: 'comfortable'
  },
  argTypes: {
    variant: {
      control: 'select',
      options: ['neutral-01', 'neutral-01-2', 'neutral-02', 'neutral-02-2', 'neutral-02-outlined', 'brand-01', 'brand-02'] as Variant[]
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`variant\\\` property controls the visual style of the table container card.
Available variants: \\\`neutral-01\\\`, \\\`neutral-01-2\\\`, \\\`neutral-02\\\`, \\\`neutral-02-2\\\`, \\\`neutral-02-outlined\\\` (Default), \\\`brand-01\\\`, \\\`brand-02\\\`
        \`
      }
    }
  },
  render: args => {
    const wrapperVariant = args.variant === 'tertiary' ? 'primary' : 'tertiary';
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-card variant="\${wrapperVariant}" padding="l" border-radius="m">
        <gds-table
          \${ref(tableRef)}
          .density="\${args.density}"
          .variant="\${args.variant}"
          .columns="\${args.columns}"
          .data="\${args.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=\${handleDataLoaded}
        >
        </gds-table>
      </gds-card>
    \`;
  }
}`,...(Ie=(Pe=T.parameters)==null?void 0:Pe.docs)==null?void 0:Ie.source}}};var Me,We,Fe;z.parameters={...z.parameters,docs:{...(Me=z.parameters)==null?void 0:Me.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    responsive: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`responsive\\\` boolean is set to \\\`true\\\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**:
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?responsive="\${args.responsive}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        height="80vh"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Fe=(We=z.parameters)==null?void 0:We.docs)==null?void 0:Fe.source}}};var je,Oe,Ne;P.parameters={...P.parameters,docs:{...(je=P.parameters)==null?void 0:je.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    \`;
  }
}`,...(Ne=(Oe=P.parameters)==null?void 0:Oe.docs)==null?void 0:Ne.source}}};var Ke,Ve,qe;I.parameters={...I.parameters,docs:{...(Ke=I.parameters)==null?void 0:Ke.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    simulatedState: 'normal'
  },
  argTypes: {
    simulatedState: {
      control: 'select',
      options: ['normal', 'error', 'empty', 'no-results'],
      description: 'Simulate different table states'
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const dataProvider = async (request: any) => {
      await new Promise(resolve => setTimeout(resolve, 500));
      if (args.simulatedState === 'error') {
        throw new Error('Simulated error state');
      }
      if (args.simulatedState === 'empty') {
        return {
          rows: [],
          total: 0
        };
      }
      if (args.simulatedState === 'no-results') {
        return {
          rows: [],
          total: 0
        };
      }
      return args.data(request);
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handleRetry = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`retry-\${Date.now()}\`;
      }
    };
    const handleClearSearch = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`clear-\${Date.now()}\`;
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = '';
        }
      }
    };

    // Simulate search query for no-results state
    setTimeout(() => {
      if (args.simulatedState === 'no-results' && tableRef.value) {
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = 'test search';
          searchInput.dispatchEvent(new Event('input', {
            bubbles: true
          }));
        }
      }
    }, 100);
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${dataProvider}"
        .dataLoadKey="\${args.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-m-book">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleRetry}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-m-book">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-m-book">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleClearSearch}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    \`;
  }
}`,...(qe=(Ve=I.parameters)==null?void 0:Ve.docs)==null?void 0:qe.source}}};var Be,_e,Ge;M.parameters={...M.parameters,docs:{...(Be=M.parameters)==null?void 0:Be.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    summary: 'Overview of all users in the system',
    selectable: true,
    searchable: true,
    settings: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \\\`key\\\` parameter in \\\`Slot()\\\`, or it falls back to \\\`row.id\\\`, then the row index.

#### 1. Compose data with \\\`Slot()\\\`

\\\`\\\`\\\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\\\`\\\`\\\`

The arguments are positional: \\\`Slot(value, slots, key)\\\` or \\\`Slot(value, key, slots)\\\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \\\`'value'\\\` to render the plain text alongside your components), and **key** is an optional \\\`string | number\\\` used as a stable row identifier.

#### 2. Render slot content on data load

\\\`\\\`\\\`ts
@gds-table-data-loaded=\\\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\\\`\\\`\\\`

#### 3. Slot naming convention

The slot name follows the pattern \\\`columnKey:rowKey:slotId\\\`.

\\\`\\\`\\\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\\\`\\\`\\\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \\\`Slot()\\\` API.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .headline="\${args.headline}"
        .summary="\${args.summary}"
        ?selectable="\${args.selectable}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        density="\${args.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...(Ge=(_e=M.parameters)==null?void 0:_e.docs)==null?void 0:Ge.source}}};var He,Ye,Qe;W.parameters={...W.parameters,docs:{...(He=W.parameters)==null?void 0:He.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table component dispatches events for various user interactions.

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \\\`{ page: number }\\\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \\\`{ rows: number }\\\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \\\`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\\\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

This example demonstrates listening to pagination and sorting events and displaying the current state.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const state = {
      currentPage: 1,
      currentRows: 10,
      sortColumn: '',
      sortDirection: 'asc'
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handlePageChange = (e: CustomEvent) => {
      console.log('🔔 Page change event:', e.detail);
      state.currentPage = e.detail.page;
      const pageEl = document.querySelector('#page-status');
      if (pageEl) {
        pageEl.textContent = \`Page: \${state.currentPage}\`;
      }
    };
    const handleRowsChange = (e: CustomEvent) => {
      console.log('🔔 Rows change event:', e.detail);
      state.currentRows = e.detail.rows;
      const rowsEl = document.querySelector('#rows-status');
      if (rowsEl) {
        rowsEl.textContent = \`Rows: \${state.currentRows}\`;
      }
    };
    const handleSortChange = (e: CustomEvent) => {
      console.log('🔔 Sort change event:', e.detail);
      state.sortColumn = e.detail.sortColumn;
      state.sortDirection = e.detail.sortDirection;
      const sortEl = document.querySelector('#sort-status');
      if (sortEl) {
        sortEl.textContent = \`\${state.sortColumn} (\${state.sortDirection})\`;
      }
    };
    return html\`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="neutral-02" padding="m" border-radius="m" outline>
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-s-book" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Page: \${state.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Rows: \${state.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    \${state.sortColumn ? \`\${state.sortColumn} (\${state.sortDirection})\` : 'None'}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          \${ref(tableRef)}
          .columns="\${args.columns}"
          .data="\${args.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=\${handleDataLoaded}
          @gds-page-change=\${(e: CustomEvent) => handlePageChange(e)}
          @gds-rows-change=\${(e: CustomEvent) => handleRowsChange(e)}
          @gds-sort-change=\${(e: CustomEvent) => handleSortChange(e)}
        >
        </gds-table>
      </gds-flex>
    \`;
  }
}`,...(Qe=(Ye=W.parameters)==null?void 0:Ye.docs)==null?void 0:Qe.source}}};var Je,Xe,Ze;F.parameters={...F.parameters,docs:{...(Je=F.parameters)==null?void 0:Je.docs,source:{originalSource:`{
  args: {
    rows: 8,
    options: [8, 20]
  },
  parameters: {
    docs: {
      description: {
        story: \`
      Demonstrates a pagination edge case where the selected page size exceeds the total number of rows.

1. Dataset has 13 rows total
2. Initial page size is 8
3. Change rows per page to 20

In this edge case, page navigation is hidden when only one page remains, while the rows-per-page selector stays visible so users can switch back to a smaller page size.
        \`
      }
    }
  },
  render: args => {
    const columns = [{
      key: 'id',
      label: 'ID',
      sortable: true
    }, {
      key: 'name',
      label: 'Name',
      sortable: true
    }, {
      key: 'status',
      label: 'Status',
      sortable: true
    }];
    const rows = Array.from({
      length: 13
    }, (_, i) => ({
      id: i + 1,
      name: \`User \${i + 1}\`,
      status: i % 2 === 0 ? 'Active' : 'Inactive'
    }));
    const dataProvider = async (request: any) => {
      const start = (request.page - 1) * request.rows;
      const end = start + request.rows;
      return {
        rows: rows.slice(start, end),
        total: rows.length
      };
    };
    return html\`
      <gds-table
        headline="Pagination edge case"
        summary="13 rows total with page-size switching between 8 and 20."
        .columns=\${columns}
        .data=\${dataProvider}
        .rows=\${args.rows}
        .options=\${args.options}
      >
      </gds-table>
    \`;
  }
}`,...(Ze=(Xe=F.parameters)==null?void 0:Xe.docs)==null?void 0:Ze.source}}};const cn=["Overview","Selection","Filtering","Caching","Sorting","Cell","Formatting","Actions","Tfoot","Headline","Plain","Striped","Density","Variant","Responsive","Slots","StateSlots","SlotComposition","Events","EdgeCase"];export{D as Actions,C as Caching,x as Cell,E as Density,F as EdgeCase,W as Events,$ as Filtering,k as Formatting,L as Headline,v as Overview,A as Plain,z as Responsive,w as Selection,M as SlotComposition,P as Slots,S as Sorting,I as StateSlots,U as Striped,R as Tfoot,T as Variant,cn as __namedExportsOrder,dn as default};
