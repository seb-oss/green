import{b as l,a2 as f}from"./iframe-tS67NpJ4.js";import{n as b,e as h}from"./ref-lmoHawXe.js";import{S as x}from"./table-DXmqiNun.js";import"./card-DVA_RZ5U.js";import"./badge-C9Io_FWU.js";import"./dropdown-BkeKbpPu.js";import"./segmented-control-D0bgy5Lu.js";import"./context-menu-3fc-9j--.js";import"./divider-DtabE2_A.js";import"./link-DSj63hYU.js";import"./input-BgTOmKy2.js";import"./img-DiOKX156.js";import"./formatted-account-DNeI83a_.js";import"./formatted-number-DZV3S-3O.js";import"./formatted-date-DZoe6LDo.js";import"./pagination-C7vLebEZ.js";import"./dialog-QElHmbCe.js";import"./dot-grid-one-horizontal-DB9Fhu7O.js";import"./arrow-rotate-counter-clockwise.component-CST9jEro.js";import"./zoom-out.component-CmBkikw3.js";import"./chevron-right-CGMinQuD.js";import"./circle-check-BD2DodS_.js";import"./cross-small-_0HiFG8x.js";import{a as ve}from"./argTableProps-cWTUIpeJ.js";import"./preload-helper-PPVm8Dsz.js";import"./localized-decorator-CssqLJfC.js";import"./datepicker.component-zTq91ZFZ.js";import"./query-async-BeXxQPAl.js";import"./dropdown.component-CZ5CidOY.js";import"./icon.component-CQ32B3qz.js";import"./floating-ui.dom-DPpwG6Py.js";import"./field-base.component-BQBXz53O.js";import"./form-control-header.component-DPueWcBI.js";import"./badge.component-6J_s3avm.js";import"./flex.component-BFPMbxdW.js";import"./triangle-exclamation.component-DNeChqep.js";import"./card.component-C54DQE4o.js";import"./circle-info.component-e1ZPXj9C.js";import"./form-control-host.style-CbMgq7Et.js";import"./menu-listbox.style-zvjgEIXq.js";import"./checkmark.component-ifAGQQD5.js";import"./chevron-bottom.component-HKu1RWos.js";import"./cross-small.component-BOWOJm6s.js";import"./popover.component-7czUo6Er.js";import"./rbcb-toggle.template-DfQuDjRO.js";import"./attribute-converters-CdI0trPX.js";import"./calendar.component-NbsI4FID.js";import"./calender-add.component-CMfaeeWP.js";import"./chevron-left.component-D-D_ZhPH.js";import"./chevron-right.component-DBmo7ONj.js";import"./menu-heading.component-BpwzClKm.js";import"./context-menu.component-DcwOioKi.js";import"./alert.component-gDaABWxX.js";import"./circle-check.component-BDBrwveh.js";import"./avatar.component-CuDI7vM5.js";import"./people-profile.component-DsxptVNH.js";import"./blur.component-Uv6myhuy.js";import"./breadcrumb.component-C3Dc18rz.js";import"./text.component-D9AlA_Y_.js";import"./default-typography.styles-C998HRCC.js";import"./card-linked.component-C98x2vEc.js";import"./props-link-6nw9X420.js";import"./checkbox-group.component-DmmQzYsl.js";import"./toggle-control-base.component-vNN_1oef.js";import"./minus-small.component-CkR6EOWr.js";import"./coachmark.component-om8bK39r.js";import"./details.component-pgY-M34n.js";import"./signal.component-Cf4Lsm0a.js";import"./filter-chips.component-DfYyaUha.js";import"./resize-observer-CfkbZofs.js";import"./summary.component-C4la1Zv3.js";import"./arrow-up.component-DSOO8M-5.js";import"./grouped-list.component-CHRS-Xqj.js";import"./list-item-C_zLPF9W.js";import"./arrow-down.component-D2KvPhfW.js";import"./arrow-left.component-PuS4c4Sj.js";import"./arrow-right.component-CHaTMjXu.js";import"./sun.component-I3o3TBP4.js";import"./star.component-DHZVZQHK.js";import"./bubbles.component-D0XaEmIF.js";import"./chain-link-9B3HvMQL.js";import"./chevron-right-small.component-BviPWh81.js";import"./chevron-top.component-Dxdn5npH.js";import"./circles-three.component-Di_Ml3-S.js";import"./cloud-upload.component-OgC-siST.js";import"./cloudy-sun.component-DChrAuN7.js";import"./credit-card.component-DrGtkHkR.js";import"./cross-large.component-DDnw-jaq.js";import"./folder.component-BO0n42cM.js";import"./magnifying-glass.component-BHh10wXS.js";import"./push.component-B1OqqV3w.js";import"./rocket.component-f6OAX5Pi.js";import"./square-grid-circle.component-ou1rRN7w.js";import"./img.component-CVm4VMV5.js";import"./input.component-KY5jr7Ay.js";import"./textarea.component-Cpt0rDK6.js";import"./mask.component-BA4ea4jw.js";import"./menu-button.component-BKK83Cgd.js";import"./radio-group.component-CZogZZiv.js";import"./rich-text.component-D6ev3vjo.js";import"./sensitive-number.component-DOM_43x8.js";import"./sensitive-date.component-BwoRMF4S.js";import"./formatted-date.component-kLtZhDOP.js";import"./formatted-text-_avv8k0w.js";import"./text-B_tIOtpF.js";import"./sensitive-account.component-CFVXIgFN.js";import"./select.component-CRfhGaew.js";import"./slider.component-Br_1MP5W.js";import"./spinner.component-Buh_BmED.js";import"./scroll-lock-VYoEjz16.js";import"./switch.component-C_45HyRd.js";import"./video.component-CKzDU4Nj.js";import"./card-pattern-01.component-Crh3p5LH.js";import"./list-item-pattern-01.component-BwRDaQDh.js";const xe="https://api.seb.io/components/table/table.users.json",we="https://api.seb.io/components/table/table.feedback.json";let re=null,A=null,le=null,T=null;const $e=e=>({...e,name:x(e.name,["avatar","value"]),email:x(e.email,["value","copy-button"]),status:x(e.status,["status"]),amount:x(e.amount,["amount","currency"]),account:x(e.account,["main"]),login:x(e.lastLogin,["main"]),download:x(e.download??"#",["main"]),actions:x(void 0,["main"])});let de=null,z=null;const Se=async()=>de||z||(z=fetch(xe).then(e=>e.json()).then(e=>(de=e,e)),z),Ce=async()=>re||A||(A=Se().then(e=>e.map($e)).then(e=>(re=e,e)),A),ye=e=>async t=>{let n=await e();if(t.searchQuery){const o=t.searchQuery.toLowerCase();n=n.filter(d=>Object.values(d).some(g=>g?.toString().toLowerCase().includes(o)))}if(t.sortColumn&&n.length>0){const o=t.sortColumn;n=[...n].sort((d,g)=>{const p=d[o]?.toString()??"",i=g[o]?.toString()??"";return t.sortDirection==="asc"?p.localeCompare(i):i.localeCompare(p)})}const a=(t.page-1)*t.rows,s=a+t.rows;return{rows:n.slice(a,s),total:n.length}},ke=ye(Ce),r={Columns:[{key:"id",label:"ID",sortable:!1,width:"6ch"},{key:"name",label:"Name",sortable:!0,width:"28ch"},{key:"email",label:"Email",sortable:!0,justify:"space-between"},{key:"role",label:"Role",sortable:!0,visible:!1},{key:"status",label:"Status",sortable:!0},{key:"department",label:"Department",sortable:!0},{key:"amount",label:"Amount",sortable:!0,justify:"end"},{key:"account",label:"Account",sortable:!0},{key:"login",label:"Last Login",sortable:!0},{key:"download",label:"Download"},{key:"actions",label:"Actions",justify:"end",sticky:"right"}],Tfoot:{label:"Totals",sticky:!0},Data:ke,SlotContent:e=>l`
      ${e.map(t=>l`
          <!-- name: avatar -->
          <gds-img
            src="${t.avatarUrl??"#"}"
            alt="${String(t.name)}"
            slot="name:${t.id}:avatar"
            width="xl"
            height="xl"
          ></gds-img>

          <!-- email: copy button -->
          <gds-button
            slot="email:${t.id}:copy-button"
            rank="tertiary"
            size="small"
          >
            <gds-icon-copy></gds-icon-copy>
          </gds-button>

          <!-- status: badge -->
          <gds-badge
            slot="status:${t.id}:status"
            variant="${String(t.status)==="Active"?"positive":"negative"}"
            size="small"
          >
            ${String(t.status)}
          </gds-badge>

          <!-- amount: formatted number -->
          <gds-formatted-number
            slot="amount:${t.id}:amount"
            .value=${t.amount}
          ></gds-formatted-number>

          <!-- amount: currency -->
          <gds-badge slot="amount:${t.id}:currency" size="small">
            SEK
          </gds-badge>

          <!-- account: formatted account -->
          <gds-formatted-account
            slot="account:${t.id}:main"
            account="${t.account}"
            format="seb-account"
          ></gds-formatted-account>

          <!-- login: formatted date -->
          <gds-formatted-date
            slot="login:${t.id}:main"
            .value="${String(t.login)}"
            locale="sv-SE"
            format="dateLong"
          ></gds-formatted-date>

          <!-- download: link with icon -->
          <gds-link
            slot="download:${t.id}:main"
            href="${t.download??"#"}"
            text-decoration="underline"
            download
          >
            Download file
            <gds-icon-cloud-download slot="trail"></gds-icon-cloud-download>
          </gds-link>

          <!-- actions: context menu -->
          <gds-context-menu slot="actions:${t.id}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit ${String(t.name)}</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,TfootSlotContent:e=>{const t=e.reduce((o,d)=>o+(Number(d.amount)||0),0),n=e.filter(o=>String(o.status)==="Active").length,a=Math.round(n/e.length*100),s=new Set(e.map(o=>o.department)).size;return l`
      <!-- tfoot: name row count -->
      <span slot="tfoot:name">${e.length} Users</span>

      <!-- tfoot: status active rate -->
      <gds-badge
        slot="tfoot:status"
        variant="${a>=70?"positive":a>=40?"warning":"negative"}"
        size="small"
      >
        ${a}% active
      </gds-badge>

      <!-- tfoot: department count -->
      <gds-badge slot="tfoot:department" variant="information" size="small">
        ${s} depts
      </gds-badge>

      <!-- tfoot: amount sum -->
      <gds-flex slot="tfoot:amount" gap="s" align-items="center">
        <gds-formatted-number .value=${t}></gds-formatted-number>
        <gds-badge size="small">SEK</gds-badge>
      </gds-flex>
    `}},Re=async()=>le||T||(T=fetch(we).then(e=>e.json()).then(e=>(le=e,e)),T),De=ye(Re),ue={Columns:[{key:"name",label:"Name",sortable:!0,align:"start"},{key:"feedback",label:"Feedback",align:"start",width:"350px"},{key:"notes",label:"Notes",align:"start",width:"460px"},{key:"department",label:"Department",align:"start",width:"120px"},{key:"status",label:"Status",sortable:!0,align:"start",justify:"end",width:"100px"}],Data:De},ge=[{id:1,task:"Review contract",assignee:"Anna Svensson",priority:"High"},{id:2,task:"Update pricing",assignee:"Erik Lindgren",priority:"Medium"},{id:3,task:"Send invoice",assignee:"Maria Berg",priority:"Low"},{id:4,task:"Approve budget",assignee:"Johan Nilsson",priority:"High"}],Ee=async e=>{const t=(e.page-1)*e.rows;return{rows:ge.slice(t,t+e.rows).map(a=>({...a,actions:x(void 0,["main"],a.id)})),total:ge.length}},E={Columns:[{key:"task",label:"Task",align:"center"},{key:"assignee",label:"Assignee",align:"center"},{key:"priority",label:"Priority",align:"center",width:"100px"},{key:"actions",label:"Actions",align:"center",justify:"end",sticky:"right"}],MultipleActionsSlotContent:e=>l`
      ${e.map((t,n)=>l`
          <gds-button
            slot="actions:${t.id??n+1}:main"
            rank="secondary"
            size="small"
            variant="positive"
          >
            <gds-icon-circle-check size="m"></gds-icon-circle-check>
          </gds-button>
          <gds-button
            slot="actions:${t.id??n+1}:main"
            rank="secondary"
            size="small"
            variant="negative"
          >
            <gds-icon-cross-small size="m"></gds-icon-cross-small>
          </gds-button>
        `)}
    `,ActionLinkSlotContent:e=>l`
      ${e.map((t,n)=>l`
          <gds-button
            slot="actions:${t.id??n+1}:main"
            size="small"
            rank="secondary"
          >
            View details
          </gds-button>
        `)}
    `,ActionContextMenuSlotContent:e=>l`
      ${e.map((t,n)=>l`
          <gds-context-menu slot="actions:${t.id??n+1}:main">
            <gds-button slot="trigger" rank="tertiary" size="small">
              <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
            </gds-button>
            <gds-menu-item>Edit</gds-menu-item>
            <gds-menu-item>Archive</gds-menu-item>
            <gds-menu-item>Delete</gds-menu-item>
          </gds-context-menu>
        `)}
    `,Data:Ee},Pe=[{id:201,account:"SEB Payroll Main",accountNumber:"5001 10 123 45",owner:"Nordic Retail AB",segment:"Operating",currency:"SEK",amount:125e4,available:98e4,status:"Healthy",lastUpdated:"2026-03-01",expansionMode:"children"},{id:202,account:"SEB Liquidity Reserve",accountNumber:"5001 10 128 01",owner:"Nordic Retail AB",segment:"Treasury",currency:"SEK",amount:34e5,available:3389e3,status:"Healthy",lastUpdated:"2026-03-01",expansionMode:"full"},{id:203,account:"SEB Project Escrow",accountNumber:"5001 10 129 77",owner:"Green Build One",segment:"Escrow",currency:"SEK",amount:49e4,available:21e4,status:"Watch",lastUpdated:"2026-03-02",expansionMode:"children"},{id:204,account:"SEB Vendor Settlement",accountNumber:"5001 10 132 09",owner:"Supply Chain Ops",segment:"Operating",currency:"SEK",amount:28e4,available:75e3,status:"Risk",lastUpdated:"2026-03-02",expansionMode:"full"},{id:205,account:"SEB FX Settlement Flow",accountNumber:"5001 10 140 55",owner:"Cross Border Team",segment:"FX",currency:"SEK",amount:77e4,available:59e4,status:"Healthy",lastUpdated:"2026-03-03",expansionMode:"none"},{id:206,account:"SEB Tax Holding",accountNumber:"5001 10 143 88",owner:"Tax Office",segment:"Compliance",currency:"SEK",amount:36e4,available:359900,status:"Healthy",lastUpdated:"2026-03-03",expansionMode:"none"},{id:207,account:"SEB Benefits Wallet",accountNumber:"5001 10 147 31",owner:"People Operations",segment:"Payroll",currency:"SEK",amount:42e4,available:165e3,status:"Watch",lastUpdated:"2026-03-04",expansionMode:"children"},{id:208,account:"SEB Capital Buffer",accountNumber:"5001 10 149 92",owner:"CFO Office",segment:"Treasury",currency:"SEK",amount:51e5,available:5095e3,status:"Healthy",lastUpdated:"2026-03-04",expansionMode:"children"},{id:209,account:"SEB M&A Holding",accountNumber:"5001 10 151 66",owner:"Strategy Group",segment:"Investment",currency:"SEK",amount:11e5,available:85e4,status:"Watch",lastUpdated:"2026-03-05",expansionMode:"full"},{id:210,account:"SEB Lease Clearing",accountNumber:"5001 10 158 03",owner:"Property Ops",segment:"Operating",currency:"SEK",amount:195e3,available:42e3,status:"Risk",lastUpdated:"2026-03-05",expansionMode:"children"},{id:211,account:"SEB Insurance Pool",accountNumber:"5001 10 161 74",owner:"Risk Management",segment:"Insurance",currency:"SEK",amount:62e4,available:6e5,status:"Healthy",lastUpdated:"2026-03-06",expansionMode:"children"},{id:212,account:"SEB Card Settlement",accountNumber:"5001 10 166 20",owner:"Payments Platform",segment:"Payments",currency:"SEK",amount:86e4,available:31e4,status:"Watch",lastUpdated:"2026-03-06",expansionMode:"children"}],Ue=e=>e.map(t=>({...t,account:x(t.account,["value"]),amount:x(t.amount,["amount-value","amount-currency"]),available:x(t.available,["available-value","available-currency"]),status:x(t.status,["status"]),accountNumber:String(t.accountNumberDisplay??""),accountNumberDisplay:x(t.accountNumberDisplay,["main"]),actions:x(void 0,["main"])})),Le=Pe.map(e=>({...e.expansionMode==="children"?{isExpandable:!0,subRowCount:2,subRows:async()=>{const t=Te[Number(e.id)]??[];return await new Promise(n=>setTimeout(n,180)),Ue(t)}}:e.expansionMode==="full"?{isExpandable:!0,subRowCount:1,fullContent:async()=>(await new Promise(t=>setTimeout(t,180)),!0)}:{isExpandable:e.expansionMode!=="none"},...e,account:x(e.account,["account-icon","value"]),amount:x(e.amount,["amount-value","amount-currency"]),available:x(e.available,["available-value","available-currency"]),status:x(e.status,["status"]),accountNumberDisplay:x(e.accountNumber,["main"]),actions:x(void 0,["main"])})),Ae=async e=>{let t=[...Le];if(e.searchQuery){const s=e.searchQuery.toLowerCase();t=t.filter(o=>[o.id,o.account,o.owner,o.segment,o.currency,o.status,o.accountNumber].join(" ").toLowerCase().includes(s))}if(e.sortColumn&&t.length>0){const s=e.sortColumn;t.sort((o,d)=>{const g=String(o[s]??""),p=String(d[s]??"");return e.sortDirection==="desc"?p.localeCompare(g):g.localeCompare(p)})}const n=(e.page-1)*e.rows,a=n+e.rows;return{rows:t.slice(n,a),total:t.length}},Te={201:[{id:"201-r1",account:"Payroll Batch - March",owner:"Nordic Retail AB",segment:"Outgoing",status:"Booked",amount:52e4,available:73e4,currency:"SEK",lastUpdated:"2026-03-01",accountNumberDisplay:"5001 10 123 45"},{id:"201-r2",account:"Payroll Tax Reserve",owner:"Nordic Retail AB",segment:"Reserved",status:"Pending",amount:21e4,available:52e4,currency:"SEK",lastUpdated:"2026-03-01",accountNumberDisplay:"5001 10 123 45"}],203:[{id:"203-r1",account:"Project Milestone A",owner:"Green Build One",segment:"Escrow Hold",status:"Held",amount:18e4,available:31e4,currency:"SEK",lastUpdated:"2026-03-02",accountNumberDisplay:"5001 10 129 77"},{id:"203-r2",account:"Project Milestone B",owner:"Green Build One",segment:"Escrow Hold",status:"Held",amount:11e4,available:2e5,currency:"SEK",lastUpdated:"2026-03-02",accountNumberDisplay:"5001 10 129 77"}],205:[{id:"205-r1",account:"Cross Border Payroll Bridge",owner:"Cross Border Team",segment:"FX Transfer",status:"Booked",amount:24e4,available:53e4,currency:"SEK",lastUpdated:"2026-03-03",accountNumberDisplay:"5001 10 140 55"}],207:[{id:"207-r1",account:"Benefits Payout Week 10",owner:"People Operations",segment:"Outgoing",status:"Pending",amount:14e4,available:28e4,currency:"SEK",lastUpdated:"2026-03-04",accountNumberDisplay:"5001 10 147 31"}],208:[{id:"208-r1",account:"Quarterly Buffer Allocation",owner:"CFO Office",segment:"Reserved",status:"Booked",amount:7e5,available:4395e3,currency:"SEK",lastUpdated:"2026-03-04",accountNumberDisplay:"5001 10 149 92"}],210:[{id:"210-r1",account:"Lease Settlement Q1",owner:"Property Ops",segment:"Outgoing",status:"Pending",amount:108e3,available:87e3,currency:"SEK",lastUpdated:"2026-03-05",accountNumberDisplay:"5001 10 158 03"}],212:[{id:"212-r1",account:"Card Clearing Morning",owner:"Payments Platform",segment:"Settled",status:"Booked",amount:21e4,available:65e4,currency:"SEK",lastUpdated:"2026-03-06",accountNumberDisplay:"5001 10 166 20"},{id:"212-r2",account:"Card Clearing Afternoon",owner:"Payments Platform",segment:"Pending",status:"Pending",amount:34e4,available:31e4,currency:"SEK",lastUpdated:"2026-03-06",accountNumberDisplay:"5001 10 166 20"}],211:[{id:"211-r1",account:"Insurance Buffer Allocation",owner:"Risk Management",segment:"Reserved",status:"Booked",amount:12e4,available:48e4,currency:"SEK",lastUpdated:"2026-03-06",accountNumberDisplay:"5001 10 161 74"}]},P={Columns:[{key:"account",label:"Name",sortable:!0,width:"28ch",sticky:"left"},{key:"owner",label:"Owner",sortable:!0,width:"22ch"},{key:"segment",label:"Segment",sortable:!0,width:"14ch"},{key:"status",label:"Status",sortable:!0,width:"12ch"},{key:"accountNumberDisplay",label:"Account",sortable:!0,width:"18ch"},{key:"amount",label:"Booked",sortable:!0,justify:"end"},{key:"available",label:"Balance",sortable:!0,justify:"end"},{key:"actions",label:"Actions",sortable:!1,justify:"end",sticky:"right"}],Tfoot:{label:"Totals",sticky:!0},Data:Ae,SlotContent:e=>l`
    ${e.map(t=>l`
        <gds-badge
          slot="status:${t.id}:status"
          size="small"
          variant="${String(t.status)==="Healthy"?"positive":String(t.status)==="Watch"?"warning":"negative"}"
        >
          ${String(t.status)}
        </gds-badge>

        <gds-formatted-account
          slot="accountNumberDisplay:${t.id}:main"
          account="${String(t.accountNumber??t.accountNumberDisplay??"")}"
          format="seb-account"
        ></gds-formatted-account>

        <gds-badge
          slot="account:${t.id}:account-icon"
          size="small"
          variant="information"
        >
          ${String(t.currency)}
        </gds-badge>

        <gds-formatted-number
          slot="amount:${t.id}:amount-value"
          .value=${Number(t.amount)}
        ></gds-formatted-number>
        <gds-badge slot="amount:${t.id}:amount-currency" size="small">
          ${String(t.currency)}
        </gds-badge>

        <gds-formatted-number
          slot="available:${t.id}:available-value"
          .value=${Number(t.available)}
        ></gds-formatted-number>
        <gds-badge slot="available:${t.id}:available-currency" size="small">
          ${String(t.currency)}
        </gds-badge>

        <gds-context-menu slot="actions:${t.id}:main">
          <gds-button slot="trigger" rank="tertiary" size="small">
            <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
          </gds-button>
          <gds-menu-item>Open account</gds-menu-item>
          <gds-menu-item>View transactions</gds-menu-item>
          <gds-menu-item>Export statement</gds-menu-item>
        </gds-context-menu>

        ${t.expansionMode==="full"&&t.id===204?l`
              <gds-card
                slot="expand:${t.id}:full"
                variant="neutral-02-outlined"
              >
                <gds-flex flex-direction="column" gap="m" padding="m">
                  <gds-flex
                    justify-content="space-between"
                    align-items="center"
                    flex-wrap="wrap"
                    gap="s"
                  >
                    <gds-flex flex-direction="column" gap="2xs">
                      <gds-text font="heading-xs">
                        Vendor Settlement Monitoring
                      </gds-text>
                      <gds-text font="detail-s-book">
                        Available balance on ${String(t.account)} is below
                        internal threshold for ${String(t.segment)}.
                      </gds-text>
                    </gds-flex>
                    <gds-badge variant="negative" size="small"
                      >Action needed</gds-badge
                    >
                  </gds-flex>

                  <gds-grid columns="1; s{1}; m{2}; l{3}" gap="s">
                    <gds-card variant="neutral-02-outlined" padding="s">
                      <gds-flex flex-direction="column" gap="2xs">
                        <gds-text font="detail-s-book">Booked Amount</gds-text>
                        <gds-formatted-number
                          .value=${Number(t.amount)}
                        ></gds-formatted-number>
                      </gds-flex>
                    </gds-card>

                    <gds-card variant="neutral-02-outlined" padding="s">
                      <gds-flex flex-direction="column" gap="2xs">
                        <gds-text font="detail-s-book">Available</gds-text>
                        <gds-formatted-number
                          .value=${Number(t.available)}
                        ></gds-formatted-number>
                      </gds-flex>
                    </gds-card>

                    <gds-card variant="neutral-02-outlined" padding="s">
                      <gds-flex flex-direction="column" gap="2xs">
                        <gds-text font="detail-s-book">Owner</gds-text>
                        <gds-text font="detail-s-bold"
                          >${String(t.owner)}</gds-text
                        >
                      </gds-flex>
                    </gds-card>
                  </gds-grid>

                  <gds-flex gap="s" flex-wrap="wrap">
                    <gds-button size="small">Top up account</gds-button>
                    <gds-button rank="secondary" size="small">
                      Set alert rule
                    </gds-button>
                    <gds-button rank="tertiary" size="small">
                      View settlement queue
                    </gds-button>
                  </gds-flex>
                </gds-flex>
              </gds-card>
            `:t.expansionMode==="full"&&t.id===202?l`
                <gds-card
                  slot="expand:${t.id}:full"
                  variant="neutral-02-outlined"
                >
                  <gds-flex flex-direction="column" gap="m" padding="m">
                    <gds-flex
                      justify-content="space-between"
                      align-items="center"
                      flex-wrap="wrap"
                      gap="s"
                    >
                      <gds-flex flex-direction="column" gap="2xs">
                        <gds-text font="heading-xs">
                          Liquidity Reserve Overview
                        </gds-text>
                        <gds-text font="detail-s-book">
                          ${String(t.account)} acts as the primary treasury
                          reserve used for short-term liquidity balancing.
                        </gds-text>
                      </gds-flex>
                      <gds-badge variant="information" size="small">
                        Treasury account
                      </gds-badge>
                    </gds-flex>

                    <gds-grid columns="s{1}; m{2}; l{4}" gap="s">
                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">
                            Booked Amount
                          </gds-text>
                          <gds-formatted-number
                            .value=${Number(t.amount)}
                          ></gds-formatted-number>
                        </gds-flex>
                      </gds-card>

                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Available</gds-text>
                          <gds-formatted-number
                            .value=${Number(t.available)}
                          ></gds-formatted-number>
                        </gds-flex>
                      </gds-card>

                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Owner</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.owner)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>

                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Updated</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.lastUpdated)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>
                    </gds-grid>

                    <gds-flex gap="s" flex-wrap="wrap">
                      <gds-button size="small">View cash forecast</gds-button>
                      <gds-button rank="secondary" size="small">
                        Transfer funds
                      </gds-button>
                      <gds-button rank="tertiary" size="small">
                        Download reserve report
                      </gds-button>
                    </gds-flex>
                  </gds-flex>
                </gds-card>
              `:t.expansionMode==="full"&&t.id===209?l`
                  <gds-flex
                    slot="expand:${t.id}:full"
                    gap="s"
                    align-items="center"
                    padding="m"
                  >
                    <gds-badge size="small" variant="warning"
                      >Monitoring</gds-badge
                    >
                    <gds-text font="detail-s-book">
                      Available balance on ${String(t.account)} is below
                      internal threshold for ${String(t.segment)}.
                    </gds-text>
                  </gds-flex>
                `:null}
      `)}
  `,TfootSlotContent:e=>{const t=e.reduce((s,o)=>s+(Number(o.amount)||0),0),n=e.reduce((s,o)=>s+(Number(o.available)||0),0),a=e.filter(s=>String(s.status)==="Risk").length;return l`
      <span slot="tfoot:account">${e.length} accounts</span>
      <gds-badge
        slot="tfoot:status"
        size="small"
        variant="${a>0?"negative":"positive"}"
      >
        ${a>0?`${a} at risk`:"No risk"}
      </gds-badge>
      <gds-flex slot="tfoot:amount" gap="s" align-items="center">
        <gds-formatted-number .value=${t}></gds-formatted-number>
        <gds-badge size="small">SEK</gds-badge>
      </gds-flex>
      <gds-flex slot="tfoot:available" gap="s" align-items="center">
        <gds-formatted-number .value=${n}></gds-formatted-number>
        <gds-badge size="small">SEK</gds-badge>
      </gds-flex>
    `}},me=["Engineering","Finance","Legal","Marketing","Operations","HR","Sales","Support","Design","Product"],D=["Active","Inactive","Pending","Review","Archived"],be=["SEK","EUR","USD","NOK","DKK"],pe=["Stockholm","Gothenburg","Malmo","Oslo","Copenhagen","Helsinki","London","Berlin"],he=["Critical","High","Medium","Low"],ze=e=>Array.from({length:e},(t,n)=>{const a=n+1,s=me[n%me.length],o=D[n%D.length],d=be[n%be.length],g=pe[n%pe.length],p=he[n%he.length],i=`User ${a}`,y=`5001 ${String(10+n%90).padStart(2,"0")} ${String(100+n).slice(-3)} ${String(10+n%90).padStart(2,"0")}`,C=`2024-${String(n%12+1).padStart(2,"0")}-${String(n%28+1).padStart(2,"0")}`,R=`2026-${String(n%12+1).padStart(2,"0")}-${String(n%28+1).padStart(2,"0")}`,w=`Manager ${n%20+1}`,u=`Team ${String.fromCharCode(65+n%8)}`,m=`Building ${n%5+1}`,$=`Project ${String.fromCharCode(65+n%12)}-${Math.floor(n/12)+1}`,oe=n%5===0,v=n%5===2,c={id:a,name:x(i,["avatar","value"]),email:x(`user${a}@example.com`,["value","copy-button"]),department:s,status:x(o,["status"]),region:g,priority:p,currency:d,amount:x(Math.round(Math.random()*5e5+1e3),["amount","currency"]),balance:Math.round(Math.random()*1e6),score:Math.round(Math.random()*100),rating:(Math.random()*5).toFixed(1),tasks:Math.floor(Math.random()*50),completed:Math.floor(Math.random()*40),account:y,joined:C,lastLogin:R,manager:w,team:u,building:m,floor:n%10+1,extension:1e3+n,costCenter:`CC-${String(100+n%50)}`,project:$,hours:Math.round(Math.random()*160+20),actions:x(void 0,["main"])};return oe?{...c,isExpandable:!0,subRowCount:2,subRows:async()=>[{id:`${a}-r1`,name:`${i} - Sub A`,email:`sub.a.${a}@example.com`,department:s,status:D[(n+1)%D.length],region:g,priority:p,currency:d,amount:a*1234%1e5+500,balance:a*5678%2e5,score:a*91%100,rating:(a*13%50/10).toFixed(1),tasks:a*3%10,completed:a*2%10,account:`${y}-A`,joined:C,lastLogin:R,manager:w,team:u,building:m,floor:c.floor,extension:c.extension+1,costCenter:c.costCenter,project:`${$}-A`,hours:a*7%80+10,actions:x(void 0,["main"])},{id:`${a}-r2`,name:`${i} - Sub B`,email:`sub.b.${a}@example.com`,department:s,status:D[(n+2)%D.length],region:g,priority:p,currency:d,amount:a*2345%1e5+500,balance:a*6789%2e5,score:a*47%100,rating:(a*17%50/10).toFixed(1),tasks:a*5%10,completed:a*4%10,account:`${y}-B`,joined:C,lastLogin:R,manager:w,team:u,building:m,floor:c.floor,extension:c.extension+2,costCenter:c.costCenter,project:`${$}-B`,hours:a*11%80+10,actions:x(void 0,["main"])}]}:v?{...c,isExpandable:!0,subRowCount:1,fullContent:async()=>!0}:{...c,isExpandable:!1}}),ie={},Ne=e=>(ie[e]||(ie[e]=ze(e)),ie[e]),Ie=e=>async t=>{let n=Ne(e);if(t.searchQuery){const s=t.searchQuery.toLowerCase();n=n.filter(o=>Object.values(o).some(d=>d?.toString().toLowerCase().includes(s)))}if(t.sortColumn&&n.length>0){const s=t.sortColumn;n=[...n].sort((o,d)=>{const g=o[s]?.toString()??"",p=d[s]?.toString()??"";return t.sortDirection==="asc"?g.localeCompare(p):p.localeCompare(g)})}const a=(t.page-1)*t.rows;return{rows:n.slice(a,a+t.rows),total:n.length}},ce={Columns:[{key:"id",label:"ID",sortable:!0,width:"6ch",sticky:"left"},{key:"name",label:"Name",sortable:!0,width:"20ch",sticky:"left"},{key:"email",label:"Email",sortable:!0,width:"24ch",justify:"space-between"},{key:"department",label:"Department",sortable:!0},{key:"status",label:"Status",sortable:!0},{key:"region",label:"Region",sortable:!0},{key:"priority",label:"Priority",sortable:!0},{key:"currency",label:"Currency",sortable:!0,width:"10ch"},{key:"amount",label:"Amount",sortable:!0,justify:"end"},{key:"balance",label:"Balance",sortable:!0,justify:"end"},{key:"score",label:"Score",sortable:!0,justify:"end",width:"8ch"},{key:"rating",label:"Rating",sortable:!0,justify:"end",width:"8ch"},{key:"tasks",label:"Tasks",sortable:!0,justify:"end",width:"8ch"},{key:"completed",label:"Completed",sortable:!0,justify:"end",width:"10ch"},{key:"account",label:"Account",sortable:!0,width:"20ch"},{key:"joined",label:"Joined",sortable:!0},{key:"lastLogin",label:"Last Login",sortable:!0},{key:"manager",label:"Manager",sortable:!0},{key:"team",label:"Team",sortable:!0,width:"10ch"},{key:"building",label:"Building",sortable:!0,width:"12ch"},{key:"floor",label:"Floor",sortable:!0,justify:"end",width:"8ch"},{key:"extension",label:"Ext.",sortable:!0,justify:"end",width:"8ch"},{key:"costCenter",label:"Cost Center",sortable:!0},{key:"project",label:"Project",sortable:!0},{key:"hours",label:"Hours",sortable:!1,justify:"end",sticky:"right"},{key:"actions",label:"Actions",justify:"end",sticky:"right",sortable:!0}],Data:Ie,SlotContent:e=>l`
    ${e.map(t=>l`
        <gds-img
          src="https://api.dicebear.com/9.x/notionists/svg?seed=${t.id}"
          alt="${String(t.name)}"
          slot="name:${t.id}:avatar"
          width="xl"
          height="xl"
        ></gds-img>

        <gds-button
          slot="email:${t.id}:copy-button"
          rank="tertiary"
          size="small"
        >
          <gds-icon-copy></gds-icon-copy>
        </gds-button>

        <gds-badge
          slot="status:${t.id}:status"
          size="small"
          variant="${String(t.status)==="Active"?"positive":String(t.status)==="Inactive"?"negative":String(t.status)==="Pending"?"warning":"information"}"
        >
          ${String(t.status)}
        </gds-badge>

        <gds-formatted-number
          slot="amount:${t.id}:amount"
          .value=${Number(t.amount)}
        ></gds-formatted-number>
        <gds-badge slot="amount:${t.id}:currency" size="small">
          ${String(t.currency)}
        </gds-badge>

        <gds-context-menu slot="actions:${t.id}:main">
          <gds-button slot="trigger" rank="tertiary" size="small">
            <gds-icon-dot-grid-one-horizontal></gds-icon-dot-grid-one-horizontal>
          </gds-button>
          <gds-menu-item>View</gds-menu-item>
          <gds-menu-item>Edit</gds-menu-item>
        </gds-context-menu>

        ${typeof t.fullContent=="function"?t.id===8?l`
                <gds-table
                  plain
                  slot="expand:${t.id}:full"
                  .columns=${r.Columns}
                  .data=${r.Data}
                  @gds-table-data-loaded=${n=>n.stopPropagation()}
                ></gds-table>
              `:l`
                <gds-card
                  slot="expand:${t.id}:full"
                  variant="neutral-02-outlined"
                >
                  <gds-flex flex-direction="column" gap="m" padding="m">
                    <gds-flex
                      justify-content="space-between"
                      align-items="center"
                      flex-wrap="wrap"
                      gap="s"
                    >
                      <gds-flex flex-direction="column" gap="2xs">
                        <gds-text font="heading-xs">
                          ${String(t.name)} - Extended Details
                        </gds-text>
                        <gds-text font="detail-s-book">
                          ${String(t.department)} · ${String(t.region)} ·
                          ${String(t.priority)} priority
                        </gds-text>
                      </gds-flex>
                      <gds-badge
                        variant="${String(t.status)==="Active"?"positive":String(t.status)==="Inactive"?"negative":String(t.status)==="Pending"?"warning":"information"}"
                        size="small"
                      >
                        ${String(t.status)}
                      </gds-badge>
                    </gds-flex>

                    <gds-grid columns="1; s{1}; m{2}; l{4}" gap="s">
                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Manager</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.manager)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>
                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Team</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.team)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>
                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Cost Center</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.costCenter)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>
                      <gds-card variant="neutral-02-outlined" padding="s">
                        <gds-flex flex-direction="column" gap="2xs">
                          <gds-text font="detail-s-book">Project</gds-text>
                          <gds-text font="detail-s-bold">
                            ${String(t.project)}
                          </gds-text>
                        </gds-flex>
                      </gds-card>
                    </gds-grid>

                    <gds-flex gap="s" flex-wrap="wrap">
                      <gds-button size="small">View profile</gds-button>
                      <gds-button rank="secondary" size="small"
                        >Edit</gds-button
                      >
                      <gds-button rank="tertiary" size="small"
                        >Export</gds-button
                      >
                    </gds-flex>
                  </gds-flex>
                </gds-card>
              `:null}
      `)}
  `},fe=new WeakSet,Me=e=>l`
  <div
    ${b(t=>{if(!t||fe.has(t))return;fe.add(t);const n=new IntersectionObserver(a=>{if(a[0].isIntersecting){n.disconnect();const s=e();f(s,t);const o=t.parentNode;if(o){for(;t.firstChild;)o.insertBefore(t.firstChild,t);o.removeChild(t)}}},{rootMargin:"200px"});n.observe(t)})}
    style="min-height:40vh"
  ></div>
`,Wn={title:"Components/Table",component:"gds-table",argTypes:{...ve("gds-table"),density:{control:{type:"select"},options:["comfortable","compact","spacious"],defaultValue:"comfortable"},variant:{control:{type:"select"},options:["neutral-01","neutral-02","neutral-02-outlined"],defaultValue:"neutral-02-outlined"},selectable:{control:{type:"boolean"},defaultValue:!0},striped:{control:{type:"boolean"},defaultValue:!0},searchLabel:{control:{type:"text"}},height:{control:{type:"string"},defaultValue:!1}},tags:["autodocs"],decorators:[(e,t)=>t.viewMode==="docs"?Me(()=>e()):e()],parameters:{docs:{description:{component:`
A data table component that handles asynchronous data loading, pagination, sorting, and filtering through a provider function. Supports row selection, searchable columns, responsive layouts, and customizable cell rendering including badges, buttons, links, and formatted values.
        `}}}},N={args:{columns:r.Columns,density:"comfortable",variant:"neutral-02-outlined",data:r.Data,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?height="${e.height}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        rows="60"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},I={args:{columns:P.Columns,data:P.Data,tfoot:P.Tfoot,height:"480px",variant:"neutral-02-outlined",density:"comfortable",striped:!0},parameters:{docs:{description:{story:`
The table automatically adds a left-most toggle column when rows are expandable.

This example demonstrates mixed rows where some rows are expandable and others are not.
Expanded rows are rendered as aligned child rows so all columns remain in the same grid.
        `}}},render:e=>{const t=h(),n=h(),a=e.variant==="neutral-02"?"neutral-02":"neutral-01";let s=[],o=0,d=[],g=0;const p=()=>{const u=n.value;if(!u)return;const m=u.querySelector("[data-slideout-prev]"),$=u.querySelector("[data-slideout-next]");m&&m.toggleAttribute("disabled",g<=0),$&&$.toggleAttribute("disabled",g>=d.length-1)},i=()=>{const u=n.value;if(!u)return;const m=u.querySelector("[data-slideout-content]");if(!m)return;if(!d.length){u.heading="Account details",f(l`<gds-text>No account selected.</gds-text>`,m),p();return}const $=d[g];u.heading=String($.account??"Account details"),f(l`
          <gds-flex flex-direction="column" gap="m">
            <gds-card
              flex-direction="row"
              variant="neutral-02-outlined"
              justify-content="space-between"
              align-items="center"
              padding="s l s s"
            >
              <gds-flex gap="2xs" align-items="center">
                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Previous account"
                  data-slideout-prev
                  @click=${()=>{g<=0||(g-=1,i())}}
                >
                  <gds-icon-chevron-left></gds-icon-chevron-left>
                </gds-button>

                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Next account"
                  data-slideout-next
                  @click=${()=>{g>=d.length-1||(g+=1,i())}}
                >
                  <gds-icon-chevron-right></gds-icon-chevron-right>
                </gds-button>
              </gds-flex>

              <gds-text font="detail-xs-book" color="neutral-02">
                ${g+1} / ${d.length}
              </gds-text>
            </gds-card>

            <gds-grid columns="1; m{2}" gap="s">
              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Owner</gds-text>
                  <gds-text font="detail-s-bold">${String($.owner)}</gds-text>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Status</gds-text>
                  <gds-text font="detail-s-bold"
                    >${String($.status)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Booked</gds-text>
                  <gds-formatted-number
                    .value=${Number($.amount)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Balance</gds-text>
                  <gds-formatted-number
                    .value=${Number($.available)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>
            </gds-grid>
          </gds-flex>
        `,m),p()},y=u=>{const m=s.findIndex($=>$.id===u);m<0||(d=s,g=m,i(),n.value?.show?.("row-action"))},C=()=>{const u=t.value;u&&f(l`
          ${P.SlotContent(s)}
          ${P.TfootSlotContent(s)}
          ${s.map(m=>l`
              <gds-button
                slot="actions:${m.id}:main"
                rank="secondary"
                size="small"
                @click=${()=>y(m.id)}
              >
                Open details
              </gds-button>
            `)}
          ${o>0?l`
                <gds-button slot="header-trail" rank="secondary" size="small">
                  <gds-icon-cloud-download
                    slot="lead"
                  ></gds-icon-cloud-download>
                  Export data (${o})
                </gds-button>
              `:null}
        `,u)},R=u=>{s=u.detail.rows,C(),i()},w=u=>{o=Number(u.detail?.count??0),C()};return l`
      <gds-card
        level="1"
        background="${a}"
        padding="l"
        border-radius="m"
      >
        <gds-table
          ${b(t)}
          .density="${e.density}"
          .columns="${e.columns}"
          .data="${e.data}"
          .tfoot="${e.tfoot}"
          rows="10"
          .height="${e.height}"
          .variant="${e.variant}"
          selectable
          searchable
          responsive
          settings
          ?striped="${e.striped}"
          @gds-table-data-loaded=${R}
          @gds-table-selection=${w}
        >
        </gds-table>

        <gds-dialog
          ${b(n)}
          variant="slide-out"
          heading="Account details"
        >
          <div data-slideout-content></div>
        </gds-dialog>
      </gds-card>
    `}},M={args:{columns:r.Columns,data:r.Data,selectable:!0,density:"comfortable"},parameters:{docs:{description:{story:`
The \`selectable\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \`gds-table-selection\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        .density="${e.density}"
        ?selectable="${e.selectable}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},B={args:{columns:r.Columns,data:r.Data,searchable:!0,settings:!0},parameters:{docs:{description:{story:`
#### Search Functionality
The \`searchable\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \`settings\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},F={args:{columns:r.Columns,data:r.Data,searchable:!0,settings:!0,nocache:!0},parameters:{docs:{description:{story:"\nThe `nocache` property provides direct control over data retrieval. When set to `true`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.\n\n> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.\n        "}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        ?nocache="${e.nocache}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},j={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:`
Sorting is enabled by setting \`sortable: true\` on column configuration. <br />
Clicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />
The sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a \`gds-sort-change\` event.

This story shows two tables:
1) Regular sorting (internal state)
2) URL-driven sorting using per-column \`sorting\` state (controlled mode)
        `}}},render:e=>{const t=h(),n=h(),a=h(),s=h(),o=()=>{const v=new URLSearchParams(window.location.search),c=v.get("sortColumn")||"",S=v.get("sortDirection");return S!=="asc"&&S!=="desc"||!e.columns.some(k=>k.key===c)?null:{sortColumn:c,sortDirection:S}},d=(v,c,S)=>v.map(k=>({...k,sorting:c&&k.key===c?S:void 0})),g=e.columns.filter(v=>v.sortable),p=o();let i=p?.sortColumn||g[0]?.key||"",y=p?.sortDirection||"asc",C=d(e.columns,i,y);const R=(v=i,c=y)=>{if(!v||c!=="asc"&&c!=="desc"||!e.columns.some(L=>L.key===v))return;i=v,y=c;const S=new URL(window.location.href);S.searchParams.set("sortColumn",v),S.searchParams.set("sortDirection",c),window.history.replaceState({},"",S),C=d(e.columns,v,c);const k=n.value;k&&(k.columns=C)},w=v=>{const c=t.value;c&&f(r.SlotContent(v.detail.rows),c)},u=v=>{const c=n.value;c&&f(r.SlotContent(v.detail.rows),c)},m=v=>{const c=v?.value;return typeof c=="string"?c:Array.isArray(c)?String(c[0]??""):String(c??"")},$=()=>{const v=m(a.value),c=m(s.value);(c==="asc"||c==="desc")&&R(v,c)},oe=v=>{const c=String(v.detail?.sortColumn??""),S=v.detail?.sortDirection;if(!c||S!=="asc"&&S!=="desc")return;const k=new URL(window.location.href);k.searchParams.set("sortColumn",c),k.searchParams.set("sortDirection",S),window.history.replaceState({},"",k),i=c,y=S,a.value&&a.value.setAttribute("value",i),s.value&&s.value.setAttribute("value",y),C=d(e.columns,c,S);const L=n.value;L&&(L.columns=C)};return l`
      <gds-grid
        columns="1; l{1}; xl{2}"
        gap="6xl; l{6xl} xl{4xl}"
        width="100%"
        padding="4xl"
      >
        <gds-table
          ${b(t)}
          headline="Regular sorting"
          summary="Header clicks update internal table sort state."
          searchable
          .columns="${e.columns}"
          .data="${e.data}"
          @gds-table-data-loaded=${w}
        >
        </gds-table>

        <gds-table
          ${b(n)}
          headline="URL-driven controlled sorting"
          summary="Uses per-column sorting state controlled via header slot inputs."
          searchable
          .columns="${C}"
          .data="${e.data}"
          @gds-table-data-loaded=${u}
          @gds-sort-change=${oe}
        >
          <gds-dropdown
            slot="header-trail"
            ${b(a)}
            plain
            size="small"
            width="100px"
            label="Sort column"
            value="${i}"
            @change=${()=>$()}
          >
            ${g.map(v=>l`
                <gds-option value="${String(v.key)}">
                  ${String(v.label)}
                </gds-option>
              `)}
          </gds-dropdown>

          <gds-dropdown
            slot="header-trail"
            ${b(s)}
            plain
            size="small"
            width="100px"
            label="Sort direction"
            value="${y}"
            @change=${()=>$()}
          >
            <gds-option value="asc">Ascending</gds-option>
            <gds-option value="desc">Descending</gds-option>
          </gds-dropdown>
        </gds-table>
      </gds-grid>
    `}},O={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},V={args:{columns:ue.Columns,data:ue.Data},parameters:{docs:{description:{story:`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        `}}},render:e=>l`
    <gds-table .columns="${e.columns}" .data="${e.data}"></gds-table>
  `},K={args:{columns:E.Columns,data:E.Data},parameters:{docs:{description:{story:"\nActions are now implemented as a regular column using slot composition.\n\nAdd an `actions` column to `columns`, put `actions: Slot(undefined, ['main'])` in row data,\nand render content via the `actions:rowKey:main` slot.\n\nUse `sticky: 'right'` on the `actions` column when you want the action column pinned.\n        "}}},render:e=>{const t=h(),n=h(),a=h();return l`
      <gds-table
        ${b(t)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${s=>{const o=t.value;o&&f(E.MultipleActionsSlotContent(s.detail.rows),o)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${b(n)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${s=>{const o=n.value;o&&f(E.ActionContextMenuSlotContent(s.detail.rows),o)}}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        ${b(a)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=${2}
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${s=>{const o=a.value;o&&f(E.ActionLinkSlotContent(s.detail.rows),o)}}
      >
      </gds-table>
    `}},H={args:{columns:r.Columns,density:"comfortable",variant:"neutral-02-outlined",data:r.Data,tfoot:r.Tfoot,selectable:!0,searchable:!0,searchLabel:"Search users",settings:!0,plain:!1,height:"80vh",responsive:!1,nocache:!1,striped:!1},parameters:{docs:{description:{story:`
The \`tfoot\` property adds a \`<tfoot>\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \`label\` is provided, the first cell renders as a \`<th scope="row">\` for proper accessibility semantics.

#### Configuration

\`\`\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\`\`\`

#### Slot Convention

Table footer slots use the naming pattern \`tfoot:{columnKey}\`:

\`\`\`html
<gds-formatted-number slot="tfoot:amount" .value=\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\${count} active</gds-badge>
\`\`\`

Compute your aggregation in the \`gds-table-data-loaded\` handler and render slot content into the table.

When \`sticky: true\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(l`${r.SlotContent(a.detail.rows)}
          ${r.TfootSlotContent(a.detail.rows)}`,s)};return l`
      <gds-table
        ${b(t)}
        density="${e.density}"
        variant="${e.variant}"
        search-label="${e.searchLabel}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        ?plain="${e.plain}"
        ?responsive="${e.responsive}"
        ?nocache="${e.nocache}"
        ?striped="${e.striped}"
        ?selectable="${e.selectable}"
        height="${e.height}"
        .columns="${e.columns}"
        .data="${e.data}"
        .tfoot="${e.tfoot}"
        rows="50"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},W={args:{columns:r.Columns,data:r.Data,headline:"User Management",headlineTag:"h2",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:"\nWhen the `headline` and `summary` properties are set, they will be displayed at the top of the table.\n\nYou can change the `headline-tag` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.\n        "}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        headline="${e.headline}"
        headline-tag="${e.headlineTag}"
        summary="${e.summary}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},q={args:{columns:r.Columns,data:r.Data,plain:!0},parameters:{docs:{description:{story:"\nWhen `plain` boolean is set, the table header and footer are removed, rendering only the data table.\n        "}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        ?plain="${e.plain}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},G={args:{columns:r.Columns,data:r.Data,striped:!0,density:"compact"},parameters:{docs:{description:{story:"\nWhen `striped` boolean is set, alternating row background colors are applied to even-numbered rows.\n        "}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        .density="${e.density}"
        ?striped="${e.striped}"
        .columns="${e.columns}"
        .data="${e.data}"
        rows="20"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},_={args:{columns:r.Columns,data:r.Data,density:"compact",headline:"User Management",summary:"Overview of all users in the system"},parameters:{docs:{description:{story:'\nThe `density` property controls the table\'s visual spacing and affects the table\'s **built-in UI** (search input, column settings dropdown, pagination).\n\n- **`comfortable`** (Default) · Standard spacing for balanced readability\n- **`compact`** · Reduced spacing to maximize information density\n- **`spacious`** · Increased spacing for enhanced readability\n\n> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use `size="small"` on badges and buttons in `compact` or `comfortable` mode, and `size="medium"` in `spacious` mode to maintain visual consistency.\n        '}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        headline="${e.headline}"
        summary="${e.summary}"
        searchable
        settings
        .density="${e.density}"
        .columns="${e.columns}"
        .data="${e.data}"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},Q={args:{columns:r.Columns,data:r.Data,variant:"neutral-02-outlined",density:"comfortable"},argTypes:{variant:{control:"select",options:["neutral-01","neutral-02","neutral-02-outlined"]}},parameters:{docs:{description:{story:"\nThe `variant` property controls the visual style of the table container card.\nAvailable variants: `neutral-01`, `neutral-02`, `neutral-02-outlined` (Default)\n        "}}},render:e=>{const t=e.variant==="neutral-02"?"neutral-02":"neutral-01",n=h(),a=s=>{const o=n.value;o&&f(r.SlotContent(s.detail.rows),o)};return l`
      <gds-card
        level="1"
        background="${t}"
        padding="l"
        border-radius="m"
      >
        <gds-table
          ${b(n)}
          .density="${e.density}"
          .variant="${e.variant}"
          .columns="${e.columns}"
          .data="${e.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=${a}
        >
        </gds-table>
      </gds-card>
    `}},Y={args:{columns:r.Columns,data:r.Data,responsive:!0},parameters:{docs:{description:{story:`
When the \`responsive\` boolean is set to \`true\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**:
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        ?responsive="${e.responsive}"
        .columns="${e.columns}"
        .data="${e.data}"
        height="80vh"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},X={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=${n}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    `}},J={args:{columns:r.Columns,data:r.Data,simulatedState:"normal"},argTypes:{simulatedState:{control:"select",options:["normal","error","empty","no-results"],description:"Simulate different table states"}},parameters:{docs:{description:{story:`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        `}}},render:e=>{const t=h(),n=async d=>{if(await new Promise(g=>setTimeout(g,500)),e.simulatedState==="error")throw new Error("Simulated error state");return e.simulatedState==="empty"?{rows:[],total:0}:e.simulatedState==="no-results"?{rows:[],total:0}:e.data(d)},a=d=>{const g=t.value;g&&f(r.SlotContent(d.detail.rows),g)},s=()=>{e.simulatedState="normal",t.value&&(t.value.dataLoadKey=`retry-${Date.now()}`)},o=()=>{if(e.simulatedState="normal",t.value){t.value.dataLoadKey=`clear-${Date.now()}`;const d=t.value.shadowRoot?.querySelector("gds-input");d&&(d.value="")}};return setTimeout(()=>{if(e.simulatedState==="no-results"&&t.value){const d=t.value.shadowRoot?.querySelector("gds-input");d&&(d.value="test search",d.dispatchEvent(new Event("input",{bubbles:!0})))}},100),l`
      <gds-table
        ${b(t)}
        .columns="${e.columns}"
        .data="${n}"
        .dataLoadKey="${e.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=${a}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-m-book">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${s}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-m-book">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-m-book">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=${o}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    `}},Z={parameters:{docs:{description:{story:`
Demonstrates the two loading states the table produces during data fetching.

- **Skeleton**: Shown on initial load (or when navigating to an uncached page). The table renders placeholder rows with animated skeleton cells until the data resolves.
- **Fade**: When navigating between pages that are not yet cached, the existing rows fade while the new data loads in the background.
        `}}},render:()=>{const e=h(),t=h(),n=async o=>new Promise(()=>{}),a=async o=>(await new Promise(d=>setTimeout(d,1500)),r.Data(o)),s=o=>{const d=t.value;d&&f(r.SlotContent(o.detail.rows),d)};return l`
      <gds-flex flex-direction="column" gap="4xl">
        <gds-table
          ${b(e)}
          nocache
          headline="Skeleton"
          summary="Simulates the initial loading state with animated skeleton placeholders."
          rows="4"
          .columns="${r.Columns}"
          .data="${n}"
        ></gds-table>
        <gds-table
          ${b(t)}
          nocache
          headline="Fade"
          summary="Existing rows fade out while new data is loading when switching between pages."
          rows="4"
          .columns="${r.Columns}"
          .data="${a}"
          @gds-table-data-loaded=${s}
        ></gds-table>
      </gds-flex>
    `}},ee={args:{columns:r.Columns,data:r.Data,headline:"User Management",summary:"Overview of all users in the system",selectable:!0,searchable:!0,settings:!0,density:"comfortable"},parameters:{docs:{description:{story:`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \`key\` parameter in \`Slot()\`, or it falls back to \`row.id\`, then the row index.

#### 1. Compose data with \`Slot()\`

\`\`\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\`\`\`

The arguments are positional: \`Slot(value, slots, key)\` or \`Slot(value, key, slots)\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \`'value'\` to render the plain text alongside your components), and **key** is an optional \`string | number\` used as a stable row identifier.

#### 2. Render slot content on data load

\`\`\`ts
@gds-table-data-loaded=\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\`\`\`

#### 3. Slot naming convention

The slot name follows the pattern \`columnKey:rowKey:slotId\`.

\`\`\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\`\`\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \`Slot()\` API.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-table
        ${b(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        .headline="${e.headline}"
        .summary="${e.summary}"
        ?selectable="${e.selectable}"
        ?searchable="${e.searchable}"
        ?settings="${e.settings}"
        density="${e.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=${n}
      >
      </gds-table>
    `}},te={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:`
The table component dispatches events for various user interactions.

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \`{ page: number }\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \`{ rows: number }\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

**Settings Events**
- **gds-column-visibility-change**: Fired when the user toggles column visibility via the settings dropdown. Detail: \`{ visibleColumns: string[] }\`

\`\`\`js
table.addEventListener('gds-column-visibility-change', (e) => {
  // e.detail.visibleColumns — string[] of currently visible column keys
  // any key absent from this array is hidden
  console.log(e.detail.visibleColumns)
})
\`\`\`

This example demonstrates listening to pagination, sorting, and column visibility events and displaying the current state.
        `}}},render:e=>{const t=h(),a={currentPage:1,currentRows:10,sortColumn:"",sortDirection:"asc",visibleColumns:e.columns.filter(i=>i.visible!==!1).map(i=>i.key).join(", ")},s=i=>{const y=t.value;y&&f(r.SlotContent(i.detail.rows),y)},o=i=>{console.log("🔔 Page change event:",i.detail),a.currentPage=i.detail.page;const y=document.querySelector("#page-status");y&&(y.textContent=`Page: ${a.currentPage}`)},d=i=>{console.log("🔔 Rows change event:",i.detail),a.currentRows=i.detail.rows;const y=document.querySelector("#rows-status");y&&(y.textContent=`Rows: ${a.currentRows}`)},g=i=>{console.log("🔔 Sort change event:",i.detail),a.sortColumn=i.detail.sortColumn,a.sortDirection=i.detail.sortDirection;const y=document.querySelector("#sort-status");y&&(y.textContent=`${a.sortColumn} (${a.sortDirection})`)},p=i=>{console.log("🔔 Column visibility change event:",i.detail),a.visibleColumns=i.detail.visibleColumns.join(", ");const y=document.querySelector("#column-visibility-status");y&&(y.textContent=a.visibleColumns)};return l`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="neutral-02" padding="m" border-radius="m" outline>
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-s-book" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Page: ${a.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Rows: ${a.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    ${a.sortColumn?`${a.sortColumn} (${a.sortDirection})`:"None"}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Visible Columns
                  </gds-text>
                  <gds-text
                    id="column-visibility-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    ${a.visibleColumns}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          ${b(t)}
          .columns="${e.columns}"
          .data="${e.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=${s}
          @gds-page-change=${i=>o(i)}
          @gds-rows-change=${i=>d(i)}
          @gds-sort-change=${i=>g(i)}
          @gds-column-visibility-change=${p}
        >
        </gds-table>
      </gds-flex>
    `}},ne={args:{rows:8,options:[8,20]},parameters:{docs:{description:{story:`
      Demonstrates a pagination edge case where the selected page size exceeds the total number of rows.

1. Dataset has 13 rows total
2. Initial page size is 8
3. Change rows per page to 20

In this edge case, page navigation is hidden when only one page remains, while the rows-per-page selector stays visible so users can switch back to a smaller page size.
        `}}},render:e=>{const t=[{key:"id",label:"ID",sortable:!0},{key:"name",label:"Name",sortable:!0},{key:"status",label:"Status",sortable:!0}],n=Array.from({length:13},(s,o)=>({id:o+1,name:`User ${o+1}`,status:o%2===0?"Active":"Inactive"}));return l`
      <gds-table
        headline="Pagination edge case"
        summary="13 rows total with page-size switching between 8 and 20."
        .columns=${t}
        .data=${async s=>{const o=(s.page-1)*s.rows,d=o+s.rows;return{rows:n.slice(o,d),total:n.length}}}
        .rows=${e.rows}
        .options=${e.options}
      >
      </gds-table>
    `}},ae={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:"\nReplaces the built-in table footer with a compact `gds-pagination` in `simple` mode by using the `footer` slot. <br/>\nThis is the recommended approach when you want minimal pagination UI, just prev/next arrows and a short range label.\n        "}}},render:e=>{const t=h(),n=8,a=120;let s=1;const o=g=>{const p=t.value;p&&f(r.SlotContent(g.detail.rows),p)},d=g=>{s=g.detail.page;const p=t.value;p&&(p.page=s);const i=document.querySelector("[data-pagination-simple-table]");i&&i.setAttribute("page",String(s))};return l`
      <gds-table
        ${b(t)}
        .columns="${e.columns}"
        .data="${e.data}"
        .rows="${n}"
        searchable
        settings
        @gds-table-data-loaded=${o}
      >
        <gds-pagination
          data-pagination-simple-table
          slot="footer"
          simple
          hide-options
          page="1"
          .rows="${n}"
          .total="${a}"
          .options="${[n]}"
          label="1-${n} of ${a}"
          @gds-page-change=${d}
        ></gds-pagination>
      </gds-table>
    `}},U={args:{columns:r.Columns,data:r.Data},parameters:{docs:{description:{story:`
Demonstrates ARIA forwarding for tables, allowing external labels and descriptions to be associated with the internal table element.
        `}}},render:e=>{const t=h(),n=a=>{const s=t.value;s&&f(r.SlotContent(a.detail.rows),s)};return l`
      <gds-flex flex-direction="column" gap="s">
        <div id="table-label">User Directory</div>
        <div id="table-description">
          A list of all registered users in the system
        </div>
        <gds-table
          ${b(t)}
          plain
          gds-aria-labelledby="table-label"
          gds-aria-describedby="table-description"
          .columns="${e.columns}"
          .data="${e.data}"
          rows="5"
          @gds-table-data-loaded=${n}
        >
        </gds-table>
      </gds-flex>
    `}},se={args:{rowCount:200,height:"70vh",density:"comfortable",variant:"neutral-02-outlined",searchable:!0,selectable:!0,settings:!0,striped:!0,nocache:!0,responsive:!0},argTypes:{rowCount:{control:{type:"select"},options:[50,100,200,500],description:"Rows per page for the performance test."}},parameters:{docs:{description:{story:`
Stress test story for measuring rendering performance with a 1000-row dataset.
Select between 50, 100, 200, and 500 rows per page across 25+ columns with slot-based cells.

Every 5th row has async sub-rows (child rows), and every 5th row offset by 2 has a full-width async expanded panel.
        `}}},render:e=>{const t=h(),n=h();let a=[],s=0,o=[],d=0;const g=()=>{const w=n.value;if(!w)return;const u=w.querySelector("[data-slideout-prev]"),m=w.querySelector("[data-slideout-next]");u&&u.toggleAttribute("disabled",d<=0),m&&m.toggleAttribute("disabled",d>=o.length-1)},p=()=>{const w=n.value;if(!w)return;const u=w.querySelector("[data-slideout-content]");if(!u)return;if(!o.length){w.heading="User details",f(l`<gds-text>No user selected.</gds-text>`,u),g();return}const m=o[d];w.heading=String(m.name),f(l`
          <gds-flex flex-direction="column" gap="m">
            <gds-card
              flex-direction="row"
              variant="neutral-02-outlined"
              justify-content="space-between"
              align-items="center"
              padding="s l s s"
            >
              <gds-flex gap="2xs" align-items="center">
                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Previous user"
                  data-slideout-prev
                  @click=${()=>{d<=0||(d-=1,p())}}
                >
                  <gds-icon-chevron-left></gds-icon-chevron-left>
                </gds-button>

                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Next user"
                  data-slideout-next
                  @click=${()=>{d>=o.length-1||(d+=1,p())}}
                >
                  <gds-icon-chevron-right></gds-icon-chevron-right>
                </gds-button>
              </gds-flex>

              <gds-text font="detail-xs-book" color="neutral-02">
                ${d+1} / ${o.length}
              </gds-text>
            </gds-card>

            <gds-grid columns="1; m{2}" gap="s">
              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Department</gds-text>
                  <gds-text font="detail-s-bold">
                    ${String(m.department)}
                  </gds-text>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Status</gds-text>
                  <gds-text font="detail-s-bold"
                    >${String(m.status)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Amount</gds-text>
                  <gds-formatted-number
                    .value=${Number(m.amount)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Balance</gds-text>
                  <gds-formatted-number
                    .value=${Number(m.balance)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Region</gds-text>
                  <gds-text font="detail-s-bold"
                    >${String(m.region)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Manager</gds-text>
                  <gds-text font="detail-s-bold">
                    ${String(m.manager)}
                  </gds-text>
                </gds-flex>
              </gds-card>
            </gds-grid>
          </gds-flex>
        `,u),g()},i=w=>{const u=a.findIndex(m=>m.id===w);u<0||(o=a,d=u,p(),n.value?.show?.("row-action"))},y=()=>{const w=t.value;w&&f(l`
          ${ce.SlotContent(a)}
          ${a.map(u=>l`
              <gds-button
                slot="actions:${u.id}:main"
                rank="secondary"
                size="small"
                @click=${()=>i(u.id)}
              >
                Open details
              </gds-button>
            `)}
          ${a.filter(u=>typeof u.subRows=="function").flatMap(u=>[l`
                <gds-button
                  slot="actions:${u.id}-r1:main"
                  rank="secondary"
                  variant="negative"
                  width="14ch"
                  size="small"
                >
                  Deny
                </gds-button>
              `,l`
                <gds-button
                  slot="actions:${u.id}-r2:main"
                  rank="secondary"
                  variant="positive"
                  width="14ch"
                  size="small"
                >
                  Approve
                </gds-button>
              `])}
          ${s>0?l`
                <gds-button slot="header-trail" rank="secondary" size="small">
                  <gds-icon-cloud-download
                    slot="lead"
                  ></gds-icon-cloud-download>
                  Export data (${s})
                </gds-button>
              `:null}
        `,w)},C=w=>{a=w.detail.rows,y(),p()},R=w=>{s=Number(w.detail?.count??0),y()};return l`
      <gds-card level="1" background="neutral-01" padding="l" border-radius="m">
        <gds-table
          ${b(t)}
          headline="Performance Test - 1000 rows"
          summary="Stress test with expandable sub-rows and full-width panels."
          .columns=${ce.Columns}
          .data=${ce.Data(1e3)}
          .height="${e.height}"
          .density="${e.density}"
          .variant="${e.variant}"
          search-label="Search"
          ?searchable="${e.searchable}"
          ?selectable="${e.selectable}"
          ?settings="${e.settings}"
          ?striped="${e.striped}"
          ?nocache="${e.nocache}"
          ?responsive="${e.responsive}"
          .rows=${e.rowCount}
          .options=${[50,100,200,500]}
          @gds-table-data-loaded=${C}
          @gds-table-selection=${R}
        >
        </gds-table>

        <gds-dialog
          ${b(n)}
          variant="slide-out"
          heading="User details"
        >
          <div data-slideout-content></div>
        </gds-dialog>
      </gds-card>
    `}};N.parameters={...N.parameters,docs:{...N.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    density: 'comfortable',
    variant: 'neutral-02-outlined' as TableVariant,
    data: Users.Data,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?height="\${args.height}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="60"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...N.parameters?.docs?.source}}};I.parameters={...I.parameters,docs:{...I.parameters?.docs,source:{originalSource:`{
  args: {
    columns: ExpandableFinance.Columns,
    data: ExpandableFinance.Data,
    tfoot: ExpandableFinance.Tfoot,
    height: '480px',
    variant: 'neutral-02-outlined' as TableVariant,
    density: 'comfortable',
    striped: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table automatically adds a left-most toggle column when rows are expandable.

This example demonstrates mixed rows where some rows are expandable and others are not.
Expanded rows are rendered as aligned child rows so all columns remain in the same grid.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const slideoutRef = createRef<any>();
    const wrapperBackground: 'neutral-01' | 'neutral-02' = args.variant === 'neutral-02' ? 'neutral-02' : 'neutral-01';
    let currentRows: any[] = [];
    let selectedCount = 0;
    let slideoutRows: any[] = [];
    let slideoutIndex = 0;
    const setSlideoutNavState = () => {
      const dialog = slideoutRef.value;
      if (!dialog) return;
      const previousButton = dialog.querySelector('[data-slideout-prev]') as HTMLElement | null;
      const nextButton = dialog.querySelector('[data-slideout-next]') as HTMLElement | null;
      if (previousButton) {
        previousButton.toggleAttribute('disabled', slideoutIndex <= 0);
      }
      if (nextButton) {
        nextButton.toggleAttribute('disabled', slideoutIndex >= slideoutRows.length - 1);
      }
    };
    const renderSlideoutContent = () => {
      const dialog = slideoutRef.value;
      if (!dialog) return;
      const contentHost = dialog.querySelector('[data-slideout-content]') as HTMLElement | null;
      if (!contentHost) return;
      if (!slideoutRows.length) {
        dialog.heading = 'Account details';
        render(html\`<gds-text>No account selected.</gds-text>\`, contentHost);
        setSlideoutNavState();
        return;
      }
      const row = slideoutRows[slideoutIndex];
      dialog.heading = String(row.account ?? 'Account details');
      render(html\`
          <gds-flex flex-direction="column" gap="m">
            <gds-card
              flex-direction="row"
              variant="neutral-02-outlined"
              justify-content="space-between"
              align-items="center"
              padding="s l s s"
            >
              <gds-flex gap="2xs" align-items="center">
                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Previous account"
                  data-slideout-prev
                  @click=\${() => {
        if (slideoutIndex <= 0) return;
        slideoutIndex -= 1;
        renderSlideoutContent();
      }}
                >
                  <gds-icon-chevron-left></gds-icon-chevron-left>
                </gds-button>

                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Next account"
                  data-slideout-next
                  @click=\${() => {
        if (slideoutIndex >= slideoutRows.length - 1) return;
        slideoutIndex += 1;
        renderSlideoutContent();
      }}
                >
                  <gds-icon-chevron-right></gds-icon-chevron-right>
                </gds-button>
              </gds-flex>

              <gds-text font="detail-xs-book" color="neutral-02">
                \${slideoutIndex + 1} / \${slideoutRows.length}
              </gds-text>
            </gds-card>

            <gds-grid columns="1; m{2}" gap="s">
              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Owner</gds-text>
                  <gds-text font="detail-s-bold">\${String(row.owner)}</gds-text>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Status</gds-text>
                  <gds-text font="detail-s-bold"
                    >\${String(row.status)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Booked</gds-text>
                  <gds-formatted-number
                    .value=\${Number(row.amount)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Balance</gds-text>
                  <gds-formatted-number
                    .value=\${Number(row.available)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>
            </gds-grid>
          </gds-flex>
        \`, contentHost);
      setSlideoutNavState();
    };
    const openSlideoutForRow = (rowId: string | number) => {
      const rowIndex = currentRows.findIndex(row => row.id === rowId);
      if (rowIndex < 0) return;
      slideoutRows = currentRows;
      slideoutIndex = rowIndex;
      renderSlideoutContent();
      slideoutRef.value?.show?.('row-action');
    };
    const renderSlots = () => {
      const table = tableRef.value;
      if (!table) return;
      render(html\`
          \${ExpandableFinance.SlotContent(currentRows)}
          \${ExpandableFinance.TfootSlotContent(currentRows)}
          \${currentRows.map((row: any) => html\`
              <gds-button
                slot="actions:\${row.id}:main"
                rank="secondary"
                size="small"
                @click=\${() => openSlideoutForRow(row.id)}
              >
                Open details
              </gds-button>
            \`)}
          \${selectedCount > 0 ? html\`
                <gds-button slot="header-trail" rank="secondary" size="small">
                  <gds-icon-cloud-download
                    slot="lead"
                  ></gds-icon-cloud-download>
                  Export data (\${selectedCount})
                </gds-button>
              \` : null}
        \`, table);
    };
    const handleDataLoaded = (e: CustomEvent) => {
      currentRows = e.detail.rows;
      renderSlots();
      renderSlideoutContent();
    };
    const handleSelection = (e: CustomEvent) => {
      selectedCount = Number(e.detail?.count ?? 0);
      renderSlots();
    };
    return html\`
      <gds-card
        level="1"
        background="\${wrapperBackground}"
        padding="l"
        border-radius="m"
      >
        <gds-table
          \${ref(tableRef)}
          .density="\${args.density}"
          .columns="\${args.columns}"
          .data="\${args.data}"
          .tfoot="\${args.tfoot}"
          rows="10"
          .height="\${args.height}"
          .variant="\${args.variant}"
          selectable
          searchable
          responsive
          settings
          ?striped="\${args.striped}"
          @gds-table-data-loaded=\${handleDataLoaded}
          @gds-table-selection=\${handleSelection}
        >
        </gds-table>

        <gds-dialog
          \${ref(slideoutRef)}
          variant="slide-out"
          heading="Account details"
        >
          <div data-slideout-content></div>
        </gds-dialog>
      </gds-card>
    \`;
  }
}`,...I.parameters?.docs?.source}}};M.parameters={...M.parameters,docs:{...M.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    selectable: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`selectable\\\` boolean enables row selection with checkboxes. Supports individual row selection, select all via header checkbox, and partial selection indication.

Emits \\\`gds-table-selection\\\` event with selected row data and indices. Provides methods for programmatic selection control.

See Developer Guide for complete selection API documentation.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?selectable="\${args.selectable}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...M.parameters?.docs?.source}}};B.parameters={...B.parameters,docs:{...B.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
#### Search Functionality
The \\\`searchable\\\` property adds a search input to the table, enabling real-time filtering across all columns.

#### Column Settings
The \\\`settings\\\` property adds a column visibility dropdown, allowing users to show/hide specific columns.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...B.parameters?.docs?.source}}};F.parameters={...F.parameters,docs:{...F.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    searchable: true,
    settings: true,
    nocache: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`nocache\\\` property provides direct control over data retrieval. When set to \\\`true\\\`, the table bypasses its internal caching mechanism, fetching fresh data on each request. By default, the table caches sorted and filtered results for 5 minutes to improve performance.

> Note: The example is simulated with a mock data provider introducing a 1000ms delay to demonstrate real-world data fetching scenarios.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?nocache="\${args.nocache}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...F.parameters?.docs?.source}}};j.parameters={...j.parameters,docs:{...j.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Sorting is enabled by setting \\\`sortable: true\\\` on column configuration. <br />
Clicking a sortable column header toggles between ascending and descending order, resetting the table to the first page. <br />
The sorting mechanism generates a unique cache key, caching sorted results for 5 minutes, and emits a \\\`gds-sort-change\\\` event.

This story shows two tables:
1) Regular sorting (internal state)
2) URL-driven sorting using per-column \\\`sorting\\\` state (controlled mode)
        \`
      }
    }
  },
  render: args => {
    const regularTableRef = createRef<any>();
    const urlTableRef = createRef<any>();
    const sortColumnRef = createRef<any>();
    const sortDirectionRef = createRef<any>();
    const getSortFromUrl = (): {
      sortColumn: string;
      sortDirection: 'asc' | 'desc';
    } | null => {
      const params = new URLSearchParams(window.location.search);
      const sortColumn = params.get('sortColumn') || '';
      const sortDirection = params.get('sortDirection');
      if (sortDirection !== 'asc' && sortDirection !== 'desc') return null;
      if (!args.columns.some((column: any) => column.key === sortColumn)) {
        return null;
      }
      return {
        sortColumn,
        sortDirection
      };
    };
    const withControlledSorting = (columns: any[], sortColumn?: string, sortDirection?: 'asc' | 'desc') => columns.map(column => ({
      ...column,
      sorting: sortColumn && column.key === sortColumn ? sortDirection : undefined
    }));
    const sortableColumns = args.columns.filter((column: any) => column.sortable);
    const initialSort = getSortFromUrl();
    let selectedSortColumn = initialSort?.sortColumn || sortableColumns[0]?.key || '';
    let selectedSortDirection: 'asc' | 'desc' = initialSort?.sortDirection || 'asc' as 'asc' | 'desc';
    let controlledColumns = withControlledSorting(args.columns, selectedSortColumn, selectedSortDirection);
    const applyControlledSort = (sortColumn = selectedSortColumn, sortDirection = selectedSortDirection) => {
      if (!sortColumn || sortDirection !== 'asc' && sortDirection !== 'desc') {
        return;
      }
      if (!args.columns.some((column: any) => column.key === sortColumn)) {
        return;
      }
      selectedSortColumn = sortColumn;
      selectedSortDirection = sortDirection;
      const url = new URL(window.location.href);
      url.searchParams.set('sortColumn', sortColumn);
      url.searchParams.set('sortDirection', sortDirection);
      window.history.replaceState({}, '', url);
      controlledColumns = withControlledSorting(args.columns, sortColumn, sortDirection);
      const table = urlTableRef.value;
      if (!table) return;
      table.columns = controlledColumns;
    };
    const handleRegularDataLoaded = (e: CustomEvent) => {
      const table = regularTableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handleUrlDataLoaded = (e: CustomEvent) => {
      const table = urlTableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const getDropdownValue = (el: any): string => {
      const value = el?.value;
      if (typeof value === 'string') return value;
      if (Array.isArray(value)) return String(value[0] ?? '');
      return String(value ?? '');
    };
    const handleControlledDropdownChange = () => {
      const nextColumn = getDropdownValue(sortColumnRef.value);
      const nextDirection = getDropdownValue(sortDirectionRef.value);
      if (nextDirection === 'asc' || nextDirection === 'desc') {
        applyControlledSort(nextColumn, nextDirection);
      }
    };
    const handleControlledSortChange = (e: CustomEvent) => {
      const sortColumn = String(e.detail?.sortColumn ?? '');
      const sortDirection = e.detail?.sortDirection as 'asc' | 'desc';
      if (!sortColumn || sortDirection !== 'asc' && sortDirection !== 'desc') {
        return;
      }
      const url = new URL(window.location.href);
      url.searchParams.set('sortColumn', sortColumn);
      url.searchParams.set('sortDirection', sortDirection);
      window.history.replaceState({}, '', url);
      selectedSortColumn = sortColumn;
      selectedSortDirection = sortDirection;
      if (sortColumnRef.value) {
        sortColumnRef.value.setAttribute('value', selectedSortColumn);
      }
      if (sortDirectionRef.value) {
        sortDirectionRef.value.setAttribute('value', selectedSortDirection);
      }
      controlledColumns = withControlledSorting(args.columns, sortColumn, sortDirection);
      const table = urlTableRef.value;
      if (!table) return;
      table.columns = controlledColumns;
    };
    return html\`
      <gds-grid
        columns="1; l{1}; xl{2}"
        gap="6xl; l{6xl} xl{4xl}"
        width="100%"
        padding="4xl"
      >
        <gds-table
          \${ref(regularTableRef)}
          headline="Regular sorting"
          summary="Header clicks update internal table sort state."
          searchable
          .columns="\${args.columns}"
          .data="\${args.data}"
          @gds-table-data-loaded=\${handleRegularDataLoaded}
        >
        </gds-table>

        <gds-table
          \${ref(urlTableRef)}
          headline="URL-driven controlled sorting"
          summary="Uses per-column sorting state controlled via header slot inputs."
          searchable
          .columns="\${controlledColumns}"
          .data="\${args.data}"
          @gds-table-data-loaded=\${handleUrlDataLoaded}
          @gds-sort-change=\${handleControlledSortChange}
        >
          <gds-dropdown
            slot="header-trail"
            \${ref(sortColumnRef)}
            plain
            size="small"
            width="100px"
            label="Sort column"
            value="\${selectedSortColumn}"
            @change=\${() => handleControlledDropdownChange()}
          >
            \${sortableColumns.map((column: any) => html\`
                <gds-option value="\${String(column.key)}">
                  \${String(column.label)}
                </gds-option>
              \`)}
          </gds-dropdown>

          <gds-dropdown
            slot="header-trail"
            \${ref(sortDirectionRef)}
            plain
            size="small"
            width="100px"
            label="Sort direction"
            value="\${selectedSortDirection as any}"
            @change=\${() => handleControlledDropdownChange()}
          >
            <gds-option value="asc">Ascending</gds-option>
            <gds-option value="desc">Descending</gds-option>
          </gds-dropdown>
        </gds-table>
      </gds-grid>
    \`;
  }
}`,...j.parameters?.docs?.source}}};O.parameters={...O.parameters,docs:{...O.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table supports multiple cell types: badge, image, button, link, context menu, and formatted text (number, account, date).

Cell types correspond to their respective Green Design System components and accept their available properties for customization.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...O.parameters?.docs?.source}}};V.parameters={...V.parameters,docs:{...V.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Feedback.Columns,
    data: Feedback.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
**Column justification** <br />
Default alignment is left (start), with options to justify content to the right (end), and supports space-between layouts. <br /> Example: in email columns where cell value and action buttons need clear distinction and consistent alignment across rows.

---

**Column width and wrapping** <br />
Columns can have custom width. When width is set, content will wrap within the cell.

---

**Cell vertical alignment** <br />
Cells can be vertically aligned to start (default centered). Recommended to align to start when long or wrapped content is present, ensuring consistent top alignment for labels or values across rows.

---

**Sortable columns** <br />
When justified to the right, sorting indicator appears on the left. Column label positioned to the right of the indicator.
        \`
      }
    }
  },
  render: args => html\`
    <gds-table .columns="\${args.columns}" .data="\${args.data}"></gds-table>
  \`
}`,...V.parameters?.docs?.source}}};K.parameters={...K.parameters,docs:{...K.parameters?.docs,source:{originalSource:`{
  args: {
    columns: ActionsData.Columns,
    data: ActionsData.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Actions are now implemented as a regular column using slot composition.

Add an \\\`actions\\\` column to \\\`columns\\\`, put \\\`actions: Slot(undefined, ['main'])\\\` in row data,
and render content via the \\\`actions:rowKey:main\\\` slot.

Use \\\`sticky: 'right'\\\` on the \\\`actions\\\` column when you want the action column pinned.
        \`
      }
    }
  },
  render: args => {
    const multiRef = createRef<any>();
    const ctxRef = createRef<any>();
    const linkRef = createRef<any>();
    return html\`
      <gds-table
        \${ref(multiRef)}
        plain
        headline="Multiple Actions"
        summary="Multiple interactive elements in a single actions cell."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = multiRef.value;
      if (table) render(ActionsData.MultipleActionsSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(ctxRef)}
        plain
        headline="Context Menu"
        summary="Actions grouped inside a context menu trigger."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = ctxRef.value;
      if (table) render(ActionsData.ActionContextMenuSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>

      <br />
      <br />
      <br />

      <gds-table
        \${ref(linkRef)}
        plain
        headline="Link Action"
        summary="Single button element as a row action."
        .rows=\${2}
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${(e: CustomEvent) => {
      const table = linkRef.value;
      if (table) render(ActionsData.ActionLinkSlotContent(e.detail.rows), table);
    }}
      >
      </gds-table>
    \`;
  }
}`,...K.parameters?.docs?.source}}};H.parameters={...H.parameters,docs:{...H.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    density: 'comfortable',
    variant: 'neutral-02-outlined' as TableVariant,
    data: Users.Data,
    tfoot: Users.Tfoot,
    selectable: true,
    searchable: true,
    searchLabel: 'Search users',
    settings: true,
    plain: false,
    height: '80vh',
    responsive: false,
    nocache: false,
    striped: false
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`tfoot\\\` property adds a \\\`<tfoot>\\\` footer row at the bottom of the table for displaying aggregated values (sums, averages, counts, etc.).

The footer row is fully slot-based, the table provides insertion points for each visible column and you decide what to render (formatted numbers, badges, text, or nothing).

When a \\\`label\\\` is provided, the first cell renders as a \\\`<th scope="row">\\\` for proper accessibility semantics.

#### Configuration

\\\`\\\`\\\`typescript
const tfoot = {
  label: 'Total',        // Label shown in the first cell (renders as <th scope="row">)
  sticky: true,           // Pin to the bottom of the scroll area
}
\\\`\\\`\\\`

#### Slot Convention

Table footer slots use the naming pattern \\\`tfoot:{columnKey}\\\`:

\\\`\\\`\\\`html
<gds-formatted-number slot="tfoot:amount" .value=\\\${sum}></gds-formatted-number>
<gds-badge slot="tfoot:status" size="small">\\\${count} active</gds-badge>
\\\`\\\`\\\`

Compute your aggregation in the \\\`gds-table-data-loaded\\\` handler and render slot content into the table.

When \\\`sticky: true\\\`, the footer stays pinned to the bottom of the scroll area combined with the sticky header, this creates a fixed frame around the scrollable data.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(html\`\${Users.SlotContent(e.detail.rows)}
          \${Users.TfootSlotContent(e.detail.rows)}\`, table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        density="\${args.density}"
        variant="\${args.variant}"
        search-label="\${args.searchLabel}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        ?plain="\${args.plain}"
        ?responsive="\${args.responsive}"
        ?nocache="\${args.nocache}"
        ?striped="\${args.striped}"
        ?selectable="\${args.selectable}"
        height="\${args.height}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        .tfoot="\${args.tfoot}"
        rows="50"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...H.parameters?.docs?.source}}};W.parameters={...W.parameters,docs:{...W.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    headlineTag: 'h2',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`headline\\\` and \\\`summary\\\` properties are set, they will be displayed at the top of the table.

You can change the \\\`headline-tag\\\` to any valid heading tag (e.g., 'h1-h5') to customize the semantic structure without altering the appearance.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        headline-tag="\${args.headlineTag}"
        summary="\${args.summary}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...W.parameters?.docs?.source}}};q.parameters={...q.parameters,docs:{...q.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    plain: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`plain\\\` boolean is set, the table header and footer are removed, rendering only the data table.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?plain="\${args.plain}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...q.parameters?.docs?.source}}};G.parameters={...G.parameters,docs:{...G.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    striped: true,
    density: 'compact'
  },
  parameters: {
    docs: {
      description: {
        story: \`
When \\\`striped\\\` boolean is set, alternating row background colors are applied to even-numbered rows.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .density="\${args.density}"
        ?striped="\${args.striped}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="20"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...G.parameters?.docs?.source}}};_.parameters={..._.parameters,docs:{..._.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    density: 'compact',
    headline: 'User Management',
    summary: 'Overview of all users in the system'
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`density\\\` property controls the table's visual spacing and affects the table's **built-in UI** (search input, column settings dropdown, pagination).

- **\\\`comfortable\\\`** (Default) · Standard spacing for balanced readability
- **\\\`compact\\\`** · Reduced spacing to maximize information density
- **\\\`spacious\\\`** · Increased spacing for enhanced readability

> **Slot content sizing** The density mode does **not** automatically resize components you provide via slots. When using a specific density, size your slotted components accordingly. For example, use \\\`size="small"\\\` on badges and buttons in \\\`compact\\\` or \\\`comfortable\\\` mode, and \\\`size="medium"\\\` in \\\`spacious\\\` mode to maintain visual consistency.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        headline="\${args.headline}"
        summary="\${args.summary}"
        searchable
        settings
        .density="\${args.density}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,..._.parameters?.docs?.source}}};Q.parameters={...Q.parameters,docs:{...Q.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    variant: 'neutral-02-outlined' as TableVariant,
    density: 'comfortable'
  },
  argTypes: {
    variant: {
      control: 'select',
      options: ['neutral-01', 'neutral-02', 'neutral-02-outlined'] as TableVariant[]
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
The \\\`variant\\\` property controls the visual style of the table container card.
Available variants: \\\`neutral-01\\\`, \\\`neutral-02\\\`, \\\`neutral-02-outlined\\\` (Default)
        \`
      }
    }
  },
  render: args => {
    const wrapperBackground: 'neutral-01' | 'neutral-02' = args.variant === 'neutral-02' ? 'neutral-02' : 'neutral-01';
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-card
        level="1"
        background="\${wrapperBackground}"
        padding="l"
        border-radius="m"
      >
        <gds-table
          \${ref(tableRef)}
          .density="\${args.density}"
          .variant="\${args.variant}"
          .columns="\${args.columns}"
          .data="\${args.data}"
          searchable
          settings
          selectable
          rows="5"
          @gds-table-data-loaded=\${handleDataLoaded}
        >
        </gds-table>
      </gds-card>
    \`;
  }
}`,...Q.parameters?.docs?.source}}};Y.parameters={...Y.parameters,docs:{...Y.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    responsive: true
  },
  parameters: {
    docs: {
      description: {
        story: \`
When the \\\`responsive\\\` boolean is set to \\\`true\\\`, the table transforms its layout for smaller screens:

- **Mobile Layout**: Rows are restructured into a card-like format
- **Column Presentation**:
  - Column names are displayed on the left side
  - Corresponding cell values are shown on the right side
- **Header Behavior**: On larger screens table header becomes sticky, ensuring column labels remain visible during scrolling.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        ?responsive="\${args.responsive}"
        .columns="\${args.columns}"
        .data="\${args.data}"
        height="80vh"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...Y.parameters?.docs?.source}}};X.parameters={...X.parameters,docs:{...X.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Use slots to add custom controls to the table header, footer, and content areas.

- **header-lead**: Add filtering/sorting controls at the start of the header.
- **header-trail**: Add actions (e.g., export buttons) at the end of the header.
- **footer**: Replace the default pagination footer with custom content.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        rows="4"
        settings
        searchable
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-dropdown size="small" slot="header-lead" plain searchable>
          <span slot="trigger">Sorting</span>
          <gds-option value="by-name">Sort by Name</gds-option>
          <gds-option value="by-status">Sort by Status</gds-option>
        </gds-dropdown>
        <gds-button slot="header-trail" rank="secondary" size="small">
          Export
        </gds-button>
      </gds-table>
    \`;
  }
}`,...X.parameters?.docs?.source}}};J.parameters={...J.parameters,docs:{...J.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    simulatedState: 'normal'
  },
  argTypes: {
    simulatedState: {
      control: 'select',
      options: ['normal', 'error', 'empty', 'no-results'],
      description: 'Simulate different table states'
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
Override default error, empty, and no-results states with custom content.

- **error**: Displayed when data loading fails.
- **empty**: Displayed when no data is available.
- **no-results**: Displayed when search returns no results.

Each slot provides a default fallback if not specified.

Use the **Simulated State** control below to switch between states.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const dataProvider = async (request: any) => {
      await new Promise(resolve => setTimeout(resolve, 500));
      if (args.simulatedState === 'error') {
        throw new Error('Simulated error state');
      }
      if (args.simulatedState === 'empty') {
        return {
          rows: [],
          total: 0
        };
      }
      if (args.simulatedState === 'no-results') {
        return {
          rows: [],
          total: 0
        };
      }
      return args.data(request);
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handleRetry = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`retry-\${Date.now()}\`;
      }
    };
    const handleClearSearch = () => {
      args.simulatedState = 'normal';
      if (tableRef.value) {
        tableRef.value.dataLoadKey = \`clear-\${Date.now()}\`;
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = '';
        }
      }
    };

    // Simulate search query for no-results state
    setTimeout(() => {
      if (args.simulatedState === 'no-results' && tableRef.value) {
        const searchInput = tableRef.value.shadowRoot?.querySelector('gds-input');
        if (searchInput) {
          searchInput.value = 'test search';
          searchInput.dispatchEvent(new Event('input', {
            bubbles: true
          }));
        }
      }
    }, 100);
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${dataProvider}"
        .dataLoadKey="\${args.simulatedState}"
        rows="4"
        searchable
        settings
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-flex
          slot="error"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">Unable to load data</gds-text>
          <gds-text font="detail-m-book">
            An error occurred while loading the data. Please try again.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleRetry}>
            Retry
          </gds-button>
        </gds-flex>

        <gds-flex
          slot="empty"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No data available</gds-text>
          <gds-text font="detail-m-book">
            There are currently no records to display in this table.
          </gds-text>
        </gds-flex>

        <gds-flex
          slot="no-results"
          flex-direction="column"
          gap="s"
          align-items="flex-start"
        >
          <gds-text font="heading-s">No search results</gds-text>
          <gds-text font="detail-m-book">
            Your search did not match any records. Try different keywords.
          </gds-text>
          <gds-button rank="secondary" size="small" @click=\${handleClearSearch}>
            Clear search
          </gds-button>
        </gds-flex>
      </gds-table>
    \`;
  }
}`,...J.parameters?.docs?.source}}};Z.parameters={...Z.parameters,docs:{...Z.parameters?.docs,source:{originalSource:`{
  parameters: {
    docs: {
      description: {
        story: \`
Demonstrates the two loading states the table produces during data fetching.

- **Skeleton**: Shown on initial load (or when navigating to an uncached page). The table renders placeholder rows with animated skeleton cells until the data resolves.
- **Fade**: When navigating between pages that are not yet cached, the existing rows fade while the new data loads in the background.
        \`
      }
    }
  },
  render: () => {
    const skeletonRef = createRef<any>();
    const fadeRef = createRef<any>();
    const skeletonProvider = async (_request: any) => new Promise<never>(() => undefined);
    const fadeProvider = async (request: any) => {
      await new Promise(resolve => setTimeout(resolve, 1500));
      return Users.Data(request);
    };
    const handleFadeDataLoaded = (e: CustomEvent) => {
      const table = fadeRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-flex flex-direction="column" gap="4xl">
        <gds-table
          \${ref(skeletonRef)}
          nocache
          headline="Skeleton"
          summary="Simulates the initial loading state with animated skeleton placeholders."
          rows="4"
          .columns="\${Users.Columns}"
          .data="\${skeletonProvider}"
        ></gds-table>
        <gds-table
          \${ref(fadeRef)}
          nocache
          headline="Fade"
          summary="Existing rows fade out while new data is loading when switching between pages."
          rows="4"
          .columns="\${Users.Columns}"
          .data="\${fadeProvider}"
          @gds-table-data-loaded=\${handleFadeDataLoaded}
        ></gds-table>
      </gds-flex>
    \`;
  }
}`,...Z.parameters?.docs?.source}}};ee.parameters={...ee.parameters,docs:{...ee.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data,
    headline: 'User Management',
    summary: 'Overview of all users in the system',
    selectable: true,
    searchable: true,
    settings: true,
    density: 'comfortable'
  },
  parameters: {
    docs: {
      description: {
        story: \`
Slot composition lets you render custom components inside table cells using standard web component slots.

Each row needs a **unique key** for slot naming set via the \\\`key\\\` parameter in \\\`Slot()\\\`, or it falls back to \\\`row.id\\\`, then the row index.

#### 1. Compose data with \\\`Slot()\\\`

\\\`\\\`\\\`ts
// With explicit unique key (value, slots, key)
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value'], row.accountId),
  email: Slot(row.email, ['value', 'copy-button'], row.accountId),
  status: Slot(row.status, ['status'], row.accountId),
}))

// Without key - falls back to row.id, then row index
const data = users.map(row => ({
  ...row,
  name: Slot(row.name, ['avatar', 'value']),
  email: Slot(row.email, ['value', 'copy-button']),
  status: Slot(row.status, ['status']),
}))
\\\`\\\`\\\`

The arguments are positional: \\\`Slot(value, slots, key)\\\` or \\\`Slot(value, key, slots)\\\`. The **value** is used for sorting/search, **slots** is an ordered array of slot ids (include \\\`'value'\\\` to render the plain text alongside your components), and **key** is an optional \\\`string | number\\\` used as a stable row identifier.

#### 2. Render slot content on data load

\\\`\\\`\\\`ts
@gds-table-data-loaded=\\\${(e) => {
  render(slotContent(e.detail.rows), tableEl)
}}
\\\`\\\`\\\`

#### 3. Slot naming convention

The slot name follows the pattern \\\`columnKey:rowKey:slotId\\\`.

\\\`\\\`\\\`html
<!-- When key is set to row.accountId (e.g. "ACC-1042") -->
<gds-badge slot="status:ACC-1042:status" variant="positive">Active</gds-badge>
<gds-button slot="email:ACC-1042:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key is set and row.id is 42 -->
<gds-badge slot="status:42:status" variant="positive">Active</gds-badge>
<gds-button slot="email:42:copy-button" rank="tertiary" size="small">
  <gds-icon-copy></gds-icon-copy>
</gds-button>

<!-- When no key and no row.id - falls back to row index (1-based) -->
<gds-badge slot="status:1:status" variant="positive">Active</gds-badge>
\\\`\\\`\\\`

Only the current page's slot content is rendered - previous elements are replaced on page change.

> See the **Developer Guide** for full details on framework integration and the \\\`Slot()\\\` API.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .headline="\${args.headline}"
        .summary="\${args.summary}"
        ?selectable="\${args.selectable}"
        ?searchable="\${args.searchable}"
        ?settings="\${args.settings}"
        density="\${args.density}"
        responsive
        rows="5"
        @gds-table-data-loaded=\${handleDataLoaded}
      >
      </gds-table>
    \`;
  }
}`,...ee.parameters?.docs?.source}}};te.parameters={...te.parameters,docs:{...te.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
The table component dispatches events for various user interactions.

**Pagination Events**
- **gds-page-change**: Fired when the active page changes. Detail: \\\`{ page: number }\\\`
- **gds-rows-change**: Fired when the rows per page value changes. Detail: \\\`{ rows: number }\\\`

**Sorting Events**
- **gds-sort-change**: Fired when sorting changes. Detail: \\\`{ sortColumn: string, sortDirection: 'asc' | 'desc' }\\\`

**Data Events**
- **gds-table-data-loaded**: Fired when data is successfully loaded
- **gds-table-data-error**: Fired when data loading fails
- **gds-table-selection**: Fired when row selection changes

**Settings Events**
- **gds-column-visibility-change**: Fired when the user toggles column visibility via the settings dropdown. Detail: \\\`{ visibleColumns: string[] }\\\`

\\\`\\\`\\\`js
table.addEventListener('gds-column-visibility-change', (e) => {
  // e.detail.visibleColumns — string[] of currently visible column keys
  // any key absent from this array is hidden
  console.log(e.detail.visibleColumns)
})
\\\`\\\`\\\`

This example demonstrates listening to pagination, sorting, and column visibility events and displaying the current state.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const initialVisible = (args.columns as any[]).filter(c => c.visible !== false).map(c => c.key);
    const state = {
      currentPage: 1,
      currentRows: 10,
      sortColumn: '',
      sortDirection: 'asc',
      visibleColumns: initialVisible.join(', ')
    };
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handlePageChange = (e: CustomEvent) => {
      console.log('🔔 Page change event:', e.detail);
      state.currentPage = e.detail.page;
      const pageEl = document.querySelector('#page-status');
      if (pageEl) {
        pageEl.textContent = \`Page: \${state.currentPage}\`;
      }
    };
    const handleRowsChange = (e: CustomEvent) => {
      console.log('🔔 Rows change event:', e.detail);
      state.currentRows = e.detail.rows;
      const rowsEl = document.querySelector('#rows-status');
      if (rowsEl) {
        rowsEl.textContent = \`Rows: \${state.currentRows}\`;
      }
    };
    const handleSortChange = (e: CustomEvent) => {
      console.log('🔔 Sort change event:', e.detail);
      state.sortColumn = e.detail.sortColumn;
      state.sortDirection = e.detail.sortDirection;
      const sortEl = document.querySelector('#sort-status');
      if (sortEl) {
        sortEl.textContent = \`\${state.sortColumn} (\${state.sortDirection})\`;
      }
    };
    const handleColumnVisibilityChange = (e: CustomEvent) => {
      console.log('🔔 Column visibility change event:', e.detail);
      state.visibleColumns = e.detail.visibleColumns.join(', ');
      const colEl = document.querySelector('#column-visibility-status');
      if (colEl) colEl.textContent = state.visibleColumns;
    };
    return html\`
      <gds-flex flex-direction="column" gap="l">
        <gds-flex flex-direction="column" gap="m">
          <gds-card variant="neutral-02" padding="m" border-radius="m" outline>
            <gds-flex flex-direction="column" gap="s">
              <gds-text font="heading-s">Event Monitor</gds-text>
              <gds-text font="detail-s-book" color="neutral-01">
                Open your browser console (F12) to see event logs
              </gds-text>
              <gds-flex gap="m">
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Current Page
                  </gds-text>
                  <gds-text
                    id="page-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Page: \${state.currentPage}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Rows Per Page
                  </gds-text>
                  <gds-text
                    id="rows-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    Rows: \${state.currentRows}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Sort Column
                  </gds-text>
                  <gds-text
                    id="sort-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    \${state.sortColumn ? \`\${state.sortColumn} (\${state.sortDirection})\` : 'None'}
                  </gds-text>
                </gds-flex>
                <gds-flex flex-direction="column" gap="xs" flex="1">
                  <gds-text font="detail-s-book" color="neutral-01">
                    Visible Columns
                  </gds-text>
                  <gds-text
                    id="column-visibility-status"
                    font="body-m-regular"
                    color="primary"
                  >
                    \${state.visibleColumns}
                  </gds-text>
                </gds-flex>
              </gds-flex>
            </gds-flex>
          </gds-card>
        </gds-flex>

        <gds-table
          \${ref(tableRef)}
          .columns="\${args.columns}"
          .data="\${args.data}"
          rows="5"
          selectable
          searchable
          settings
          @gds-table-data-loaded=\${handleDataLoaded}
          @gds-page-change=\${(e: CustomEvent) => handlePageChange(e)}
          @gds-rows-change=\${(e: CustomEvent) => handleRowsChange(e)}
          @gds-sort-change=\${(e: CustomEvent) => handleSortChange(e)}
          @gds-column-visibility-change=\${handleColumnVisibilityChange}
        >
        </gds-table>
      </gds-flex>
    \`;
  }
}`,...te.parameters?.docs?.source}}};ne.parameters={...ne.parameters,docs:{...ne.parameters?.docs,source:{originalSource:`{
  args: {
    rows: 8,
    options: [8, 20]
  },
  parameters: {
    docs: {
      description: {
        story: \`
      Demonstrates a pagination edge case where the selected page size exceeds the total number of rows.

1. Dataset has 13 rows total
2. Initial page size is 8
3. Change rows per page to 20

In this edge case, page navigation is hidden when only one page remains, while the rows-per-page selector stays visible so users can switch back to a smaller page size.
        \`
      }
    }
  },
  render: args => {
    const columns = [{
      key: 'id',
      label: 'ID',
      sortable: true
    }, {
      key: 'name',
      label: 'Name',
      sortable: true
    }, {
      key: 'status',
      label: 'Status',
      sortable: true
    }];
    const rows = Array.from({
      length: 13
    }, (_, i) => ({
      id: i + 1,
      name: \`User \${i + 1}\`,
      status: i % 2 === 0 ? 'Active' : 'Inactive'
    }));
    const dataProvider = async (request: any) => {
      const start = (request.page - 1) * request.rows;
      const end = start + request.rows;
      return {
        rows: rows.slice(start, end),
        total: rows.length
      };
    };
    return html\`
      <gds-table
        headline="Pagination edge case"
        summary="13 rows total with page-size switching between 8 and 20."
        .columns=\${columns}
        .data=\${dataProvider}
        .rows=\${args.rows}
        .options=\${args.options}
      >
      </gds-table>
    \`;
  }
}`,...ne.parameters?.docs?.source}}};ae.parameters={...ae.parameters,docs:{...ae.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Replaces the built-in table footer with a compact \\\`gds-pagination\\\` in \\\`simple\\\` mode by using the \\\`footer\\\` slot. <br/>
This is the recommended approach when you want minimal pagination UI, just prev/next arrows and a short range label.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const rowsPerPage = 8;
    const total = 120;
    let page = 1;
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    const handlePageChange = (e: CustomEvent) => {
      page = e.detail.page;
      const table = tableRef.value;
      if (table) table.page = page;
      const pagination = document.querySelector('[data-pagination-simple-table]');
      if (pagination) pagination.setAttribute('page', String(page));
    };
    return html\`
      <gds-table
        \${ref(tableRef)}
        .columns="\${args.columns}"
        .data="\${args.data}"
        .rows="\${rowsPerPage}"
        searchable
        settings
        @gds-table-data-loaded=\${handleDataLoaded}
      >
        <gds-pagination
          data-pagination-simple-table
          slot="footer"
          simple
          hide-options
          page="1"
          .rows="\${rowsPerPage}"
          .total="\${total}"
          .options="\${[rowsPerPage]}"
          label="1-\${rowsPerPage} of \${total}"
          @gds-page-change=\${handlePageChange}
        ></gds-pagination>
      </gds-table>
    \`;
  }
}`,...ae.parameters?.docs?.source}}};U.parameters={...U.parameters,docs:{...U.parameters?.docs,source:{originalSource:`{
  args: {
    columns: Users.Columns,
    data: Users.Data
  },
  parameters: {
    docs: {
      description: {
        story: \`
Demonstrates ARIA forwarding for tables, allowing external labels and descriptions to be associated with the internal table element.
        \`
      }
    }
  },
  render: args => {
    const tableRef = createRef<any>();
    const handleDataLoaded = (e: CustomEvent) => {
      const table = tableRef.value;
      if (table) render(Users.SlotContent(e.detail.rows), table);
    };
    return html\`
      <gds-flex flex-direction="column" gap="s">
        <div id="table-label">User Directory</div>
        <div id="table-description">
          A list of all registered users in the system
        </div>
        <gds-table
          \${ref(tableRef)}
          plain
          gds-aria-labelledby="table-label"
          gds-aria-describedby="table-description"
          .columns="\${args.columns}"
          .data="\${args.data}"
          rows="5"
          @gds-table-data-loaded=\${handleDataLoaded}
        >
        </gds-table>
      </gds-flex>
    \`;
  }
}`,...U.parameters?.docs?.source},description:{story:"Demonstrates ARIA forwarding, allowing external labels and descriptions to be associated with the internal table element.\nUses `gds-aria-labelledby` and `gds-aria-describedby` to wire up external accessibility text.",...U.parameters?.docs?.description}}};se.parameters={...se.parameters,docs:{...se.parameters?.docs,source:{originalSource:`{
  args: {
    rowCount: 200,
    height: '70vh',
    density: 'comfortable',
    variant: 'neutral-02-outlined' as TableVariant,
    searchable: true,
    selectable: true,
    settings: true,
    striped: true,
    nocache: true,
    responsive: true
  },
  argTypes: {
    rowCount: {
      control: {
        type: 'select'
      },
      options: [50, 100, 200, 500],
      description: 'Rows per page for the performance test.'
    }
  },
  parameters: {
    docs: {
      description: {
        story: \`
Stress test story for measuring rendering performance with a 1000-row dataset.
Select between 50, 100, 200, and 500 rows per page across 25+ columns with slot-based cells.

Every 5th row has async sub-rows (child rows), and every 5th row offset by 2 has a full-width async expanded panel.
        \`
      }
    }
  },
  render: (args: any) => {
    const tableRef = createRef<any>();
    const slideoutRef = createRef<any>();
    let currentRows: any[] = [];
    let selectedCount = 0;
    let slideoutRows: any[] = [];
    let slideoutIndex = 0;
    const setSlideoutNavState = () => {
      const dialog = slideoutRef.value;
      if (!dialog) return;
      const previousButton = dialog.querySelector('[data-slideout-prev]') as HTMLElement | null;
      const nextButton = dialog.querySelector('[data-slideout-next]') as HTMLElement | null;
      if (previousButton) {
        previousButton.toggleAttribute('disabled', slideoutIndex <= 0);
      }
      if (nextButton) {
        nextButton.toggleAttribute('disabled', slideoutIndex >= slideoutRows.length - 1);
      }
    };
    const renderSlideoutContent = () => {
      const dialog = slideoutRef.value;
      if (!dialog) return;
      const contentHost = dialog.querySelector('[data-slideout-content]') as HTMLElement | null;
      if (!contentHost) return;
      if (!slideoutRows.length) {
        dialog.heading = 'User details';
        render(html\`<gds-text>No user selected.</gds-text>\`, contentHost);
        setSlideoutNavState();
        return;
      }
      const row = slideoutRows[slideoutIndex];
      dialog.heading = String(row.name);
      render(html\`
          <gds-flex flex-direction="column" gap="m">
            <gds-card
              flex-direction="row"
              variant="neutral-02-outlined"
              justify-content="space-between"
              align-items="center"
              padding="s l s s"
            >
              <gds-flex gap="2xs" align-items="center">
                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Previous user"
                  data-slideout-prev
                  @click=\${() => {
        if (slideoutIndex <= 0) return;
        slideoutIndex -= 1;
        renderSlideoutContent();
      }}
                >
                  <gds-icon-chevron-left></gds-icon-chevron-left>
                </gds-button>

                <gds-button
                  rank="tertiary"
                  size="small"
                  aria-label="Next user"
                  data-slideout-next
                  @click=\${() => {
        if (slideoutIndex >= slideoutRows.length - 1) return;
        slideoutIndex += 1;
        renderSlideoutContent();
      }}
                >
                  <gds-icon-chevron-right></gds-icon-chevron-right>
                </gds-button>
              </gds-flex>

              <gds-text font="detail-xs-book" color="neutral-02">
                \${slideoutIndex + 1} / \${slideoutRows.length}
              </gds-text>
            </gds-card>

            <gds-grid columns="1; m{2}" gap="s">
              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Department</gds-text>
                  <gds-text font="detail-s-bold">
                    \${String(row.department)}
                  </gds-text>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Status</gds-text>
                  <gds-text font="detail-s-bold"
                    >\${String(row.status)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Amount</gds-text>
                  <gds-formatted-number
                    .value=\${Number(row.amount)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Balance</gds-text>
                  <gds-formatted-number
                    .value=\${Number(row.balance)}
                  ></gds-formatted-number>
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Region</gds-text>
                  <gds-text font="detail-s-bold"
                    >\${String(row.region)}</gds-text
                  >
                </gds-flex>
              </gds-card>

              <gds-card variant="neutral-02-outlined" padding="s">
                <gds-flex flex-direction="column" gap="2xs">
                  <gds-text font="detail-s-book">Manager</gds-text>
                  <gds-text font="detail-s-bold">
                    \${String(row.manager)}
                  </gds-text>
                </gds-flex>
              </gds-card>
            </gds-grid>
          </gds-flex>
        \`, contentHost);
      setSlideoutNavState();
    };
    const openSlideoutForRow = (rowId: string | number) => {
      const rowIndex = currentRows.findIndex(row => row.id === rowId);
      if (rowIndex < 0) return;
      slideoutRows = currentRows;
      slideoutIndex = rowIndex;
      renderSlideoutContent();
      slideoutRef.value?.show?.('row-action');
    };
    const renderSlots = () => {
      const table = tableRef.value;
      if (!table) return;
      render(html\`
          \${PerformanceData.SlotContent(currentRows)}
          \${currentRows.map((row: any) => html\`
              <gds-button
                slot="actions:\${row.id}:main"
                rank="secondary"
                size="small"
                @click=\${() => openSlideoutForRow(row.id)}
              >
                Open details
              </gds-button>
            \`)}
          \${currentRows.filter((row: any) => typeof row.subRows === 'function').flatMap((row: any) => [html\`
                <gds-button
                  slot="actions:\${row.id}-r1:main"
                  rank="secondary"
                  variant="negative"
                  width="14ch"
                  size="small"
                >
                  Deny
                </gds-button>
              \`, html\`
                <gds-button
                  slot="actions:\${row.id}-r2:main"
                  rank="secondary"
                  variant="positive"
                  width="14ch"
                  size="small"
                >
                  Approve
                </gds-button>
              \`])}
          \${selectedCount > 0 ? html\`
                <gds-button slot="header-trail" rank="secondary" size="small">
                  <gds-icon-cloud-download
                    slot="lead"
                  ></gds-icon-cloud-download>
                  Export data (\${selectedCount})
                </gds-button>
              \` : null}
        \`, table);
    };
    const handleDataLoaded = (e: CustomEvent) => {
      currentRows = e.detail.rows;
      renderSlots();
      renderSlideoutContent();
    };
    const handleSelection = (e: CustomEvent) => {
      selectedCount = Number(e.detail?.count ?? 0);
      renderSlots();
    };
    return html\`
      <gds-card level="1" background="neutral-01" padding="l" border-radius="m">
        <gds-table
          \${ref(tableRef)}
          headline="Performance Test - 1000 rows"
          summary="Stress test with expandable sub-rows and full-width panels."
          .columns=\${PerformanceData.Columns}
          .data=\${PerformanceData.Data(1000)}
          .height="\${args.height}"
          .density="\${args.density}"
          .variant="\${args.variant}"
          search-label="Search"
          ?searchable="\${args.searchable}"
          ?selectable="\${args.selectable}"
          ?settings="\${args.settings}"
          ?striped="\${args.striped}"
          ?nocache="\${args.nocache}"
          ?responsive="\${args.responsive}"
          .rows=\${args.rowCount}
          .options=\${[50, 100, 200, 500]}
          @gds-table-data-loaded=\${handleDataLoaded}
          @gds-table-selection=\${handleSelection}
        >
        </gds-table>

        <gds-dialog
          \${ref(slideoutRef)}
          variant="slide-out"
          heading="User details"
        >
          <div data-slideout-content></div>
        </gds-dialog>
      </gds-card>
    \`;
  }
}`,...se.parameters?.docs?.source}}};const qn=["Overview","Expandable","Selection","Filtering","Caching","Sorting","Cell","Formatting","Actions","Tfoot","Headline","Plain","Striped","Density","Variant","Responsive","Slots","StateSlots","LoadingStates","SlotComposition","Events","EdgeCase","SimplePagination","AriaForwarding","Performance"];export{K as Actions,U as AriaForwarding,F as Caching,O as Cell,_ as Density,ne as EdgeCase,te as Events,I as Expandable,B as Filtering,V as Formatting,W as Headline,Z as LoadingStates,N as Overview,se as Performance,q as Plain,Y as Responsive,M as Selection,ae as SimplePagination,ee as SlotComposition,X as Slots,j as Sorting,J as StateSlots,G as Striped,H as Tfoot,Q as Variant,qn as __namedExportsOrder,Wn as default};
